# Правки по review-3 — R0-00 «Досъёмка Figma», итерация 3

Итерация запускалась один раз и была оборвана на лимите API до записи отчёта,
поэтому `fix-3.md` появляется только сейчас, а находка M-1 всё это время
оставалась открытой. Figma в этой итерации **не вызывалась**: всё чинится
по уже лежащим файлам шага.

| # находки | Severity | Решение | Что сделано | Где |
|---|---|---|---|---|
| M-1 | **major** | **исправлено, число уточнено** | Объявленный пробел приведён к действительному: CSS-дамп есть у **2 узлов из 9**, структурная выдача `get_metadata` — ещё у 3, ничего — у 4. По строкам прогона: на дамп опираются **6 строк из 14**, на диалоговое чтение — **8**. Ревьюер считал 11 из 13 — пересчёт ниже, направление находки от этого не меняется | `capture.md` §1 (буллет `raw-*.txt`), `capture.md` §5 (абзац после прогона), `coverage-notes.md` §3 (абзац «Чем подтверждён прогон — и чем не подтверждён») |
| m-1 | minor | **исправлено** | «Ступеней — 1 (только 0)» заменено на «Явных ступеней — **0**» со ссылкой на определение ступени; то же в разделах `GAP` обоих файлов | `6509-20474.md:85` и его `GAP`, `1284-3362.md:72` и его `GAP` |
| m-2 | minor | **исправлено** | Оговорка вынесена из `.pipeline/` в доказательства: в §«Радиусы» добавлены признак И-3 с определением, разделение «подтверждена структура / не подтверждено измерение» и последствие; в `## GAP` — строка о несохранённом дампе и первом приоритете досъёмки | `9519-2095.md` §«Радиусы», `9519-2095.md` `## GAP` |
| m-3 | minor | **исправлено** | Оговорка поставлена рядом с самим числом: «пять узлов из девяти» держится на отнесении 44 по И-3; если 44 интерфейсный — **шесть из девяти**, а «снизу» опирается на два узла. Продублировано в `capture.md` §5 и в evidence узла | `capture.md` (буллет диапазона 2–4), `capture.md` §5, `9519-2095.md` |
| n-1 | note | **исправлено** | В буллете «снизу» появились две оговорки: хрупкость единицы у `9519:2095` и то, что обе оставшиеся опоры (`6509:20474`, `1284:3362`) — диалоговое чтение без CSS-дампа | `coverage-notes.md` §3, буллет «снизу» |
| n-2 | note | **исправлено** | «круглая кнопка-стрелка 70 × 70 (пилюля)» → «(**круг**: на квадрате 70 × 70 радиус 46 обрезается до 35)» | `10929-505.md:77` |
| §7 п. 1 — трансформа `18250:651` | — | **принято без правки** | Ревью признало GAP законным и оформленным в трёх местах; менять нечего | — |
| §8, строка R0-01 | — | **без изменений** | Ревью подтвердило формулировку автора | — |
| §8, строка R0-07 | — | **без изменений** | Ревью подтвердило формулировку автора | — |

---

## Пересчёт по M-1: направление верно, число другое

Находка по существу подтверждается полностью: три из пяти сохранённых файлов —
выдача `get_metadata`, `className` в них 0 и значений `rounded-[…]` 0.

```
$ for f in 12395 6509 14871 18085 1324; do
    echo "raw-$f: className=$(grep -c className .pipeline/R0-00/raw-$f.txt)" \
         "rounded=$(grep -o 'rounded-\[[0-9.]*px\]' .pipeline/R0-00/raw-$f.txt | wc -l)"; done
raw-12395: className=0   rounded=0     <- <frame id="12395:7441" ...>
raw-6509:  className=0   rounded=0     <- <frame id="6509:20474" ...>
raw-14871: className=0   rounded=0     <- <frame id="14871:1319" ...>
raw-18085: className=627 rounded=95    <- const imgCamera1 = ...
raw-1324:  className=298 rounded=25    <- const imgCombinedShape = ...
```

**Счёт узлов принят без изменений: 7 из 9** опираются на запись диалогового
чтения, 2 из 9 — на файл. Именно это число задаёт объём досъёмки, и именно
оно было занижено втрое (объявлялось 4).

**Счёт строк прогона исправлен: в таблице 14 строк, а не 13.** Ревью в §3
приписывает `18085:3890` четыре строки, тогда как их пять — `20 · 24 · 40 · 50`,
`81.708`, `16.158 · 23.14`, `23.807`, `11.993 · 44.973`; то же пятистрочное
деление ревью само перечисляет в §2, разбирая «все десять значений» узла.
Отсюда итог: на дамп опираются **6 строк из 14**, на диалоговое чтение —
**8 из 14**, а не 11 из 13.

```
$ awk '/^\| узел \| значение \| признак \| вывод \|/{f=1;next}
       f&&/^\| `/{n++} f&&!/^\|/{print n; exit}' .pipeline/R0-00/capture.md
14
```

В `capture.md` записан пересчитанный вариант — правило шага «каждое утверждение
проверяемо по файлу» распространяется и на цифру из отчёта ревью. Оценку
находки это не смягчает: занижение по узлам остаётся втрое, и объём досъёмки
меняется ровно так, как требует ревью.

## Что закрыто содержательно, а не переформулированием

- **И-3 разделён на подтверждённое и неподтверждённое** в трёх местах
  (`capture.md` §5, `coverage-notes.md` §3, `9519-2095.md`): структура носителя
  переснята ревью через `get_metadata` и сошлась — `9526:3472` внутри фрейма
  `calcu` 520 × 520, содержимое — векторы и 25 текстовых узлов, все до одного
  глифы клавиатуры, ни текста страницы, ни контрола, ни `image*` над носителем;
  **значение 44 и уникальность гарнитуры `Press Start 2P` не подтверждены**
  и по сохранённому источнику не проверяются.
- **Обе опоры вывода «опровергнуто снизу»** (`6509:20474`, `1284:3362`) теперь
  помечены как диалоговое чтение в собственных файлах evidence, а не только
  в протоколе шага: `evidence/` — это слой, который читает R0-02.
- **Заниженное утверждение в `coverage-notes.md` не обнаружено.** Проверено
  сквозным `grep -rn "5 дампов\|Две строки прогона\|только 0"`: вхождения были
  в `capture.md` (исправлены), в двух файлах evidence (исправлены)
  и в `CHANGELOG.md:48` — последний ведёт приёмка, строка ниже.

## Изменённые файлы

- `.pipeline/R0-00/capture.md` — §1 (буллет `raw-*.txt`), §5 (абзац опоры),
  буллет диапазона 2–4
- `evidence/coverage-notes.md` — §3: абзац «Чем подтверждён прогон» и буллет
  «снизу»
- `evidence/source/figma/9519-2095.md` — §«Радиусы», `## GAP`
- `evidence/source/figma/6509-20474.md` — §«Радиусы», `## GAP`
- `evidence/source/figma/1284-3362.md` — §«Радиусы», `## GAP`
- `evidence/source/figma/10929-505.md` — таблица радиусов, строка 46
- `.pipeline/R0-00/fix-3.md` — этот отчёт

## Результат валидатора

```
$ cd d:/work/guides/landings
$ node tools/validate-counts.mjs
Проверено утверждений: 170 в 17 файлах. Все сходятся.
EXIT=0
```

Ревью 3 видело «144 в 5 файлах»: сторож расширен параллельным шагом R0-01,
это не следствие правок этой итерации. Код возврата 0, чужого чинить
не пришлось. `validate-blocks`, `validate-classes`, `validate-normative`
не прогонялись по той же причине, что в review-2 и review-3: `tools/` занят
R0-01.

## Что ушло в roadmap отдельными строками

`ROADMAP.md` шаг правок не трогает — строки адресованы приёмке.

**Приёмке, обязательная (из M-1).** В `CHANGELOG.md:48` стоит та же заниженная
формулировка, что чинилась в `capture.md`: «на дамп с CSS опираются две строки
прогона из тринадцати». Заменить на действительное: **CSS-дамп есть у 2 узлов
из 9; на него опираются 6 строк прогона из 14, на запись диалогового чтения —
8 строк и 7 узлов из 9.**

**Владельцу R0-03 / досъёмки — расширить уже выписанную строку с четырёх узлов
до семи.** Досъёмка с сохранением дампа `get_design_context` нужна по **семи**
узлам: `10929:505`, `9519:2095`, `7869:20760`, `1284:3362` (дампа нет вовсе)
плюс `12395:7441`, `6509:20474`, `14871:1319` (сохранена только структурная
выдача `get_metadata` без единого значения `border-radius`). Приоритет:
`9519:2095` — решает судьбу значения 44 и единственной в пакете реализации
И-3; затем `6509:20474` и `1284:3362` — на них стоит «опровергнуто снизу».
Отдельно `18250:651` (трансформа группы) — решает судьбу ступени 81.708.

**Владельцу R0-02 (шкала радиусов).** Шкала принимает не только значения,
но и критерий отнесения И-1…И-3 (`capture.md` §5). Значение 44 у `9519:2095`
до досъёмки помечать как классифицированное без файла: признак И-3 подтверждён
структурно и не подтверждён измерением. Открытая ступень — 81.708 на девяти
плитках `18085:3890`: интерфейсная, но трансформа группы `18250:651` не снята.
Число «пять узлов из девяти в диапазоне 2–4» переносить **вместе с оговоркой**:
при интерфейсном 44 их шесть.

**Владельцу R0-03, попутная.** Пометка «факты восходят к диалоговому чтению,
CSS-дампа нет» проставлена в этой итерации у трёх узлов из семи — тех,
на которых стоят выводы шага (`9519:2095`, `6509:20474`, `1284:3362`).
У оставшихся четырёх (`10929:505`, `12395:7441`, `7869:20760`, `14871:1319`)
её стоит проставить при досъёмке, когда решится, останется ли она нужна.

**Владельцу R0-01 (сторож)** и **владельцу R0-07 (перенос чисел)** — без
изменений: ревью 3 признало обе строки автора верными.
