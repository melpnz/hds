# Ревью R0-00 «Досъёмка Figma» — итерация 4 (подтверждение fix-3)

Вердикт: **принять**
Проверено: `fix-3.md`, `capture.md` §1 и §5, `evidence/coverage-notes.md` §3,
`evidence/source/figma/{9519-2095,6509-20474,1284-3362,10929-505}.md`,
`CHANGELOG.md`, гейт `validate-counts`. Команды — из `d:/work/guides/landings`.

## 1. M-1 (major) — пересчёт fix-3 проверен независимо, подтверждён

Fix-3 не просто занёс число из своей находки в файл, а **пересчитал таблицу
прогона заново** и обнаружил, что review-3 сама ошиблась в счёте строк
(приписала `18085:3890` четыре строки вместо пяти). Пересчитал таблицу сам,
без доверия к fix-3.md:

```
$ awk '/^\| узел \| значение \| признак \| вывод \|/{f=1;next}
       f&&/^\| `/{n++} f&&!/^\|/{print n; exit}' .pipeline/R0-00/capture.md
14
```

Построчный разбор таблицы `capture.md:278–291`: `18085:3890` действительно
даёт **пять** строк (`20·24·40·50`, `81.708`, `16.158·23.14`, `23.807`,
`11.993·44.973`), не четыре. С дампом (`18085:3890` + `1324:7793`) —
**6 строк из 14**; без дампа (семь узлов: `10929:505`, `9519:2095` ×2,
`12395:7441`, `7869:20760`, `6509:20474`, `14871:1319`, `1284:3362`) —
**8 из 14**. Это ровно то, что называет `capture.md:303–312`
и `coverage-notes.md:93–105`. **Число из review-3 (11 из 13) было неверным
и его собственной ошибкой, не занижением автора шага** — fix-3 честно это
и написал, а не спрятал за молчаливой правкой.

Счёт узлов (2 из 9 на дампе, 7 из 9 на диалоговом чтении) review-3 назвала
верно, и он не менялся.

**CHANGELOG.md обновлён** (было обязательным условием приёмки из review-3 §8):
формулировка «на дамп с CSS опираются две строки прогона из тринадцати»
заменена на «7 узлов из 9 и 8 строк прогона из 14». Сквозной
`grep -rn "5 дампов\|Две строки прогона\|только 0"` вне `.pipeline/` пуст —
старая формулировка нигде не осталась.

**Вывод: M-1 закрыта по существу**, а не переформулирована. Ни одно
измеренное значение не изменилось, изменился только размер объявленного
пробела — ровно то, что и требовалось.

## 2. Минорные и note-находки — проверены точечно

- **m-1** (`6509-20474.md:85`, `1284-3362.md:72`) — «Ступеней — 1 (только 0)»
  заменено на «Явных ступеней — 0» с определением ступени и оговоркой
  «про узел целиком не утверждается». Проверено в обоих файлах, включая их
  `## GAP`.
- **m-2** (`9519-2095.md`) — раздел «Радиусы» теперь называет И-3 по имени,
  разделяет «структура подтверждена / значение и гарнитура — нет» и метит
  44 как «классифицированное без файла»; `## GAP` получил строку
  о несохранённом дампе и статус первого приоритета досъёмки.
- **m-3** (`capture.md`, `9519-2095.md`) — оговорка «пять узлов из девяти,
  а при интерфейсном 44 — шесть» стоит рядом с самим числом в обоих файлах.
- **n-1** (`coverage-notes.md`, буллет «снизу») — хрупкость единицы
  у `9519:2095` и то, что обе опоры вывода «снизу» держатся на диалоговом
  чтении, — обе оговорки на месте.
- **n-2** (`10929-505.md:77`) — «пилюля» заменена на «круг» с арифметикой
  (46 на квадрате 70 × 70 обрезается до 35).

Все пять — исправлены по существу, не только переформулированы.

## 3. Гейт

```
$ node tools/validate-counts.mjs
Проверено утверждений: 170 в 17 файлах. Все сходятся.
EXIT=0
```

`validate-blocks`, `validate-classes`, `validate-normative` не прогонялись —
та же причина, что в review-2/3/fix-3: `tools/` занят параллельным R0-01,
и коды возврата этого шага не касаются.

## 4. Что проверено и претензий нет

- Пересчёт таблицы прогона — независимо, до строки; 14/14 сходится.
- Все четыре файла evidence из таблицы находок (`9519-2095`, `6509-20474`,
  `1284-3362`, `10929-505`) — правки на месте, а не только в `.pipeline/`.
- `CHANGELOG.md` — обязательная правка из review-3 §8 внесена.
- `capture.md` §1 — подпись `raw-*.txt` разделена на дампы CSS (2)
  и структурные дампы (3), как требовало review-3 §8.
- Роадмап-строки для владельцев R0-02, R0-03, R0-01, R0-07 из review-3 §8 —
  либо перенесены в `fix-3.md` §«Что ушло в roadmap отдельными строками»
  дословно, либо подтверждены без изменений.
- Всё, что review-1/2/3 признали чистым, не переоткрывалось.

## 5. Не проверено

- Досъёмка семи узлов без CSS-дампа — не входит в объём итерации 3
  (`fix-3.md` прямо пишет: «Figma не вызывалась»). Остаётся задачей R0-03,
  строка в `fix-3.md` для его владельца готова.
- Разделы B/C/D чеклиста — по-прежнему неприменимы: шаг ничего не верстает.

## 6. Итог

Находок нет. Шаг готов к `guide-accept`.
