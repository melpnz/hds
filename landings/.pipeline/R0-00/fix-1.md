# Правки по review-1 — R0-00 «Досъёмка Figma»

Итерация правок 1. Находок в отчёте: **critical 1 · major 3 · minor 8 · note 1**.
Закрыто исправлением — **12**, отклонено — **0**, перенесено строкой роадмапа — **1**
(находка 10, частично: сам сторож не в моей области правки).

Пересъёмка Figma на этой итерации велась только по `1324:7793`
(`get_design_context`, дамп сохранён в `.pipeline/R0-00/raw-1324.txt`)
и точечно по `8056:20974` — обе для находок 3, 8 и 9. Ничего сверх списка находок
не снималось и не переписывалось.

## Таблица решений

| # | Severity | Решение | Что сделано | Где |
|---|---|---|---|---|
| 1 | **critical** | исправлено | В трёх файлах область действия объявлена явно: строка «доля снятого» в шапке (`12395`: CSS 2 секции из 13 · `6509`: 3 из 12 · `1284`: 3 блока из 13) и раздел `## GAP` с перечислением неснятых секций. Универсальные формулировки переписаны как «в снятых поддеревьях»; превосходные степени («самый строгий узел», «единственный узел, где скруглений нет вовсе») сняты либо ограничены выборкой; универсальное отрицание «ни Inter, ни ALS Hauss в узле нет» заменено на «в трёх снятых блоках других гарнитур не встретилось» | `evidence/source/figma/12395-7441.md`, `6509-20474.md`, `1284-3362.md` (`grep -c "^## GAP"` даёт 1 в каждом из трёх) |
| 2 | **major** | исправлено | Правило подсчёта объявлено явно: «стопка из N членов даёт N−1 стык», членом считается видимый фрейм верхнего уровня во всю ширину, участвующий в замощении. Пересчитано по семи узлам: **83 стыка из 85, зазор 0 (97.6 %)**, два исключения названы поимённо. Прежнее «86 из 88» помечено как не дающееся ни одним правилом подсчёта — включая «20 стыков, 18 нулевых» у `14871:1319`. Число переписано и в §6, и в сводке §10, и в строке хендоффа §7 | `.pipeline/R0-00/capture.md` §6, §7, §10 |
| 3 | **major** | исправлено | Две величины разделены: **явные значения `border-radius`** и **элементы без радиуса** — отдельной колонкой, ступенью не называется. Узел `1324:7793` переснят целиком: 10 × 18, 36 × 1, 40 × 1, 56 × 5 → **4 явные ступени**, то есть **внутри** диапазона 2–4. Вывод переписан: корпусное «2–4 ступени, строго выдержаны» опровергнуто **снизу** и **по строгости**, **сверху — не опровергнуто**. Счёт с псевдоступенью оставлен рядом как альтернативная методика с оговоркой, что при нём сравнение идёт не с тем же самым | `capture.md` §5 (определение, таблица девяти узлов, раздел выводов) и §10; `1324-7793.md` разделы «Радиусы»; `coverage-notes.md` §3 (методика подсчёта — добавлена на этой итерации) |
| 4 | **major** | исправлено | Координатный довод («узлы не попадают на страницу `0:1` по координатам») снят как неверный, ошибочность разобрана явно. Заменён прямым перечислением детей канвы: `get_metadata` на `0:1` отдаёт **ровно семь** фреймов (`1:838`, `1:414`, `1:1694`, `1:1263`, `94:198`, `80:554`, `297:1126`), ни одного из 18 узлов пакета среди них нет | `coverage-notes.md` §4, §7; `capture.md` §4, §10 |
| 5 | minor | исправлено | Сводка «подтверждено CSS на восьми из девяти» заменена на **«CSS подтверждает тему у шести узлов из девяти; у трёх классификация выведена косвенно — из цвета текста, рендера или фона вложенных окон»**. В таблицу добавлена колонка «вид подтверждения» — построчно, чтобы сводная не расходилась со строками | `capture.md` §8 |
| 6 | minor | исправлено | Число вызовов `get_design_context` по `10929:505` сведено к **6** — по числу поддеревьев, перечисленных в evidence (`10943:2225`, `10943:2454`, `10943:2462`, `10943:4522`, `10948:665`, `10943:4273`) | `capture.md` §1, таблица |
| 7 | minor | исправлено | Дамп `raw-1324.txt` досохранён, ссылка «строка 537» по нему воспроизводится (`rounded-[36px]` на 537-й строке — проверено). Обещание «`raw-*.txt` — дампы, по которым велось чтение» ограничено фактом: **сохранено 5 из 9**, четыре узла (`10929:505`, `9519:2095`, `7869:20760`, `1284:3362`) читались в диалоге. Добавлено правило: ссылок на номера строк в файлах без сохранённого дампа быть не должно | `.pipeline/R0-00/raw-1324.txt` (новый); `capture.md` §1, список вспомогательных файлов |
| 8 | minor | исправлено | Градиенты `1324:7793` пересчитаны по переснятому узлу: **семь заливок, четыре различных рецепта**, каждый со строкой дампа. Недостающий рецепт `linear-gradient(180deg, #1BF2C8 0%, #5162FF 61.458%, #212930 100%)` внесён как **C**. Отдельно исправлено: `rounded-[56px]` стоит на **пяти** элементах (122, 267.907, 200.93 × 3), а не на одной полосе | `1324-7793.md`, раздел «Цвет» |
| 9 | minor | исправлено | Слово «однотипные» из аргумента про 52/54 снято: карточки **разнотипные и из разных секций** (`7885:21620` — форма в hero 396 × 321, `8056:20997` — карточка Q&A 372 × 340). Добавлено, что внутри одной секции радиус держится согласованно — соседка `8056:20974` тоже 54 (переснято). Аргумент про строгость оставлен в ослабленной форме, плюс второй, более твёрдый случай: 20 и 24 на соседних карточках одной секции у снятого целиком `18085:3890`. Ошибки адресации исправлены: поле 120 держит `8043:20626` (у `8043:20624` y = 192); поле 218 подтверждено на двух узлах из четырёх, у `8043:20624` оно 217, у `8044:20871` — 224 | `7869-20760.md` разделы «Радиусы», «Поля»; `capture.md` §5, §6 |
| 10 | minor | **перенесено** (строка роадмапа) | Правка `ROADMAP.md`, `BRIEF.md` и `inventory.json` шагу снятия запрещена, а `evidence/` вне области `validate-counts` — почини́ть сторож здесь нельзя. Оформлено двумя средствами: (а) во всех местах хендоффа стоит явный маркер **«ЧИСЛО ОЖИДАЕТ ПЕРЕПИСЫВАНИЯ В R0-07»** с указанием, что действующим до переноса считается число `ROADMAP.md` (`measuredCss: 9`, и сторож зелёный именно на нём); (б) строка для роадмапа — ниже, раздел «Что ушло в роадмап» | `capture.md` §7, §8; `coverage-notes.md` §8 |
| 11 | minor | исправлено | Оба кандидата отграничены от ближайших соседей реестра — таблицей и по снятому: `14871:2768` → `partners` (9/18), структурного отличия снятое не даёт, отличие только семантическое; `14871:1962` → `footer-community` (7/18), тот же паттерн в середине страницы и адресованный B2B. Добавлено покрытие **1/18** у обоих → `rule: false`, `wave: null`, как `testimonials`. Вердикт «тип или вариант соседа» явно оставлен за R0-05 — шаг даёт материал и границу | `capture.md` §3; строка находок §9 |
| 12 | minor | исправлено | Таблица §3 приведена к трём объявленным колонкам («узел \| секций верхнего уровня \| из них CSS снят \| что снято»), числа больше не текстом внутри ячейки. Расхождения с соседними документами сведены: `6509:20474` — **12 секций, из них 11 видимых** (стыки в `capture.md` §6 считаются по одиннадцати); `1284:3362` — **секций верхнего уровня нет**, 13 плавающих блоков, снято 3 | `coverage-notes.md` §3 |
| 13 | note | исправлено | В протокол добавлен раздел с прогонами и дословной выдачей вместе с кодами возврата. Заявления «по всем код 0» в нём **нет и быть не может** — это оговорено отдельно: код 0 отдают только `validate-counts` и `node --check`; код **2** у `validate-blocks`, `validate-classes` и `validate-normative` — документированное «проверять нечего» до R0-05, R0-06 и R0-02/R0-03, а не поломка | `capture.md` §8 (прогоны и коды возврата) |

## Что доделано именно на этой итерации правок

Предыдущий проход итерации 1 закрыл находки 1, 2, 4–9, 11–13 и большую часть 3,
но оборвался, не записав отчёт. Доделано здесь:

1. **Находка 3, последний хвост.** Методика подсчёта ступени была объявлена
   в `capture.md` §5 и в `1324-7793.md`, но не в `coverage-notes.md` §3 —
   документе, который и задаёт, как читается инвентаризация `P2-4`.
   Добавлен абзац: «Ступень = измеренное значение `border-radius`», отсутствие
   скругления идёт отдельной колонкой; альтернативный счёт с псевдоступенью
   сравнивается не с корпусным «2–4»; итог — опровергнуто снизу и по строгости,
   не сверху.
2. **Находка 7, арифметика списка дампов.** Строка в `capture.md` §1
   перечисляла пять позиций (включая нечитаемое `1324:7793`-metadata),
   а называла их «эти четыре узла». Список приведён к четырём реальным узлам
   без сохранённого дампа, добавлено сводное «сохранено 5 дампов из 9».
3. **Отчёт `fix-1.md`** — этот файл.

## Изменённые файлы

- `d:\work\guides\landings\.pipeline\R0-00\capture.md`
- `d:\work\guides\landings\evidence\coverage-notes.md`
- `d:\work\guides\landings\.pipeline\R0-00\fix-1.md` (новый)

Изменены на предыдущем проходе той же итерации:
`evidence/source/figma/12395-7441.md`, `6509-20474.md`, `1284-3362.md`,
`1324-7793.md`, `7869-20760.md`, `10929-505.md`, `9519-2095.md`,
`14871-1319.md`, `18085-3890.md`; `.pipeline/R0-00/raw-1324.txt` (новый).

`tools/`, `ui/`, `package.json`, `ROADMAP.md`, `BRIEF.md`, `README.md`,
`CHANGELOG.md`, `.pipeline/inventory.json` не трогались — там идёт R0-01
и запрет на правку документов шагом снятия.

## Результат валидатора

Прогон после правок, дословно:

```
$ node tools/validate-counts.mjs
Проверено утверждений: 144 в 5 файлах. Все сходятся.
  landingNodes: 18
  brandBoards: 4
  banners: 23
  measuredCss: 9
  blockTypes: 19
  primitives: 5
  registryTotal: 24
  patterns: 3
  productionLines: 3
  findings: 40
  openQuestions: 4
  coverageLimits: 12
  roadmapSteps: 46
  roadmapR0: 9
  renderPercent: 83
  renderCoveredPx: 12000
  renderNodeHeight: 14462
  themePairs: 0
  cover:hero: 18
  … (18 строк покрытия блоков)
EXIT=0

$ node --check .pipeline/R0-00/extract-style-facts.mjs
EXIT=0
```

Число утверждений выросло с 70 (на момент ревью) до 144 — сторож дополняется
параллельно идущим шагом R0-01; к результатам этого шага изменение отношения
не имеет. `measuredCss: 9` зелёный — это верное **действующее** число пакета
до переноса в R0-07 (см. находку 10).

`validate-blocks`, `validate-classes`, `validate-normative` на этой итерации
не прогонялись: `tools/` правится шагом R0-01 прямо сейчас, их коды возврата
меняются не по причинам этого шага. Зафиксированные в `capture.md` §8 коды **2**
у всех трёх — документированное «проверять нечего» до R0-05/R0-06/R0-02.

## Что ушло в роадмап отдельными строками

Правка `ROADMAP.md` мне запрещена — строки передаю текстом владельцам шагов.

**Владельцу `validate-counts` (R0-01):**

> `validate-counts` не видит `evidence/*.md`: `evidence/coverage-notes.md` §8
> утверждает «CSS снят у 18 из 18», `ROADMAP.md` и `.pipeline/inventory.json`
> держат `measuredCss: 9`, и сторож зелёный, потому что сверяет документы
> между собой, а `evidence/` вне области сканирования. Это дыра в покрытии,
> а не нормальное разделение ответственности. Либо расширить область
> на `evidence/*.md`, либо ввести маркер «ЧИСЛО ОЖИДАЕТ ПЕРЕПИСЫВАНИЯ В R0-07»,
> который сторож понимает и трактует как отложенное, а не как расхождение.
> Маркер уже проставлен в `capture.md` §7, §8 и `coverage-notes.md` §8 —
> сторожу остаётся его читать.

**Владельцу R0-07 (перенос чисел):** таблица «было → стало» лежит готовой
в `capture.md` §7 и `coverage-notes.md` §8 — семь строк по `ROADMAP.md`
и `BRIEF.md` §5. Перенос закрывает и противоречие, описанное строкой выше.

**Владельцу R0-05 (реестр блоков):** `14871:2768` и `14871:1962` — кандидаты
в типы, отграничения от `partners` и `footer-community` снятое не даёт,
покрытие 1/18 у обоих → `rule: false`, `wave: null`. Материал — `capture.md` §3.

**Владельцу R0-03 / досъёмки:** дампы по `10929:505`, `9519:2095`,
`7869:20760`, `1284:3362` не сохранены — повторный разбор этих узлов
`extract-style-facts.mjs` потребует нового вызова MCP.
