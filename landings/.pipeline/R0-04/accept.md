# Приёмка R0-04 — продуктовые палитры

Вердикт: **ПРИНЯТО**
Дата: 9 сентября 2026. Приёмщик: `guide-accept`.
Основание: `review-5.md` — вердикт «принять», находок 0 (пятая, финальная
итерация: полный прогон чеклиста и всех шести валидаторов по прямому
указанию вызывающего, а не только инкрементальная проверка diff fix-5).
Пять итераций review и пять итераций fix закрыли в сумме: blocker 04-6
(палитра Курсов была снята с баннера `5164:8025` без лок-апа продукта
вместо верифицированного `5164:8169` — маркеры `@snapshot` переписаны
честно, значения `--product-*` НЕ изменены), major 04-7 (досъёмка трёх
баннеров `01_Social` по Q-2, третий и самый объёмный конфликтующий
источник палитры Курсов), два minor и находку 04-13 (major,
самопротиворечие `BRIEF.md`: счётчик `openQuestions | Q-1…Q-5` утверждал
пять вопросов, а список вопросов показывал четыре — Q-5 не был виден во
всех headline-разделах пакета).

Все гейты прогнаны здесь, своим запуском — ни один не принят по логу
чужого отчёта. Расхождений с заявленным в `review-5.md`/`fix-5.md` не
найдено ни по одному числу.

Шаг R0-04 — не отдельный компонент со своей записью в `blocks/manifest.json`,
а слой продуктовых палитр внутри `ui/tokens.css`, тот же род шага, что
R0-02 (слой токенов, принят 9 сентября 2026, `.pipeline/R0-02/accept.md`).
Гейты адаптированы по тому же принципу: применимость определяется тем,
что шаг делает, а не тем, что уже случайно лежит в пакете.

---

## Гейты — прогнаны здесь, своим запуском

### 1. Валидатор — зелёный, все шесть

```
$ node tools/validate-blocks.mjs --strict
Записей в реестре 12, инвентаризация ожидает 24 (registryTotals.total) — реестр наполняется волнами
Записей 12: статьи на месте, cssRoots сошлись, якоря сошлись.
EXIT=0

$ node tools/validate-counts.mjs
Проверено утверждений: 170 в 17 файлах. Все сходятся.
EXIT=0

$ node tools/validate-classes.mjs
Проверено классов: 201 в 4 файле(ах) против 11 таблиц стилей.
METHOD §6.2: витрин 3 с префиксом doc-, файлов ui/ без doc- — 8.
EXIT=0

$ node tools/validate-normative.mjs
Проверено: объявлений 775 в 8 файле(ах) CSS, статей 17.
Нормативных областей 3: светлая тема · ось игривости · перенос брендового стиля на веб-формат.
EXIT=0

$ node tools/validate-radius.mjs
Правило Ф-3 (Р-8): проверено пар 8, нарушений 0.
Лестница Р-7: 0 · 6 · 16 · пилюля, ступени читаются из ui/tokens.css.
EXIT=0

$ node tools/validate-tokens.mjs
Проверено ссылок на токены: 560 в 11 файлах CSS и 20 статьях.
Сверено значений в таблицах «Токены»: 69. Объявлено имён: 121.
EXIT=0

$ for f in tools/*.mjs; do node --check "$f" || echo "FAIL $f"; done
(без вывода — все разбираются)
```

Числа побайтово идентичны заявленным в `fix-5.md` и `review-5.md` — не
переставлены местами, не приблизительны, и совпадают с прогонами
`review-2…review-4`: регрессии по числам нет ни разу за пять итераций.

### 2. Браузерные проверки — зелёный, проверено независимой сессией CDP

`npm test` не запускается: скрипта `test` в `package.json` нет (заводит
R0-06, ещё `in-progress`) — формальный гейт неприменим по той же причине,
что у R0-00…R0-03.

Содержательно живая витрина проверена собственным запуском, отдельным от
`review-5.md`: headless Chrome (`--remote-debugging-port=9455`, свежий
профиль) поднят поверх уже работающего `node tools/serve.mjs`
(`http://127.0.0.1:4180/showcase/blocks.html`), голый CDP через встроенные
`fetch`/`WebSocket` Node 24, без сторонних зависимостей. Найден переключатель
`button[data-set="data-product"][data-value="courses"]`, клик выполнен
программно (`el.click()`), `getComputedStyle(document.documentElement)`
прочитан до и после:

```
до клика (habr/dark):     --product-base #070405  --product-accent #06d0ef  --product-acid none
после клика "Курсы" (dark):
  --product-base #003580  --product-base-deep #003580
  --product-accent #0099cc  --product-acid #44f91f
  --product-cta #3670f4    --product-on-acid #ffffff
после клика "светлая" (courses/light):
  --product-base #eef4fb  --product-base-deep #003580
  --product-accent #2456c9  --product-acid #2fbf0f
  --product-cta #3670f4    --product-on-acid #ffffff
```

Оба набора совпадают побайтово со значениями, которые несёт
`ui/tokens.css` (`[data-product="courses"]` и
`[data-theme="light"][data-product="courses"]`), и побайтово с тем, что
`review-5.md` получило собственным независимым CDP-прогоном той же
кнопки. Две независимые сессии в разных процессах браузера дают
идентичный результат — переключатель меняет `data-product` на `<html>`
и все шесть токенов пересчитываются живым CSS, а не JS-подменой.

### 3. Ревью закрыто — зелёный

`review-5.md`: вердикт «принять», blocker 0 · major 0 · minor 0 · note 0,
после полного (не только инкрементального) прогона чеклиста.

Цепочка закрытия проверена не по прозе `fix-N.md`, а собственными
командами:

- **04-6 (blocker, review-2) и 04-7 (major, review-2/fix-3).** Прочитан
  `ui/tokens.css:384-395` целиком: маркеры `@snapshot` на шести токенах
  Курсов честно называют узел `5164:8025` без лок-апа продукта и ссылаются
  на верифицированный `5164:8169` как на конфликтующий источник; значения
  `--product-*` Курсов не изменены (то же, что до находки) — выбор
  источника вынесен в `Q-5`. Файлы `evidence/source/figma/5164-8025.md`,
  `5164-8169.md`, `01-social-courses.md` на месте.
- **04-13 (major, review-4/fix-5).** Прямым чтением, не текстом `fix-5.md`:
  `grep -n "Q-5" README.md ROADMAP.md BRIEF.md GUIDE.md .pipeline/inventory.json`
  (см. гейт 8 ниже) находит Q-5 ровно во всех пяти headline-местах,
  включая `BRIEF.md:540-548` («Что решениями не закрыто») и
  `GUIDE.md:1386,1406-1414` (§15) — оба добавлены именно fix-5.
  Счётчик `BRIEF.md:579` (`openQuestions | Q-1…Q-5`) больше не
  противоречит списку из пяти пунктов.
- Более ранние находки review-2/review-3 (переклассификация корпусных
  цветов Р-4, снятие запрета на градиенты Ц-5, разделение вторичного
  текста Ц-2) перепроверены выборочно чтением `GUIDE.md:499-531` и
  `ui/tokens.css:253-254,339` — сошлись с текущим состоянием файлов.
- Единственная оставшаяся минорная находка **04-12** (падеж «5 вопросов»
  в `README.md:172`) сознательно не закрыта из-за жёсткого регэкспа
  `validate-counts.mjs` — перенесена строкой `X-18` в `ROADMAP.md`,
  статус `planned`, названа поимённо, не скрыта.

### 4. Реестр сходится — не применим

R0-04 — слой продуктовых палитр внутри `ui/tokens.css`, записей
`blocks/manifest.json` не заводит и не меняет: 12 записей реестра и 7
узлов «проверьте руками» (гейт 1) идентичны тем, что были на входе шага.
Реестр наполняет отдельный шаг R0-05 (`in-progress`).

### 5. Evidence на месте — зелёный

Собственная сверка (не пересказ review-N), `@snapshot`-маркеры продуктовых
токенов Курсов против evidence:

```
ui/tokens.css:384  @snapshot 04_Brand 5164:8025 — верх фонового градиента
  Курсов, с явной оговоркой «узел не несёт лок-апа продукта вовсе»
evidence/source/figma/5164-8025.md — тот же узел, та же оговорка про
  отсутствие лок-апа, ссылка на конфликтующий 5164:8169
→ совпадает по содержанию, атрибуция честная, а не подразумеваемая

evidence/source/figma/01-social-courses.md — качественное описание трёх
  страниц 01_Social (14677:12013, 13228:414, 12926:192), лок-ап «Хабр
  Курсы» на каждом баннере, синий→голубой→сине-фиолетовый градиент
ROADMAP.md:432 (Q-5, кандидат 3) — то же описание, та же оценка объёма
  («десятки» против «по 1 баннеру» у 04_Brand)
→ совпадает по составу и объёму выборки
```

Скриншоты `.pipeline/R0-04/figma-recheck/*.png` (`5164-8054.png`,
`social-12926-192.png`, `social-13228-414.png`, `social-14677-12013.png`)
— рабочие артефакты верификации fix-2/fix-3/review-3, а не случайный
мусор: referenced by name в `fix-2.md:78,194`, `fix-3.md:47`,
`review-3.md:26-28,69,232` и в самих evidence-файлах
(`5164-8054.md:11`, `5164-8169.md:68`).

Открыто честно, не блокирует: точные hex/rgba значений `01_Social` и
точная граница узла `5164:8025`/`5164:8036` не извлечены — это и есть
содержание вопроса Q-5, оставленного открытым до решения владельца
(не предмет review-цикла или fix-5).

### 6. Копируемость — зелёный

Грепом по `ui/*.css` на `https?://` — совпадения только в
`ui/state-contract.css` (data-URI SVG, инлайновый) и в комментариях-
источниках `ui/fonts.css`; внешних URL в рабочих путях нет. Продуктовые
палитры используют исключительно `var(--…)`/литералы с node id под
`@snapshot`, ни один внешний ассет не подключается.

### 7. Соответствие exit criteria волны R0 — применимая часть зелёная

Из семи условий волны R0 этот шаг предметно закрывает часть пункта 2
(«каждое значение в `ui/tokens.css` помечено: снято или спроектировано»)
для продуктового слоя: `validate-normative` зелёный (гейт 1), продуктовые
палитры Курсов промаркированы честно даже там, где атрибуция под
вопросом (Q-5) — норматив не выдан за факт. Пункты 1, 4, 5, 6 — вне
этого шага (R0-01, R0-06, R0-07, R0-05). Волна R0 этим шагом не
закрывается: из 9 шагов приняты 4 (R0-00, R0-01, R0-02, теперь R0-04),
R0-03/05/06/08 стоят `in-progress`/`review`, R0-07 — `planned`.

Предметные утверждения самого шага проверены прямым чтением:

- **Переклассификация корпусных цветов (решение Р-4).** `GUIDE.md:499-506`:
  `#548eab`/`#8164f7`/`#ff7e47` названы цветами продуктового UI, а не
  языком лендингов, с основанием («`#548eab` не встретился ни в одном из
  18 макетов»).
- **Снятие запрета на градиенты (Ц-5).** `GUIDE.md:516-526`: опровержение
  по всем трём пунктам (фон, кнопка, текст) с CSS-цитатами; `ui/tokens.css:398-399`
  несёт то же признание в коде.
- **«Два акцента максимум» — проверено и опровергнуто (Ц-4).** `GUIDE.md:513`:
  «три и более цветовых роли на секцию — норма», покрытие
  `16418`/`20572`/`10929`.
- **Вторичный текст: непрозрачный серый vs альфа (Ц-2).** `GUIDE.md:856-857`,
  `ui/tokens.css:253-254,339`: `text-secondary` — отдельный hex не альфа
  у лендингов, `text-muted` — альфа у бренда; различие названо явно, не
  подразумевается.

### 8. Честность статуса — зелёный

```
$ grep -n "Q-5" README.md ROADMAP.md BRIEF.md GUIDE.md .pipeline/inventory.json
README.md:211, ROADMAP.md:42/394/422/429/432/460, BRIEF.md:540/579,
GUIDE.md:735/1386, inventory.json:260 — Q-5 присутствует консистентно
и с одинаковой оценкой источников/объёма во всех пяти headline-местах.
```

Открытый вопрос Q-5 не спрятан — назван поимённо в `ROADMAP.md`, в
`BRIEF.md` §«Что решениями не закрыто», в `GUIDE.md` §15, в
`inventory.json.openQuestions` и в `README.md`. Значения `--product-*`
Курсов честно не изменены до ответа владельца — норматив не выдан за
факт. Минорная находка 04-12 (падеж) названа строкой `X-18`, не скрыта.
Шаг не объявляет «complete» сверх подтверждённого.

### 9. Две проекции (machine layer) — не применим

Машинный слой — волна R5, ещё не существует.

### 10. Правила доказаны — не применим напрямую этим шагом

Правила Ц-2…Ц-5 введены в `GUIDE.md` с покрытием дробью (гейт 7), но
формальный реестр правил `L-*`/`C-*` с coverage — предмет `R5-01`
(`docs/guide/composition.md`, уже частично сделан вне очереди). Этот шаг
не выводит Decision Guide.

### 11. Страница из принятого — не применим

R0-04 не собирает страницу из компонентов — предмет волны R4.

---

## Что внесено в документы

### `ROADMAP.md`

1. Строка `R0-04`: статус `review` → `**done**`, с датой, ссылкой на
   `accept.md`, кратким сводом пяти итераций review/fix и явным упоминанием
   нового открытого вопроса Q-5 для владельца.
2. «Где пакет сейчас»: `Статусы шагов` — `done` 3 → 4, `review` 17 → 16.
   `Принято шагов` — добавлена запись о R0-04.

Числа пересчитаны после правки и остались зелёными: `validate-counts` —
170 утверждений, 17 файлов, EXIT=0.

### `CHANGELOG.md`

Одна запись в `## Unreleased` (перед записью о `R0-02`): что шаг принят,
чем отличаются пять итераций review/fix, что закрыто (blocker 04-6, major
04-7 и 04-13, два minor), явное упоминание нового открытого вопроса Q-5
для владельца, что осталось открытым и не блокирует.

### Чего приёмка не трогала

- `ui/tokens.css`, `showcase/blocks.html`, `GUIDE.md`, `BRIEF.md`,
  `.pipeline/inventory.json`, `evidence/source/figma/*` — результат
  build → fix-2…5, уже готов и проверен; приёмка только фиксирует, что
  он верен, и не правит.
- `.pipeline/R0-04/*` кроме этого файла — протокол review/fix чужой, не
  переписывается задним числом (тот же принцип, что в `.pipeline/R0-02/accept.md`).
- `career/`, `courses/`, `habr/`, `partner-specials/`, `_sources/`,
  корпус `0*.md`, `.audits/`.

---

## Волна R0 — не закрыта

Принято 4 шага из 9 (R0-00, R0-01, R0-02, R0-04). Из семи exit criteria
волны выполнены: пункт 1 (R0-01), пункты 2 и 3 (R0-02, часть пункта 2 —
R0-04), пункт 7 (R0-00). Не хватает: витрины и её проверок (R0-06),
реестра (R0-05), границ покрытия (R0-07), словаря состояний (R0-08 —
стоит `review`), типографической шкалы как принятого шага (R0-03 —
`in-progress`), пунктов 4, 5, 6 exit criteria. Релиз не предлагается:
пакетом пока нельзя пользоваться полноценно.
