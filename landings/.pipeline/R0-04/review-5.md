# Ревью R0-04 «Продуктовые палитры» — итерация 5 (финальная проверка перед guide-accept)

Batch: **R0-04**. Режим: **инкрементальный** для diff `fix-5` (единственная
назначенная находка — 04-13, major, из `review-4.md`), расширенный до
**полного прогона чеклиста и всех шести валидаторов** по прямому указанию
вызывающего (пятая, финальная итерация перед приёмкой).

Вердикт: **принять**
Находки: blocker 0 · major 0 · minor 0 · note 0

## Область правки fix-5, доказанная не текстом отчёта

`git diff --stat` против HEAD после fix-5 показывает изменения в семи файлах,
но это накопленный diff всего цикла R0-04 (build → fix-2 → fix-3 → fix-4 →
fix-5), а не diff одного fix-5. Локальность fix-5 проверена не по описанию
`fix-5.md`, а по времени модификации файлов:

```
15:19:56  review-4.md   (записана находка 04-13)
15:21:10  BRIEF.md      (изменён)
15:21:23  GUIDE.md      (изменён)
15:22:18  fix-5.md      (записан отчёт)
```

`.pipeline/inventory.json` (14:57), `README.md` (15:13), `ROADMAP.md` (15:13),
`ui/tokens.css` (14:41), `showcase/blocks.html` (14:48) — все с mtime раньше
`review-4.md`, то есть не менялись в этом цикле вовсе. Заявление fix-5.md
«CSS, токены, showcase, inventory.json этим циклом не тронуты» подтверждено
таймстампами файловой системы, а не поверено на слово. Значит fix-5 —
точечная правка ровно двух файлов, `BRIEF.md` и `GUIDE.md`, как и заявлено.

## Проверка закрытия находки 04-13 (единственная назначенная fix-5)

Проверял текущее состояние файлов напрямую, не отчёт `fix-5.md`.

```
grep -n "Q-5" README.md ROADMAP.md BRIEF.md GUIDE.md .pipeline/inventory.json
```

Q-5 присутствует как отдельный вопрос ровно во всех пяти местах пакета, где
перечисляются открытые вопросы владельцу:

- `README.md:211`, раздел «Открытые вопросы владельцу» — не изменился с
  review-4, был чист и раньше.
- `ROADMAP.md:432` (полная запись реестра `Q-5`) и `ROADMAP.md:42`
  (executive summary) — не изменились с review-4, были чисты и раньше.
- `.pipeline/inventory.json:260`, `openQuestions[Q-5]` — не изменился с
  review-4, был чист и раньше.
- `BRIEF.md:540-548`, раздел **«Что решениями не закрыто»** — **новый пункт**
  `Q-5` появился здесь именно в fix-5 (`git diff` подтверждает вставку между
  Q-2 и абзацем «Q-1, Q-3, Q-4 закрыты»). Строка-счётчик `openQuestions |
  Q-1…Q-5` (`BRIEF.md:579`) больше не противоречит списку вопросов — раздел,
  который review-4 уличило в самопротиворечии (таблица утверждала «5»,
  список показывал «4»), теперь показывает пять пунктов ровно по счётчику.
- `GUIDE.md:1384-1414`, раздел **«15. Что решить владельцу»** — заголовок
  раздела поправлен: «Открыт частично — Q-2» → «Открыт частично — Q-2;
  открыт — Q-5» (`GUIDE.md:1386`), добавлен нумерованный пункт **5**
  (`GUIDE.md:1406-1414`) с полным текстом вопроса.

Формулировка Q-5 в `BRIEF.md`/`GUIDE.md` (детальная: холодный сине-бирюзовый
+ неон `#44F91F` без лок-апа у `5164:8025`, тёплый розово-оранжевый с
подтверждённым лок-апом у `5164:8169`, десятки баннеров с лок-апом у
`01_Social`) и сжатая версия в `README.md:211` совпадают по составу трёх
источников и по оценке объёма («по 1 баннеру каждый» / «десятки… самый
объёмный»); `ROADMAP.md:432` и `inventory.json` несут ту же оценку с ещё
большей детализацией (координаты доски, ширина словесного знака 321.284px).
Расхождений по фактам между пятью формулировками нет — README и BRIEF/GUIDE
намеренно разного уровня детализации (headline-сводка против развёрнутого
раздела), но не разного содержания.

Диагноз review-4 (04-13) закрыт фактически, тем же методом, каким review-4
проверило 04-11: чтением текущего состояния файлов и `git diff`, не текстом
`fix-5.md`.

## Полный прогон чеклиста (запрошен вызывающим для финальной проверки)

Раздел A (соответствие источнику), раздел B (инварианты пакета, включая
B.6/E.19 — предмет 04-13) и раздел E (документация) уже дали единственную
находку 04-13, теперь закрытую. Дополнительных расхождений при повторном
прогоне не найдено:

- Других мест, перечисляющих открытые вопросы владельцу, в пакете нет —
  `grep -rn` по `Q-1.*Q-4|Открытые вопросы|Что решить владельцу|Что решениями
  не закрыто"` по всем `.md` вне `.pipeline/` даёт ровно те пять мест, что
  проверены выше, других headline-разделов с перечнем вопросов не существует.
- `README.md:172` («5 вопрос**а** владельцу») по-прежнему не поправлен на
  «вопросов» — это находка 04-12 (minor, review-3), сознательно оставленная
  fix-4/fix-5 из-за жёсткого регэкспа `validate-counts.mjs` (`/\d+
  вопроса владельцу/`, без формы «вопросов»); перенесена в `ROADMAP.md`
  строкой `X-18`, статус `planned`. Не переоткрываю: `fix-4.md`/`review-4.md`
  уже разобрали этот компромисс, ничего не изменилось.
- Разделы C (состояния) и D (responsive) чеклиста по-прежнему неприменимы —
  R0-04 не трогает разметку компонентов, только значения токенов и
  документацию; то же заключение, что в review-2/review-3.
- Раздел F/G/H чеклиста вне объёма шага (нет страниц/принципов/машинного
  слоя в этом batch).

Находок сверх 04-13 (уже закрытой) нет.

## Полный прогон валидаторов (свежий, независимый от логов fix-5)

Запустил все шесть валидаторов заново напрямую (не переиспользовал
`fix-5.validate.log`), лог сохранён: `.pipeline/R0-04/review-5.validate.log`.

```
validate-normative:  объявлений 775 в 8 файле(ах) CSS, статей 17. exit:0
validate-classes:    классов 201 в 4 файле(ах) против 11 таблиц. exit:0
validate-counts:     утверждений 170 в 17 файлах, все сходятся. exit:0
validate-radius:     пар 8, нарушений 0 (Ф-3/Р-8); лестница 0·6·16·пилюля. exit:0
validate-tokens:     ссылок 560 в 11 CSS + 20 статьях; таблицы 69; имён 121. exit:0
validate-blocks --strict: 12/24 записей, 7 пунктов "руками" (те же узлы,
                     что в fix-3/fix-4/fix-5 логах — не новые). exit:0
```

Все числа побайтово совпадают с `fix-5.validate.log` и с `review-4.validate.log`
— regression нет ни в одном валидаторе. Ожидаемо: fix-5 не менял CSS/токены/
manifest-подобные счётчики, только текст двух markdown-файлов.

## Живая витрина — реальный клик по переключателю продукта «Курсы», не чтение разметки

Запустил headless Chrome (`--remote-debugging-port=9333`) и повёл его по
голому CDP (`fetch`/`WebSocket`, встроенные в Node 24 — `puppeteer-core`/`ws`
в проекте не установлены, как и отметило review-4). Открыл уже поднятый
`node tools/serve.mjs` (`http://127.0.0.1:4180/showcase/blocks.html`,
`ui/tokens.css` не менялся этим циклом). Нашёл переключатель продукта
(`button[data-set="data-product"][data-value="courses"]`), кликнул по нему
программно (`el.click()`), прочитал `getComputedStyle(document.documentElement)`
до и после клика:

```
до клика (default habr/dark):
  --product-base: #070405  --product-accent: #06d0ef  --product-acid: none
после клика "Курсы" (dark):
  --product-base: #003580  --product-base-deep: #003580
  --product-accent: #0099cc  --product-acid: #44f91f
  --product-cta: #3670f4    --product-on-acid: #ffffff
после клика "светлая" (courses, light):
  --product-base: #eef4fb  --product-base-deep: #003580
  --product-accent: #2456c9  --product-acid: #2fbf0f
  --product-cta: #3670f4    --product-on-acid: #ffffff
```

Оба набора совпадают побайтово со значениями `[data-product="courses"]`
(`ui/tokens.css:385-395`) и `[data-theme="light"][data-product="courses"]`
(`ui/tokens.css:480-485`). Переключатель — не декорация: клик по кнопке
реально меняет `data-product` на `<html>` и все шесть токенов пересчитываются
живым CSS, а не JS-подменой значений. Это более сильная проверка, чем в
review-4 (которое не смогло поднять CDP и полагалось на доказательство
отсутствия diff) — здесь получен фактический живой рендер именно в этой
итерации, для продукта «Курсы» — то, что запросил вызывающий.

Комбинации Хабр/Карьера в обеих темах не переоткрывал: review-2 и review-3
уже подтвердили их живым `getComputedStyle` побайтово, `ui/tokens.css` не
менялся ни разу с review-3 (mtime 14:41, раньше review-4 и fix-5) — не
затронуто изменениями.

## Что проверено и претензий нет

- Q-5 присутствует и фактически одинаков по содержанию (три источника, та же
  оценка объёма) во всех пяти headline-местах пакета: `README.md`,
  `ROADMAP.md` (дважды — сводка и полная запись), `inventory.json`, `BRIEF.md`
  §«Что решениями не закрыто», `GUIDE.md` §15 — проверено прямым чтением, не
  текстом `fix-5.md`.
- `BRIEF.md` больше не противоречит сам себе: счётчик `openQuestions |
  Q-1…Q-5` (строка 579) и список вопросов (пять пунктов) теперь сходятся.
- Локальность fix-5 (только `BRIEF.md`, `GUIDE.md`) доказана таймстампами
  файловой системы, а не заявлением отчёта.
- Шесть валидаторов зелёные при независимом свежем прогоне, числа
  побайтово идентичны четырём предыдущим прогонам (review-2…review-4, fix-5).
- Живой рендер: реальный клик по переключателю продукта «Курсы» в headless
  Chrome через голый CDP подтверждает шесть токенов `--product-*` в обеих
  темах побайтово против `ui/tokens.css`.
- Находка 04-12 (minor) остаётся честно перенесённой строкой `X-18`
  (`ROADMAP.md`, `planned`) — не в объёме fix-5, не переоткрываю.
- Находки 04-6 (blocker) и 04-7 (major, review-2) остаются закрытыми —
  fix-5 не касался `ui/tokens.css` и не мог их сломать.
- Другого места, перечисляющего открытые вопросы владельцу, кроме
  проверенных пяти, в пакете нет (`grep -rn` по ключевым заголовкам разделов).
- `capture.md` по-прежнему отсутствует — пятая итерация подряд с тем же
  принятым обоснованием (`fix-2.md`), не блокирует.

## Не проверено

- Точные hex/rgba `01_Social` и точная граница узла `5164:8025`/`5164:8036`
  (`get_metadata` parent/child) — честный GAP, зафиксирован в
  `evidence/source/figma/01-social-courses.md` и `5164-8025.md`, не предмет
  ни этого review-цикла, ни fix-5 (это и есть содержание вопроса Q-5,
  который сам шаг оставляет открытым до решения владельца).
- Комбинации Хабр/Карьера × тема живым CDP в этой итерации не переоткрывал —
  `ui/tokens.css` не менялся с review-3 (mtime раньше review-4), а review-2/
  review-3 уже подтвердили их побайтово тем же методом.

## Итог

Единственная назначенная находка (04-13, major) закрыта фактически в обоих
указанных review-4 местах — `BRIEF.md` §«Что решениями не закрыто» и
`GUIDE.md` §15 — с той же оценкой источников и объёма, что в
`README.md`/`ROADMAP.md`/`inventory.json`; самопротиворечие `BRIEF.md`
(счётчик против списка) снято. Полный повторный прогон чеклиста, всех шести
валидаторов (независимо, не по логу) и живой витрины (реальный клик по
переключателю «Курсы» через CDP) новых расхождений не нашёл. Пятая итерация
review не находит находок по существу — честный вердикт: **принять**, шаг
готов к `guide-accept`.
