# Правки по review-4 — R0-04 «Продуктовые палитры»

| # находки | ID | Severity | Решение | Что сделано | Где |
|---|---|---|---|---|---|
| 1 | 04-13 | major | исправлено | В «Что решениями не закрыто» добавлен пункт **Q-5** (три конфликтующих источника палитры Курсов: `5164:8025` без лок-апа, `5164:8169` с лок-апом, `01_Social` — десятки баннеров, самый объёмный); формулировка совпадает с `ROADMAP.md:432`/`inventory.json` (`Q-5.note`) по составу источников и оценке объёма. Строка `openQuestions | Q-1…Q-5` (была строкой 570, после правки — 579) уже утверждала существование Q-5 — теперь раздел вопросов её подтверждает, самопротиворечие `BRIEF.md` снято | `BRIEF.md:540-548` |
| 1 | 04-13 | major | исправлено | В §15 «Что решить владельцу» добавлен пункт **5** с той же формулировкой (три источника, оценка объёма), заголовок раздела поправлен: «Открыт частично — Q-2» → «Открыт частично — Q-2; открыт — Q-5» | `GUIDE.md:1386`, `GUIDE.md:1406-1414` |
| — | 04-12 | minor | не в объёме этой правки | Подтверждено находкой ревью: 04-12 — отдельная, уже честно перенесённая находка (`ROADMAP.md` строка `X-18`, статус `planned`), не назначена этим циклом. Не тронута | — |

Находка 04-13 закрыта в обоих указанных местах одной и той же формулировкой,
взятой дословно по составу источников из `ROADMAP.md:432` и
`inventory.json` (`openQuestions[Q-5].note`/`question`) — конфликт источников
не пересказан своими словами, а сведён к тем же трём узлам и той же оценке
объёма («по 1 баннеру каждый» / «десятки… самый объёмный»).

Попутно перепроверена сама точка входа: `grep -n "Q-5" BRIEF.md GUIDE.md`
до правки давал только строку-счётчик `openQuestions | Q-1…Q-5` в `BRIEF.md`
и одну попутную фразу в §7 `GUIDE.md` (строка 735) — после правки Q-5 виден
как отдельный пункт в обоих headline-разделах, на которые указывает находка.

## Изменённые файлы

- `landings/BRIEF.md` — добавлен пункт `Q-5` в разделе «Что решениями не
  закрыто» (после `Q-2`, перед абзацем про закрытые Q-1/Q-3/Q-4).
- `landings/GUIDE.md` — добавлен пункт 5 в §15 «Что решить владельцу»,
  поправлен заголовок раздела.

CSS, токены, showcase, `.pipeline/inventory.json` этим циклом не тронуты —
находка 04-13 чисто документационная (то же самое отметило review-4 про
характер находки).

## Результат валидатора

Полный прогон всех шести валидаторов, лог `.pipeline/R0-04/fix-5.validate.log`:

```
validate-normative:  объявлений 775 в 8 файле(ах) CSS, статей 17. exit:0
validate-classes:    классов 201 в 4 файле(ах) против 11 таблиц. exit:0
validate-counts:     утверждений 170 в 17 файлах, все сходятся. exit:0
validate-radius:     пар 8, нарушений 0 (Ф-3/Р-8); лестница 0·6·16·пилюля. exit:0
validate-tokens:     ссылок 560 в 11 CSS + 20 статьях; таблицы 69; имён 121. exit:0
validate-blocks --strict: 12/24 записей, 7 пунктов "руками" (те же, что
                     в review-4/fix-4 логах — не новые). exit:0
```

Все числа побайтово совпадают с `review-4.validate.log` — правка не задела
ни CSS, ни токены, ни `manifest`-подобные счётчики; regression нет ни
в одном валидаторе.

## Что ушло в roadmap отдельными строками

Ничего нового. Находка 04-12 (minor) остаётся строкой `X-18` в `ROADMAP.md`,
заведённой ещё до этого цикла (review-3/fix-4) — этот fix её не трогал
и не добавлял к ней ничего.

## Статус пакета

`BRIEF.md`, `GUIDE.md`, `README.md`, `ROADMAP.md`, `inventory.json` теперь
согласованно показывают пять открытых вопросов и одинаково формулируют Q-5
во всех местах, где пакет обещает владельцу список вопросов (`core-contract.md`,
инвариант «проекции сходятся»). Готово к повторному ревью.
