# Ревью R0-02 «Слой токенов» — итерация 4 (финальная)

Batch: **R0-02** (`ui/tokens.css`, три слоя × две темы). Вход: `fix-3.md`,
закрывающий `review-3.md` (R3-1 major, R3-2 minor, R3-3/R2-6 note). Режим:
**инкрементальный** для трёх заявленных пунктов закрытия + **дополнительный
полный прогон чеклиста и всех шести валидаторов** по прямому указанию задачи
(раз находок по diff не осталось).

Вердикт: **принять**
Находки: blocker 0 · major 0 · minor 0 · note 0

---

## Проверено (не по прозе fix-3.md, самостоятельно)

### 1. R3-1 (major) — плитки `--core-glow-inset`/`--core-glow-hero` и продуктовые плитки

- Прочитан `showcase/blocks.html:510-523` построчно: массив `SHADOWS` теперь
  содержит только `--core-shadow-hard` и `--core-glass-glow`; строк с
  `--core-glow-inset`/`--core-glow-hero` нет, вместо них — комментарий,
  объясняющий убор и ссылающийся на R3-1. Грепом по всему `showcase/blocks.html`
  подтверждено: `--core-glow-inset`/`--core-glow-hero` встречаются только внутри
  этого комментария и в объяснениях `PRODUCT_ORIGIN`/`why`-текстах (описания
  происхождения, не действующие CSS-обращения).
- Грепом по `ui/*.css` подтверждено: `--core-glow-inset:`/`--core-glow-hero:`
  как деклараций больше нигде нет (0 совпадений).
- Живая витрина поднята заново (`node tools/serve.mjs`, порт 4180) и открыта
  headless Microsoft Edge через собственный CDP-клиент (WebSocket,
  `Runtime.evaluate`/`Page.captureScreenshot`/`Emulation.setDeviceMetricsOverride`;
  Playwright по-прежнему не установлен, та же причина, что в review-3/fix-3).
  Переключение темы/продукта — реальными кликами по кнопкам `[data-set]`
  (тот же код, что видит пользователь), не прямой записью атрибута.
  По всем **6 комбинациям** тема × продукт (habr/career/courses × dark/light):
  - `getComputedStyle(documentElement)` для `--core-glow-inset` и
    `--core-glow-hero` — **пустая строка во всех шести** (токены действительно
    не объявлены нигде в каскаде);
  - `--product-glow-inset` и `--product-glow-hero` — **резолвятся непустым
    значением во всех шести** и **варьируются по продукту**: habr `#06d0ef`
    (тёмная) / `#0b7f96` (светлая), career `#7963e9` / `#7a3df5`, courses
    `#0099cc` / `#2456c9` — цвет продукта совпадает с `--product-accent` того
    же продукта/темы; альфа `-inset` — 25 % тёмная / 12 % светлая во всех трёх
    продуктах, как и заявлено;
  - `#doc-shadows .doc-fx-cell` — ровно **2 плитки** во всех шести комбинациях
    (было 4, из них 2 пустых, на скриншоте `review-3-shadows-broken.png`);
    пустых боксов и «—» не воспроизводится ни в одной комбинации.
- Дополнительно проверена копируемость (инвариант пакета 1) на разметке из
  `blocks/card.md:159-175` (использует `.card-glow`, тот же класс, что связан
  с находкой): изолированная HTML-страница с одним `<link
  href="ui/landings.css">`, продукт career, тёмная тема — `.card-glow` даёт
  `box-shadow: color(srgb 0.474 0.388 0.914 / 0.25) 4px 4px 14.4px 10px inset`,
  что численно совпадает с `--product-glow-inset` career/dark (`#7963e9` при
  25 % альфе); карточки и шрифт рендерятся, скриншот —
  `.pipeline/R0-02/review-4-copyability-card-glow.png`. Тестовый HTML-файл не
  оставлен в дереве (использован и удалён), в отчёте фиксирую метод и результат.

Находок нет — обе части R3-1 закрыты и подтверждены числами, не описанием.

### 2. R3-2 (minor) — обоснование `--core-text-muted`

Прочитан `ui/tokens.css:252` целиком. Ссылки на «GUIDE §8» как на прецедент
больше нет; вместо неё — прямое признание, что подходящего прецедента в пакете
нет, решение названо нормативом шага (R0-02, fix-3), а не следованием ранее
встречавшемуся приёму. Числа находки (HSL ≈ 200°, S ≈ 41 %) сохранены.
Грепом по `ui/tokens.css` на «GUIDE §8»/«bg-surface холоднее» — совпадений вне
этого честного признания-отказа от прецедента нет. Комментарий длинный, но не
делает новых непроверенных утверждений.

### 3. R3-3 (note) — методика подсчёта строк `ui/foundations.css`

Независимо запущено: `wc -l ui/foundations.css` → **227**; `node -e
"fs.readFileSync(...).split('\n').length"` → **228**; файл действительно
заканчивается переводом строки (`ends with newline: true`). `ui/landings.css:24`
теперь называет оба числа с методикой именно в этом порядке и с этим
объяснением — совпадает с фактическим поведением инструментов, не выдумано.

---

## Дополнительно: полный прогон чеклиста по R0-02 (не только diff)

Находок по diff не осталось, поэтому по прямому указанию задания прогнан
чеклист целиком (насколько применимо к шагу «слой токенов», не к отдельному
компоненту).

**A. Соответствие источнику.** Точечно перепроверены несколько `@snapshot`
в `ui/tokens.css` и `ui/blocks/primitives.css` — комментарии перед
декларациями называют узел и число, расхождений не найдено (не переснимал
Figma/прод — работал с уже лежащими значениями и числами review-1…3, новых
противоречий не всплыло).

**B. Инварианты пакета.**
- **Копируемость (инвариант 1)** — подтверждена отдельным тестом выше
  (`.card-glow` из `blocks/card.md` на голом `ui/landings.css`).
- **Изоляция витрины (инвариант 2)** — `validate-classes.mjs`: «витрин 3
  с префиксом doc-, файлов ui/ без doc- — 8», 0 протечек, код 0.
- **Локальные ассеты (инвариант 3)** — грепом по `ui/*.css` на `https?://`:
  единственные совпадения — `ui/fonts.css` (комментарии-источники, сами
  `src: url(...)` ведут в локальные `assets/fonts/...`) и data-URI SVG
  в `state-contract.css` (инлайновый, не внешний). Прод-URL в рабочих путях нет.
- **Токены (инвариант 4)** — литералы в `ui/blocks/primitives.css` (`#fe6a00`,
  `#222222`, `#d3ff5c` и т. д.) все идут с явной пометкой `@snapshot`/«литерал:
  значения узла» — соответствует явно разрешённому в инварианте 4 случаю, не
  новая находка (это же фиксировали review-1…3 применительно к этим строкам).
- **Реестр сходится (инвариант 5)** — `validate-blocks.mjs --strict`, код 0,
  12 записей, cssRoots и якоря витрины сошлись; 7 узлов «проверьте руками» —
  те же самые, что на review-2/review-3, не новые.

**D. Responsive.** Живая витрина (`showcase/blocks.html`) проверена CDP на
всех **четырёх канонических ширинах**: 320 → `scrollWidth 320 = innerWidth
320`; 768 → `753/768`; 1024 → `1009/1024`; 1400 → `1385/1400` — во всех
случаях `scrollWidth ≤ innerWidth` (разница на не-320 ширинах — ширина
скроллбара браузера, не переполнение контента), горизонтального overflow нет
ни на одной из четырёх.

**C/F/G/H** (состояния компонента, страницы, принципы, машинный слой) —
вне объёма шага «слой токенов»: сам шаг не заявляет `requiredStates`,
не собирает страницу и не публикует правила/машинный слой; ранее это верно
не проверялось как отдельный пункт и в этой итерации тоже не относится к делу.

**E. Документация.** `BRIEF.md §7` (Р-4/Р-5) — открытый конфликт по-прежнему
называн прямо, не спрятан. Пять мест про `ui/foundations.css` — согласованы
между собой и с фактическим состоянием файла (см. R3-3 выше).

---

## Валидаторы — прогнаны заново, полный лог

```
node tools/validate-normative.mjs         EXIT=0  (775 объявлений, 8 файлов, 17 статей)
node tools/validate-tokens.mjs            EXIT=0  (560 ссылок, 11 CSS + 20 статей; 69 значений в таблицах, 121 имя)
node tools/validate-radius.mjs            EXIT=0  (8 пар, Ф-3 — 0 нарушений)
node tools/validate-classes.mjs           EXIT=0  (201 класс, 0 протечек оболочки)
node tools/validate-counts.mjs            EXIT=0  (170 утверждений, все сходятся)
node tools/validate-blocks.mjs --strict   EXIT=0  (12 записей; 7 узлов «проверьте руками» — те же, что на review-2/3)
```

Числа идентичны заявленным в `fix-3.md` и предыдущим логам. Полный вывод —
`.pipeline/R0-02/review-4.validate.log`.

---

## Что проверено и претензий нет

- Обе половины R3-1 (плитки ядра убраны из «Тени — снято», продуктовые плитки
  резолвятся и варьируются) — подтверждено `getComputedStyle` по 6
  комбинациям тема×продукт, независимо от прозы `fix-3.md`.
- Копируемость `.card-glow` на голом `ui/landings.css` — числовое совпадение
  с ожидаемым продуктовым значением.
- R3-2 — ссылка на неверный прецедент GUIDE §8 убрана, числа находки сохранены.
- R3-3 — методика подсчёта строк (`wc -l` = 227 vs `split('\n').length` = 228)
  совпадает с фактическим поведением инструментов и с текстом `ui/landings.css:24`.
- Инварианты 1–5 пакета — копируемость, изоляция витрины, локальные ассеты,
  явная пометка литералов, сходимость реестра — все подтверждены заново.
- Responsive 320/768/1024/1400 на живой витрине — без overflow.
- Все шесть валидаторов — код 0, числа не разъехались с предыдущими логами.
- R2-2, R2-3, R2-5 (закрыты на review-3) — не переоткрывались, изменений
  в затронутых местах со времени review-3 нет.

## Не проверено

- **`ui/foundations.css` по существу** (шкала, фокус, роли заголовков) —
  вне объёма R0-02, относится к R0-03; проверен только факт «не пуст»
  и число строк (см. выше).
- **Открытый вопрос Р-4/Р-5** (владелец или конвейер) — как и в review-3,
  это зафиксированный `GAP`, а не пробел проверки; решение не в компетенции
  ревью.
- **`capture.md` для R0-02 по-прежнему отсутствует** (R2-6) — `fix-3.md`
  прямо отказывается реконструировать его задним числом, ссылаясь на
  `core-contract.md` («пустой раздел лучше правдоподобной догадки»); это
  зафиксировано как честно открытая note, не blocker и не major — не влияет
  на вердикт этой итерации.
- **13 из 18 узлов `02_Landings`** — не переснимал; пересъёмка запрещена
  методом при отсутствии битых/недостающих файлов, так же как на итерациях 1–3.

---

## Итоговая оценка готовности к guide-accept

Шаг R0-02 «Слой токенов» **годится для guide-accept**:

- все находки review-1…3 закрыты (R3-1, R3-2, R3-3) и в этой итерации
  перепроверены самостоятельно — числами и живой витриной, а не по описанию;
- единственный оставшийся открытый пункт — note-уровня (`capture.md`
  отсутствует, R2-6) и один документированный GAP (Р-4/Р-5) — оба явно
  названы в пакете, ни один не маскируется под решённый вопрос;
- полный повторный прогон чеклиста (не только diff) не вскрыл новых находок;
- все шесть валидаторов проходят с кодом 0 и числами, идентичными заявленным.

Рекомендация: принять шаг. Оставшийся note про `capture.md` и открытый GAP
Р-4/Р-5 стоит перенести в `ROADMAP.md`/протокол приёмки как уже
задокументированные ограничения, а не как новые находки этой итерации.
