# Приёмка R0-02 — слой токенов

Вердикт: **ПРИНЯТО**
Дата: 9 сентября 2026. Приёмщик: `guide-accept`.
Основание: `review-4.md` — вердикт «принять», находок 0. Четыре итерации
review (`review-1…4`) и три итерации правок (`fix-1…3`) закрыли в сумме
10 (review-1, коммит `059af70`, вне протокола) + 6 (review-2) + 3 (review-3)
находки; часть закрытий review-2/3 переоткрывалась следующей итерацией
(R2-1 → R3-1) и была закрыта окончательно только в `fix-3.md`, что
`review-4.md` подтвердил независимым пересчётом, а не переписыванием
прозы `fix-3.md` на слово.

Все гейты прогнаны здесь, своим запуском — ни один не принят по логу
чужого отчёта. Расхождений с заявленным в `review-4.md`/`fix-3.md` не
найдено ни по одному числу.

Шаг R0-02 — не отдельный компонент со своей записью в `blocks/manifest.json`,
а слой токенов CSS (`ui/tokens.css`). Гейты 4, 9, 10, 11 адаптированы под
это по тому же принципу, что в `.pipeline/R0-00/accept-2.md` и
`.pipeline/R0-01/accept.md`: применимость гейта определяется тем, что шаг
делает, а не тем, что уже случайно лежит в пакете. В отличие от R0-00 (шаг
разметки не производит) и отчасти R0-01 (разметки в пакете ещё не было),
R0-02 производит настоящий продуктовый CSS, которым уже пользуется
разметка блоков (`.card-glow` и другие классы `ui/blocks/primitives.css`) —
поэтому гейт 6 (копируемость) здесь **применим и проверен**, а не пропущен.

---

## Гейты — прогнаны здесь, своим запуском

### 1. Валидатор — зелёный, все шесть

```
$ node tools/validate-blocks.mjs --strict
Записей в реестре 12, инвентаризация ожидает 24 (registryTotals.total) — реестр наполняется волнами
Записей 12: статьи на месте, cssRoots сошлись, якоря сошлись.
EXIT=0

$ node tools/validate-counts.mjs
Проверено утверждений: 170 в 17 файлах. Все сходятся.
EXIT=0

$ node tools/validate-classes.mjs
Проверено классов: 201 в 4 файле(ах) против 11 таблиц стилей.
METHOD §6.2: витрин 3 с префиксом doc-, файлов ui/ без doc- — 8.
EXIT=0

$ node tools/validate-normative.mjs
Проверено: объявлений 775 в 8 файле(ах) CSS, статей 17.
Нормативных областей 3: светлая тема · ось игривости · перенос брендового стиля на веб-формат.
EXIT=0

$ node tools/validate-radius.mjs
Правило Ф-3 (Р-8): проверено пар 8, нарушений 0.
Лестница Р-7: 0 · 6 · 16 · пилюля, ступени читаются из ui/tokens.css.
EXIT=0

$ node tools/validate-tokens.mjs
Проверено ссылок на токены: 560 в 11 файлах CSS и 20 статьях.
Сверено значений в таблицах «Токены»: 69. Объявлено имён: 121.
EXIT=0

$ for f in tools/*.mjs; do node --check "$f" || echo "FAIL $f"; done
(без вывода — все разбираются)
```

Числа идентичны заявленным в `fix-3.md` и `review-4.md` — не переставлены
местами, не приблизительны.

Независимый пересчёт деклараций `ui/tokens.css` собственным скриптом
(strip комментариев, регэксп начала CSS-декларации), не запуском чужого
инструмента:

```
declarations: 175
unique names: 96
@snapshot: 70
@normative: 42
```

Совпадает с `ROADMAP.md` и `review-4.md` до символа.

### 2. Браузерные проверки — не применим формально, проверено содержательно

`npm test` не запускается: скрипта `test` в `package.json` нет (заводит
R0-06, ещё `in-progress`). Формальный гейт 2 не применим по той же причине,
что у R0-00 и R0-01.

Содержательно живой браузер проверен своим запуском, не по логу
`review-4.md`: изолированная статическая раздача копии `ui/` + headless
Microsoft Edge через собственный CDP-клиент (`Page.navigate`,
`Runtime.evaluate`) на отдельной странице с одной разметкой
`<article class="card card-r20 card-glow">` и одним `<link
href="./ui/landings.css">`, `data-product="career"
data-theme="dark"` на `<body>`. Результат:

```json
{"boxShadow":"color(srgb 0.47451 0.388235 0.913725 / 0.25) 4px 4px 14.4px 10px inset",
 "fontFamily":"Inter, \"Helvetica Neue\", Arial, sans-serif","exists":true}
```

`color(srgb 0.4745 0.3882 0.9137)` — точно `#7963e9`, продуктовый акцент
Карьеры в тёмной теме (`0x79/255=0.4745`, `0x63/255=0.3882`,
`0xe9/255=0.9137`), альфа 0.25 — как и заявлено для `--product-glow-inset`
в тёмной теме. Подтверждает и результат `review-4.md` (тот же класс,
продукт career/dark), и то, что подключение работает не только внутри
витрины, но и на голой странице.

### 3. Ревью закрыто — зелёный

`review-4.md`: вердикт «принять», blocker 0 · major 0 · minor 0 · note 0.

Цепочка закрытия проверена не по прозе `fix-N.md`, а собственными
командами:

- **R3-1 (major, витрина не убрала `--core-glow-inset`/`--core-glow-hero`
  из «Тени — снято»).** Грепом по `showcase/blocks.html`: обе строки
  встречаются только в поясняющем комментарии (`:510,515`), не в массиве
  `SHADOWS`. Грепом по `ui/*.css`: деклараций `--core-glow-inset:` /
  `--core-glow-hero:` — 0 совпадений.
- **R3-2 (minor, обоснование `--core-text-muted` ссылалось на GUIDE §8 не
  того рода).** Прочитан `ui/tokens.css:252` целиком: ссылка на GUIDE §8
  как на действующий прецедент убрана, вместо неё — честное признание
  отсутствия прецедента; числа находки (HSL ≈ 200°, S ≈ 41 %) сохранены.
- **R3-3 (note, методика счёта строк `ui/foundations.css`).** Независимо:
  `wc -l ui/foundations.css` → 227; `node -e
  "...split('\n').length"` → 228. `ui/landings.css:24` называет оба числа
  с методикой в этом порядке — совпадает.
- Более ранние находки (R2-2 — реестр решений Р-5…Р-8, R2-3 — пять мест,
  ложно называвших `ui/foundations.css` пустым, R2-5 — счётчик деклараций)
  перепроверены выборочно и сошлись с текущим состоянием файлов.

### 4. Реестр сходится — не применим

R0-02 — слой токенов, записей `blocks/manifest.json` не заводит и не
меняет: 12 записей в реестре и 7 узлов «проверьте руками» — те же, что
были до этого шага (сверено с логом review-2/review-3, числа не
сдвинулись). Реестр наполняет отдельный шаг R0-05 (`in-progress`). Тот же
принцип неприменимости, что у R0-00 и R0-01: применимость гейта — по
работе шага, а не по факту существования файла реестра.

### 5. Evidence на месте — зелёный

Собственная сверка (не пересказ review-2/3), два `@snapshot` независимо
проверены против `evidence/source/figma/`:

```
ui/tokens.css:89   @snapshot 02_Landings 10929:505 — сетка 1600: поле 218, контейнер 1164
evidence/source/figma/10929-505.md:110  «Горизонтальное поле — 218 px от края артборда 1600 (контейнер 1164)»
→ совпадает до символа

ui/tokens.css:94   @snapshot 02_Landings 18085:3890 — сетка 1440: поле 208, контейнер 1024
evidence/source/figma/18085-3890.md:173-174  «Горизонтальные поля: … 208 у контентных секций (контейнер 1024 при артборде 1440)»
→ совпадает до символа
```

Открыто честно, не блокирует: `.pipeline/R0-02/capture.md` отсутствует
(находка R2-6, подтверждена нерешённой на review-3 и review-4). `fix-3.md`
прямо отказывается реконструировать протокол задним числом, ссылаясь на
`core-contract.md` («пустой раздел лучше правдоподобной догадки») —
причина принята: часть решающей работы (закрытие R2-1/R3-1) прошла вне
протокола capture, это уже названо в других местах пакета (находка R2-3),
выдумывать содержимое `capture.md` сейчас значило бы нарушить И-1.

### 6. Копируемость — зелёный, применим (в отличие от R0-00)

Проверено своим запуском, не по описанию review-4 — см. гейт 2 выше:
изолированная страница вне пакета, один `<link>` на `ui/landings.css`,
класс `.card-glow` резолвится в точное продуктовое значение. `ui/fonts.css`
и локальные ассеты — точечно проверены грепом на `https?://` в `ui/*.css`:
совпадения только в комментариях-источниках (`ui/fonts.css`) и в
data-URI SVG (`state-contract.css`, инлайновый), внешних URL в рабочих
путях нет.

### 7. Соответствие exit criteria волны R0 — применимая часть зелёная

Из семи условий волны R0 этот шаг напрямую отвечает за пункты 2 и 3:

- **Пункт 2** («каждое значение в `ui/tokens.css` помечено: снято или
  спроектировано; светлая тема целиком в норме «спроектировано»,
  `validate-normative` подтверждает кодом 0») — выполнен: `validate-normative`
  зелёный (см. гейт 1), 70 `@snapshot` + 42 `@normative` покрывают все 175
  деклараций.
- **Пункт 3** («имена токенов в светлой и тёмной темах совпадают один
  в один») — выполнен для цветоносных ролей: независимая проверка (не по
  прозе review-2) — программный разбор блоков `[data-theme="light"]`
  против остального файла: 0 имён встречаются только в светлой теме без
  тёмного аналога. Имена, встречающиеся только в тёмном контексте без
  светлой пары (73 из 96), — структурные (радиусы, отступы, сетка), не
  цветоносные: по конструкции темонезависимы, объявлены раз в `:root`
  и не обязаны иметь пару, что соответствует смыслу пункта 3 (пары
  требуются для того, что меняется по теме).

Пункты 1, 4, 5, 6, 7 — вне этого шага (подключение — R0-01, витрина —
R0-06, `coverage.md` — R0-07, `validate-blocks`/`validate-counts` на пустом
реестре — R0-01/R0-05, досъёмка — R0-00). Волна R0 этим шагом не
закрывается целиком.

### 8. Честность статуса — зелёный

Число деклараций (175/96/70/42) пересчитано независимо и совпадает с тем,
что теперь стоит в `ROADMAP.md` (гейт 1). Ни один из трёх открытых пунктов
(`capture.md` отсутствует, GAP Р-4/Р-5 нерешён, пробел покрытия
`validate-tokens.mjs` на JS-обращениях витрины) не скрыт — все названы
поимённо в `review-4.md`, `fix-3.md` и теперь в обновлённой строке
`ROADMAP.md` и записи `CHANGELOG.md`. Шаг не объявляет `complete` сверх
подтверждённого.

### 9. Две проекции (machine layer) — не применим

Машинный слой — волна R5, ещё не существует. Не сломан — его нет.

### 10. Правила доказаны — не применим

R0-02 не выводит принцип или Decision Guide с coverage/победителем по
частоте — это предмет волны R5 (`R5-01`, `R5-02`, уже частично сделаны вне
очереди, но не по материалу этого шага).

### 11. Страница из принятого — не применим

R0-02 не собирает страницу из компонентов — это предмет волны R4.

---

## Что внесено в документы

### `ROADMAP.md`

1. Строка `R0-02`: статус `review` → `**done**`, с указанием даты,
   `accept.md`, кратким сводом четырёх итераций review/fix и честным
   списком трёх открытых нерешающих пунктов.
2. «Где пакет сейчас»: `Статусы шагов` — `done` 2 → 3, `review` 18 → 17,
   с оговоркой, что R0-02 — исключение из общего правила «результат лежит,
   отчёта ревью нет»: у него отчёт есть, конвейер пройден целиком.
   `Принято шагов` — 2 → 3, добавлена запись о R0-02.

Числа пересчитаны заново после правки и остались зелёными:
`validate-counts` — 170 утверждений, 17 файлов, EXIT=0.

### `CHANGELOG.md`

Одна запись в `## Unreleased` (перед записью о `R0-00`): что шаг принят,
чем он отличается от R0-00/R0-01 (полный конвейер, а не приёмка
внеочередного результата), что закрыли четыре итерации review/fix, что
осталось открытым и не блокирует.

### Чего приёмка не трогала

- `ui/tokens.css`, `showcase/blocks.html`, `ui/blocks/primitives.css`,
  `blocks/card.md`, `tools/validate-tokens.mjs`, `BRIEF.md`, `GUIDE.md`,
  `README.md`, `ui/landings.css` — результат `fix-1…3`, уже готов и
  проверен; приёмка только фиксирует, что он верен, и не правит.
- `.pipeline/R0-02/*` кроме этого файла — протокол review/fix чужой,
  переписывать задним числом не стал (то же правило, что в
  `accept-2.md` R0-00).
- `ui/foundations.css` по существу (шкала, фокус, роли заголовков) — это
  предмет R0-03, отдельно не принят этой приёмкой; факт «не пуст» и
  корректность пяти мест, ранее лгавших о его состоянии, — уже закрыты
  находкой R2-3 внутри review/fix R0-02, это не новое решение приёмки.
- `career/`, `courses/`, `habr/`, `partner-specials/`, `_sources/`,
  корпус `0*.md`, `.audits/`.

---

## Волна R0 — не закрыта

Принято 3 шага из 9 (R0-00, R0-01, R0-02). Из семи exit criteria волны
выполнены: пункт 1 (R0-01), пункты 2 и 3 (R0-02, этот шаг), пункт 7
(R0-00). Не хватает: витрины и её проверок (R0-06), реестра (R0-05),
границ покрытия (R0-07), словаря состояний (R0-08 — стоит `review`, не
`done`), пунктов 4, 5, 6 exit criteria. Релиз не предлагается: пакетом
пока нельзя пользоваться полноценно — типографической шкалы, палитр
продукта и реестра блоков как принятых шагов ещё нет, хотя их результат
уже частично лежит в пакете со статусом `review`.
