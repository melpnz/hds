# Приёмка R0-08 — словарь состояний и нормативный контракт

Вердикт: **ПРИНЯТО**
Дата: 9 сентября 2026. Приёмщик: `guide-accept`.
Основание: `review-4.md` — вердикт «принять с замечаниями», blocker 0 ·
major 0 · minor 0 · note 2. По чеклисту `note` — наблюдение на будущее,
на применимость элемента не влияет и вердикт не роняет.

Четыре итерации review и три итерации fix закрыли **2 blocker, 5 major,
8 minor**. Главный blocker (review-3) — `var(--product-accent)` без
запасного значения: на странице без `data-product` подстановка пуста,
объявление становится недействительным на этапе вычисления, и кольцо
фокуса вместе с заливкой контролов пропадало целиком. Это прямой отказ
инварианта копируемости METHOD §6.1, а не косметика.

Все гейты прогнаны здесь, своим запуском. Ни один не принят по логу
чужого отчёта — включая тот, что был центральным предметом ревью.

Шаг R0-08 — не компонент со своей записью в реестре, а словарь состояний
и нормативный CSS-контракт: тот же род шага, что R0-02 (слой токенов) и
R0-04 (продуктовые палитры). Гейты адаптированы по тому же принципу:
применимость определяется тем, что шаг делает. **Отличие от обоих
предшественников:** этот шаг менял `blocks/manifest.json`, поэтому гейт 4
«реестр сходится» здесь применим, а у них был неприменим.

---

## Гейты — прогнаны здесь, своим запуском

### 1. Валидатор — зелёный, все шесть

```
$ node tools/validate-blocks.mjs --strict
Записей в реестре 12, инвентаризация ожидает 24 (registryTotals.total) — реестр наполняется волнами
Записей 12: статьи на месте, cssRoots сошлись, якоря сошлись.
EXIT=0

$ node tools/validate-counts.mjs
Проверено утверждений: 170 в 17 файлах. Все сходятся.
EXIT=0

$ node tools/validate-classes.mjs
Проверено классов: 201 в 4 файле(ах) против 11 таблиц стилей.
METHOD §6.2: витрин 3 с префиксом doc-, файлов ui/ без doc- — 8.
EXIT=0

$ node tools/validate-normative.mjs
Проверено: объявлений 777 в 8 файле(ах) CSS, статей 17.
Нормативных областей 3: светлая тема · ось игривости · перенос брендового стиля на веб-формат.
EXIT=0

$ node tools/validate-radius.mjs
Правило Ф-3 (Р-8): проверено пар 8, нарушений 0.
Лестница Р-7: 0 · 6 · 16 · пилюля, ступени читаются из ui/tokens.css.
EXIT=0

$ node tools/validate-tokens.mjs
Проверено ссылок на токены: 572 в 11 файлах CSS и 20 статьях.
Сверено значений в таблицах «Токены»: 69. Объявлено имён: 123.
EXIT=0

$ for f in tools/*.mjs; do node --check "$f" || echo "FAIL $f"; done
(без вывода — все разбираются)
```

Логи: `.pipeline/R0-08/accept.validate-{blocks,counts,classes,normative,radius,tokens}.log`.

Числа сошлись с заявленными в `review-4.md` побайтово, включая дельту
против `review-3` (нормативных объявлений 775 → 777, ссылок на токены
565 → 572). Регрессии по числам нет.

Прогон повторён **после** правок самой приёмки (`ROADMAP.md`, `README.md`,
`CHANGELOG.md`) — все шесть остались `EXIT=0`.

### 1a. Общий `validate-step.mjs` — неприменим по виду шага, назван честно

```
$ node ../.claude/guide/validate-step.mjs --product . --id R0-08 --kind principle \
    --pipeline .pipeline/R0-08 --json-out .pipeline/R0-08/accept-auto.json \
    --md-out .pipeline/R0-08/accept-auto.md
FAIL: 1/5 gates; failed=R0-08:human-doc, R0-08:showcase, R0-08:machine-doc, R0-08:validator
```

Этот `FAIL` **не прячется и не выдаётся за успех**, но и не является
отказом гейта: у скрипта четыре вида шага (`component`, `page`,
`principle`, `machine`), и R0-08 не относится ни к одному. Каждая из
четырёх упавших проверок ищет артефакт, которого шаг не создаёт по
замыслу:

| Гейт скрипта | Что ищет | Почему неприменим |
|---|---|---|
| `human-doc` | запись `R0-08` в `docs/guide/composition.md` / `decisions.md` | R0-08 не выводит принцип; его human-документ — `components/STATES.md` |
| `showcase` | якорь `data-rule=R0-08` | шаг не вводит правила с `data-rule`; живой пример состояний — `showcase/primitives.html` |
| `machine-doc` | запись в `machine/rules.json` | машинного слоя в пакете нет вовсе — волна R5, `planned` |
| `validator` | `tools/validate-components.mjs` | у лендингов валидатор реестра называется `tools/validate-blocks.mjs` — прогнан в гейте 1 |

Единственный применимый гейт скрипта — `review` — **PASS**
(`review-4.md: accepted`). Роль «применимого валидатора» для этого пакета
исполняет чеклист приёмки самого `ROADMAP.md` (шесть инструментов
`tools/`), прогнанный выше целиком. Тот же порядок применён на приёмках
R0-02 и R0-04.

### 2. Браузерные тесты пакета — формально неприменимы, содержательно проверено

`npm test` не запускается: скрипта `test` в `package.json` нет
(`scripts` = validate, validate:counts, validate:classes,
validate:normative, validate:tokens, serve, capture). Заводит его R0-06,
шаг `in-progress` — та же причина, что на приёмках R0-00…R0-04.

Содержательная часть проверена собственным прогоном — гейт 6 ниже.

### 3. Ревью закрыто — зелёный

`review-4.md`: вердикт «принять с замечаниями», blocker 0 · major 0 ·
minor 0 · note 2. Обе `note` перенесены строками роадмапа (гейт 8).

Цепочка закрытия проверена не по прозе `fix-N.md`, а собственными
командами:

- **blocker review-3 (`var(--product-*)` без fallback).** Строгий
  `grep -nE 'var\(--product-[a-z0-9-]+\)'` (без запятой, то есть без
  запасного значения) по `ui/**/*.css`: в изменённых файлах два
  попадания, **оба в комментариях** (`ui/state-contract.css:58`,
  `ui/blocks/primitives.css:53`). Остальные пять — в `ui/tokens.css`,
  внутри самого слоя продукта и слоя концепции; `ui/tokens.css` этим
  шагом не изменён (`git diff --stat` его не показывает), это территория
  R0-02/R0-04. Полный перечень ссылок в трёх изменённых файлах — 12
  строк, из них 3 комментария и **9 объявлений, у всех девяти запасное
  значение**:

  ```
  ui/state-contract.css:84   --state-focus-ring: var(--product-accent, var(--core-text-primary));
  ui/state-contract.css:91   --state-accent:     var(--product-cta, #5a6c7c);
  ui/state-contract.css:92   --state-on-accent:  var(--product-on-acid, #ffffff);
  ui/state-contract.css:194  border-color:       var(--product-cta, var(--core-text-secondary));
  ui/blocks/primitives.css:68 background:        var(--product-cta, var(--core-text-primary));
  ui/blocks/primitives.css:69 color:             var(--product-on-acid, var(--core-bg-page));
  ui/blocks/primitives.css:474 box-shadow:       var(--product-glow-inset, none);
  ui/blocks/core.css:251     box-shadow:         var(--product-glow-hero, none);
  ui/blocks/core.css:506     color:              var(--product-accent, var(--core-text-primary));
  ```

  Счёт сходится с заявленным в `review-4.md` (12 / 3 / 9). Живьём
  подтверждено гейтом 6.
- **blocker review-1 (ложное отрицание носителя `expanded`).**
  `components/STATES.md:285-294` и `ui/state-contract.css:300-306`
  прочитаны целиком: носитель назван (`button.site-header-toggle
  [aria-expanded="false"]`, `blocks/site-header.md`), отсутствующим
  назван не носитель, а правило CSS на раскрытый вид меню, и GAP
  закреплён за R2-05 через `blocks/manifest.json`. Проверено
  собственным перечислением селекторов состояний в
  `ui/state-contract.css`: `aria-expanded` встречается **только в
  комментариях** — правила под него действительно нет, утверждение
  честное.
- **major review-3 (две роли `.product-lockup`).** Матрица
  `components/STATES.md:52` («Ссылка» — `.product-lockup` на `<a href>`,
  обязательны `default hover focus-visible`) против `:57` («Знак и
  лента» — `.product-lockup` на `<span role="img">`, обязателен только
  `default`). Разметка проверена своим `grep`:
  `showcase/core.html:70,84` — `<a class="product-lockup" href="#site-header">`,
  `blocks/site-header.md:144` — `<a class="product-lockup" href="/">`.
  `href` у логотипа на месте, не убран ради закрытия находки.
- **Сходимость словаря до 20** пересчитана мной по обоим файлам:
  8 реализовано + 1 покой + 2 без правила + 1 необязательное + 7 без
  носителя + 1 без повода = **20**, группы не пересекаются,
  нереализованных ровно двенадцать.
- **«Восемь реализованных» проверено не на слово.** Перечисление
  селекторов состояний в `ui/state-contract.css` даёт ровно восемь
  различных имён словаря: `:hover` (5), `:focus-visible` (4),
  `:active` → `pressed` (2), `:checked` (2), `:indeterminate` (1),
  `:disabled` (6), `:read-only` (1), `[aria-invalid="true"]` (1).
  Правила `default` нет ни одного — как и заявлено.

### 4. Реестр сходится — применим и зелёный

Шаг менял `blocks/manifest.json`, поэтому гейт применим (в отличие от
R0-02 и R0-04). Проверено собственным разбором JSON, а не чтением
диффа:

```
записей 12   статусы {"partial":3,"complete":9}

product-lockup | partial  | required default,hover,focus-visible        | missing ["hover"]
button         | complete | required default,hover,focus-visible,pressed,disabled
glass-chip     | complete | required default
form-field     | complete | required default,hover,focus-visible,disabled,invalid,checked,indeterminate
card           | complete | required default
marquee        | complete | required default
footer-corporate | complete | required default
footer-social  | partial  | required default,hover,focus-visible        | missing ["hover"]
hero           | complete | required default
site-header    | partial  | required default,hover,focus-visible,expanded | missing ["expanded"]
form-section   | complete | required default
program        | complete | required default
```

- `specPath`, `cssRoots` и якоря всех 12 записей существуют —
  `validate-blocks --strict` EXIT=0 (гейт 1).
- Переход `product-lockup` `complete` → `partial` **подтверждён живым
  замером, а не принят на веру**: `CSS.forcePseudoState hover` на
  `a.product-lockup` не меняет ни `color`, ни `opacity`, ни
  `textDecorationLine`, ни `backgroundColor`, ни `filter` — до и после
  строка идентична. Пробел настоящий и записан.
- Сводка `complete` 9 · `partial` 3 стоит одинаково в `manifest.json`,
  `blocks/INDEX.md`, `README.md:88` и `ROADMAP.md:30`.
- Семь узлов «проверьте руками» в выдаче `validate-blocks` —
  те же, что были на входе шага; новых не добавилось.

### 5. Evidence на месте — зелёный

Единственное значение шага, взятое не из головы, — литерал `#5a6c7c`
(запасная нейтраль переключателя). Проверено своей сверкой:

```
ui/tokens.css:240                      --core-border-field тёмной темы = #5a6c7c
evidence/source/figma/1324-7793.md:57  «граница чекбокса (выкл.) 2px solid #5a6c7c»,
                                       узел I1324:8114;…6948
→ литерал не новый цвет и не цвет продукта: от всех трёх --product-cta
  (#4da3cb Хабр, #7963e9/#7a3df5 Карьера, #3670f4 Курсы) отличается
```

Выбор литерала вместо `var(--core-border-field)` обоснован в
`@normative`-комментарии `ui/state-contract.css:89` и обоснование
проверяемое: ядровая роль меняет светлоту с темой, а знак поверх белый
решением Р-6 — на светлом `#acb8bf` белая галочка пропала бы.

Остальные значения контракта — норматив целиком, и это верно по существу:
в 18 макетах не встретилось ни одного состояния (BRIEF §5 п. 8).
`validate-normative` EXIT=0 подтверждает, что норматив нигде не выдан
за снимок.

### 6. Копируемость — зелёный, перемерено собственным прогоном

Это тот самый инвариант, отказ которого был blocker'ом итерации 3,
поэтому он проверен здесь заново и целиком, а не принят по `review-4.md`.

Прогон независимый: собственный скрипт (не `fix-3.probe.mjs` и не скрипт
ревью), headless Chrome на своём профиле и своём порту `9377`, погашен по
PID собственного процесса. Разметка — голая страница, собранная из
статей пакета: голые `<a>`/`<button>`/`<input>` без классов, `.button`,
`.button-hard`, `<a class="button">`, `.form-field-input`,
`.form-field-box` отмеченный и невыбранный, `.form-field-box-round`,
`.site-header-link`, `<a class="product-lockup" href="/">`, ссылка
в `.form-section-note`. Подключён **один** файл `ui/landings.css`,
`document.styleSheets.length === 1` во всех прогонах. Обход — настоящими
Tab через `Input.dispatchKeyEvent`. Отклонением считалось любое
расхождение с `(outline-style: solid, outline-width: 3px,
outline-offset: 3px)` при истинном `matches(':focus-visible')`.

| комбинация | `--state-focus-ring` | узлов | отклонений |
|---|---|---|---|
| без `data-product`, тёмная | `#f2f0eb` | 13 | **0** |
| без `data-product`, светлая | `#302e38` | 13 | **0** |
| `data-product="habr"`, тёмная | `#06d0ef` | 13 | **0** |
| `data-product="habr"`, светлая | `#0b7f96` | 13 | **0** |

Заливки и знаки без продукта видимы, а не «формально есть»:

```
без product, тёмная:  .button rgb(242,240,235) / текст rgb(0,0,0)
                      .button-hard rgb(254,106,0) — снятое значение сохранено
                      чекбокс rgb(90,108,124) + белая галочка data-URI
                      радио rgb(90,108,124); лок-ап и ссылка подписи rgb(242,240,235)
без product, светлая: .button rgb(48,46,56) / текст rgb(253,253,252)
                      чекбокс и радио rgb(90,108,124)
habr, тёмная:         .button и контролы rgb(77,163,203); ссылка подписи rgb(6,208,239)
habr, светлая:        .button и контролы rgb(77,163,203); ссылка подписи rgb(11,127,150)
```

Контрасты пересчитаны формулой WCAG relative luminance независимо (Node),
а не переписаны из отчёта:

```
#f2f0eb / #000000 = 18.439    (заявлено 18.44)
#302e38 / #fdfdfc = 13.112    (заявлено 13.11)
#ffffff / #5a6c7c =  5.425    (заявлено 5.43)
#5a6c7c / #000000 =  3.871    (заявлено 3.87)
#5a6c7c / #fdfdfc =  5.330    (заявлено 5.33)
```

Замер целиком — `.pipeline/R0-08/accept.probe.json`. Результат совпал с
`review-4.md` по всем четырём комбинациям и по всем цветам: две
независимые сессии в разных процессах браузера, разные скрипты, один
результат.

Прочие требования инварианта: внешних URL в рабочих путях `ui/` нет
(единственные `https?://` — data-URI SVG и комментарии-источники),
шрифты и иконки локальные.

### 7. Exit criteria волны R0 — применимая часть зелёная, волна не закрыта

R0-08 предметно закрывает свою тему целиком: закрытый словарь из двадцати
слов, матрица обязательных по восьми видам, честная разметка того, каких
состояний в источниках нет. Из семи exit criteria волны шаг относится
к пункту 2 в части «каждое значение помечено: снято или спроектировано» —
контракт нормативен целиком и помечен, `validate-normative` EXIT=0.

Пункты 1, 3, 4, 5, 6, 7 — вне этого шага (R0-01, R0-02, R0-06, R0-07,
R0-00). **Волна R0 этим шагом не закрывается:** из 9 шагов приняты 5
(R0-00, R0-01, R0-02, R0-04, R0-08); R0-03, R0-05, R0-06 стоят
`in-progress`, R0-07 — `planned`.

### 8. Честность статуса — зелёный

- **Ни одно состояние не выдано за снятое.** `ui/state-contract.css`
  нормативен целиком; `README.md:126-131`, `ROADMAP.md` и `BRIEF.md` §5
  п. 8 держат одно и то же: в 18 макетах состояний не встретилось,
  и это ноль наблюдений, а не доказанное отсутствие.
- **Пробелы названы, а не заглажены.** `product-lockup` и
  `footer-social` — `partial` с `missingStates: ["hover"]`,
  `site-header` — `partial` с `missingStates: ["expanded"]` и адресом
  закрытия R2-05. Двенадцать нереализованных состояний названы поимённо
  с причиной по каждому.
- **Две `note` итерации 4 перенесены строками роадмапа**, как требовал
  вызывающий, — обе предварительно перепроверены приёмкой собственным
  `grep`, а не приняты по тексту отчёта:
  - **`X-22`** — `X-20` ссылается на `showcase/primitives.html:242`,
    содержательные вхождения `checked` в файле на `:255` и `:259`.
  - **`X-23`** — `blocks/product-lockup.md:155` цитирует
    `href="/"` для `showcase/core.html`, где фактически
    `href="#site-header"` (`:70,84`).
- **Приёмка завела третью строку — `X-24`.** `README.md:90` держал
  «Принято шагов конвейером **1 из 46** — `R0-01`. Ещё 18 шагов стоят
  `review`» после трёх приёмок подряд. Headline-число главного файла
  пакета врало, `validate-counts` его не сторожит, а `X-19` называет
  только прозу `ROADMAP.md` и этот файл не покрывает. Строка поправлена
  приёмкой (5 из 46, `review` 15), но починен экземпляр, а не причина —
  причина записана строкой.

### 9. Две проекции (machine layer) — не применим

Машинного слоя в пакете нет: волна R5, все семь шагов `planned`.
`validate-machine.mjs` не существует.

### 10. Правила доказаны — не применим

Шаг не выводит правило с покрытием и `data-rule`: он вводит словарь
и нормативный контракт. Наблюдение «состояний в источнике 0 из 18»
записано границей покрытия, а не правилом.

### 11. Страница из принятого — не применим

R0-08 не собирает страницу из компонентов — предмет волны R4.

---

## Что внесено в документы

### `ROADMAP.md`

1. Строка `R0-08`: `review` → **`done`** с датой, ссылкой на `accept.md`,
   сводом четырёх итераций review и трёх fix, названием главного blocker'а
   и результатом собственного замера копируемости.
2. «Где пакет сейчас»: `Статусы шагов` — `done` 4 → **5**, `review`
   16 → **15**. `Принято шагов` — 4 → **5**, добавлена запись о R0-08.
3. Раздел «Что значит статус `review`»: «Шестнадцать» → «Пятнадцать»,
   «`done` уже четыре» → «уже пять», R0-08 добавлен в перечень шагов,
   прошедших полный конвейер.
4. «Найдено по ходу»: добавлены `X-22`, `X-23` (две `note` review-4)
   и `X-24` (счётчик принятых шагов в `README.md`).
5. Форматная правка без изменения смысла: строка `X-21` была отделена
   от таблицы пустой строкой и рендерилась вне её. Пустая строка убрана,
   строка вернулась в таблицу — иначе `X-22 … X-24` пришлось бы дописывать
   к разорванной таблице.

Числа пересчитаны после правки: `validate-counts` — 170 утверждений,
17 файлов, EXIT=0. Все шесть валидаторов зелёные повторно.

### `README.md`

Строка `Принято шагов конвейером`: «1 из 46 — `R0-01`. Ещё 18 шагов стоят
`review`» → «**5 из 46** — `R0-00`, `R0-01`, `R0-02`, `R0-04`, `R0-08`.
Ещё 15 шагов стоят `review`». Причина устаревания записана строкой `X-24`.

### `CHANGELOG.md`

Одна смысловая запись в `## Unreleased` перед записью о R0-04: что
принято, что такое словарь двадцати и почему двенадцать слов не
реализованы, два blocker'а и major с их существом, изменение реестра
(`complete` 9 · `partial` 3), собственный замер копируемости приёмкой,
и что осталось открытым (`X-22`, `X-23`, `X-24`).

### Чего приёмка не трогала

- `components/STATES.md`, `ui/state-contract.css`, `ui/blocks/*.css`,
  `ui/landings.css`, `blocks/manifest.json`, `blocks/INDEX.md`,
  `blocks/product-lockup.md`, `blocks/site-header.md`,
  `blocks/form-field.md`, `showcase/primitives.html`,
  `showcase/showcase.css` — результат build → fix-1…3, уже проверен;
  приёмка фиксирует, что он верен, и не правит.
- `.pipeline/R0-08/*` кроме этого файла и своих логов — протокол
  review/fix чужой, задним числом не переписывается.
- Обе `note` review-4 **не исправлены** приёмкой, а вынесены строками:
  правка `blocks/product-lockup.md:155` и `X-20` — это правка, а не
  приёмка.
- `career/`, `courses/`, `habr/`, `partner-specials/`, `_sources/`,
  корпус `0*.md`, `.audits/`, `showcase-template/`.

---

## Волна R0 — не закрыта

Принято 5 шагов из 9 (R0-00, R0-01, R0-02, R0-04, R0-08). Из семи exit
criteria волны выполнены: пункт 1 (R0-01), пункты 2 и 3 (R0-02, часть
пункта 2 — R0-04 и R0-08), пункт 7 (R0-00). **Не хватает:** витрины и её
проверок вместе со `smoke`-спекой и скриптом `test` (R0-06,
`in-progress`), реестра как принятого шага (R0-05, `in-progress`),
границ покрытия `evidence/coverage.md` (R0-07, `planned`),
типографической шкалы как принятого шага (R0-03, `in-progress`) —
то есть пункты 4, 5, 6 exit criteria.

Волна R1 при этом остаётся `review`: пять из шести примитивов получили
`complete` за счёт состояний этого шага, но сами шаги R1-01 … R1-06
ревью конвейера не проходили, и `product-lockup` теперь `partial`.
Exit criteria R1 («все шесть записей `complete`») **не выполнены** —
именно этим шагом, честно.

**Релиз и тег не предлагаются.** Пакет — `v0.1` в разработке, волна
основания не закрыта, пользоваться им полноценно нельзя. Ближайший
осмысленный момент для тега — закрытие R0 целиком (`landings-v0.2`
по `VERSIONING.md`), и до него не хватает четырёх шагов.
