# Ревью R0-08 · `components/STATES.md`, `ui/state-contract.css` — итерация 4

Вердикт: **принять с замечаниями**
Режим: **инкрементальный** (есть `review-3.md` и `fix-3.md`)

Правка затронула общий CSS пакета (`ui/state-contract.css`, `ui/blocks/primitives.css`,
`ui/blocks/core.css`), поэтому инвариант копируемости перепроверен целиком заново,
а не точечно. `ui/tokens.css` и `ui/foundations.css` не изменены — `git diff --stat`
их не показывает.

Проверено: `ui/state-contract.css` целиком; `ui/blocks/primitives.css:50-70,470-476`;
`ui/blocks/core.css:148-160,500-510`; `ui/tokens.css` (области видимости слоёв,
`:root` против `[data-product]`); `components/STATES.md` целиком;
`blocks/manifest.json` (`product-lockup`, `footer-social`, счёт статусов);
`blocks/INDEX.md:36-90`; `blocks/product-lockup.md:150-182`; `blocks/site-header.md:125-150`;
`showcase/primitives.html` (diff и разметка примеров), `showcase/core.html:65-90`;
`README.md:85-135`; `ROADMAP.md:27-30,141-145,194-202,460-470`;
`evidence/source/figma/1324-7793.md`.

Живьём: витрина `tools/serve.mjs` на 4180 (уже был поднят), браузер — **headless
Chrome через CDP** на собственном `--user-data-dir`, `--remote-debugging-port=9341`,
без Playwright/Puppeteer; погашен по PID собственного процесса (`61604`),
`taskkill /IM` не применялся, чужие окна Chrome не тронуты. Скрипт замера написан
заново, а не заимствован у `fix-3.probe.mjs`: голая разметка своя, добавлены
светлая тема без продукта, `.button-hard`, `.product-lockup` на `<a href>`
и четвёртая комбинация `habr + light`. Фокус меряется **парой**
`(outlineStyle, outlineWidth)` плюс `outlineOffset`, `outlineColor`
и `matches(':focus-visible')`; обход — настоящими Tab через `Input.dispatchKeyEvent`;
`:hover` — `CSS.forcePseudoState` с замером **набора** свойств до и после.
Контрасты пересчитаны формулой WCAG relative luminance независимо (Node).

## Статус прежних находок

| # (review-3) | Severity | Статус | Чем подтверждено |
|---|---|---|---|
| 1 | **blocker** | **закрыта** | Грепом по `ui/` (кроме `tokens.css`): вхождений `var(--product-*)` — 12 строк, из них 3 в комментариях, **9 объявлений, и у всех девяти есть запасное значение**. Строгий `grep -E "var\(--product-[a-z0-9-]+\)"` (без запятой) даёт только два комментария и ни одного объявления. Заявленные fix-3 «одиннадцать» — это счёт строк **до** правки: в `HEAD` без fallback стояли `state-contract.css:50,154,159,160,169,174,175,176` (8) + `primitives.css:68,69` (2) + `core.css:506` (1) = 11 строк. Счёт сходится. Живьём на голой странице (`Page.setDocumentContent`, один `<link href="/ui/landings.css">`, `document.styleSheets.length === 1`, **без `data-product`**): кольцо `solid 3px / offset 3px rgb(242,240,235)` на **13 узлах обхода из 13, отклонений 0** — включая голые `<a>`, `<button>`, `<input>` без классов пакета; `--state-focus-ring` = `#f2f0eb`. Светлая тема без продукта: `#302e38`, 13 из 13, отклонений 0. `.button` — заливка `rgb(242,240,235)`, надпись `rgb(0,0,0)`; `.button-hard` сохранила снятую `rgb(254,106,0)`. Отмеченный чекбокс — `rgb(90,108,124)` с галочкой data-URI, промежуточный — та же заливка с белой чертой, радиокнопка — белая точка. Ссылка в подписи `rgb(242,240,235)` против подписи `rgb(143,143,143)` — отличима. Протечки слоя нет: `ui/tokens.css` не изменён вовсе, в `:root` продуктовых значений не появилось; запасные — `--core-text-primary`, `--core-bg-page`, `--core-text-secondary` (все объявлены в `:root` и `[data-theme="light"]`, `ui/tokens.css:229,250,252,323,326,337`), единственный литерал `#5a6c7c` равен `--core-border-field` тёмной темы (`ui/tokens.css:240`) и **не совпадает ни с одним `--product-cta`** (Хабр `#4da3cb`, Карьера `#7963e9`/`#7a3df5`, Курсы `#3670f4`). Источник литерала лежит на диске: `evidence/source/figma/1324-7793.md:57` — «граница чекбокса (выкл.) `2px solid #5a6c7c`», узел `I1324:8114;…6948`. Контрасты пересчитаны мной: `#f2f0eb`/`#000000` = 18.439, `#302e38`/`#fdfdfc` = 13.112, белый/`#5a6c7c` = 5.425, `#5a6c7c`/`#000000` = 3.871, `#5a6c7c`/`#fdfdfc` = 5.330 — заявленные 18.44 / 13.11 / 5.43 / 3.87 / 5.33 верны. **Регрессии на продукте нет:** `data-product="habr"` тёмная — кольцо `solid 3px rgb(6,208,239)`, чекбокс и кнопка `rgb(77,163,203)`, надпись белая, ссылка подписи `rgb(6,208,239)`; светлая — кольцо `rgb(11,127,150)`, остальное то же. 13 из 13 узлов, отклонений 0 в обеих |
| 2 | **major** | **закрыта** | Матрица различает две роли: `STATES.md:52` строка «Ссылка» несёт `.product-lockup` на `<a href>` (`default hover focus-visible`), `:57` строка «Знак и лента» — `.product-lockup` на `<span role="img">`; абзац `:117-135` объясняет расхождение и даёт таблицу «какая разметка где». Разметка проверена: `showcase/core.html:70,84` — два `<a class="product-lockup" href="#site-header">` (живьём `tabIndex = 0`), `blocks/site-header.md:144` — `<a class="product-lockup" href="/">`, `blocks/product-lockup.md:137` и `showcase/primitives.html:353,365` — `<span … role="img">`. **`href` у логотипа на месте, не убран.** Реестр: `product-lockup` — `status: "partial"`, `requiredStates: ["default","hover","focus-visible"]`, `missingStates: ["hover"]`. Живьём `CSS.forcePseudoState hover` на `a.product-lockup` не меняет ни одного из четырёх замеренных свойств — пробел настоящий и записан. Счёт статусов сведён: manifest даёт 9 `complete` / 3 `partial`, и то же стоит в `README.md:88`, `ROADMAP.md:30`, `blocks/INDEX.md:38` |
| 3 | minor | **закрыта** | `blocks/manifest.json:410` (`footer-social.statusReason`) теперь говорит «по матрице `components/STATES.md` строка „Ссылка“ обязана его нести» и прямо оговаривает, что термин стоял ошибочно. Термин «ссылка-кнопка» по пакету употреблён в трёх местах, и все три — про `.button` на `<a>`: `STATES.md:51`, `ui/state-contract.css:150`, `blocks/button.md:204`. Расхождения с `blocks/INDEX.md:69` больше нет |
| 4 | minor | **закрыта** | `STATES.md:59-86` переписан: «`disabled` навигационной ссылке неприменим **не потому, что механизма нет**. Механизм есть: … `aria-disabled="true"` существует, и пакет им пользуется», причина названа настоящая (выключенного пункта меню в 18 макетах нет, и показать нечего). Абзац про `<a class="button">` (`:70-74` прежней нумерации, теперь `:81-86`) с ним больше не спорит: класс объявляет роль, роль решает набор. Противоречия с `ui/state-contract.css:150-156` не осталось |
| 5 | note | **закрыта** | Группа разделена в обоих файлах. `STATES.md:265-283` и `ui/state-contract.css:273-294`: «7 без носителя» (`loading selected open closed empty dragActive error`) и отдельно «`current` — носитель есть, повода нет». Сходимость пересчитана мной по обоим файлам: **8 + 1 + 2 + 1 + 7 + 1 = 20**, группы не пересекаются, объединение равно словарю из 20 слов; двенадцать нереализованных названы поимённо и их ровно двенадцать |

## Находки итерации 4

| # | ID | Severity | Пункт | Находка | Где | Как проверить |
|---|---|---|---|---|---|---|
| 1 | states | note | E.19 | **Строка `X-20` роадмапа указывает на строку, которой там больше нет.** `ROADMAP.md:465` ссылается на живой пример `checked` как на `showcase/primitives.html:242`; эта же итерация вставила в файл абзац-предупреждение про `data-product` (13 строк, `:117-130`), и пример уехал на `:255`. Ссылка на строку — способ воспроизвести находку, и он теперь ведёт мимо. Селектор в строке не назван, только номер | `ROADMAP.md:465` против `showcase/primitives.html:255` | `grep -n "checked" landings/showcase/primitives.html` — `255` и `259`, на `242` содержательного примера нет |
| 2 | states | note | E.19 | **Спецификация лок-апа цитирует разметку, которой в названном файле нет.** `blocks/product-lockup.md:155`: «`blocks/site-header.md` и `showcase/core.html` несут `<a class="product-lockup" href="/">`». В `site-header.md:144` — действительно `href="/"`, а в `showcase/core.html:70,84` — `href="#site-header"` (витрина одностраничная, логотип ведёт на якорь шапки). Существу находки 2 review-3 это не мешает — оба экземпляра остаются `<a href>`, — но цитата разметки неточна для одного из двух названных файлов. `STATES.md:124` ту же мысль формулирует правильно: `<a class="product-lockup" href="…">` | `blocks/product-lockup.md:155` против `showcase/core.html:70,84` | `grep -n 'class="product-lockup' landings/showcase/core.html` |

Обе находки — `note`: на применимость элемента не влияют, вердикт не роняют
и в roadmap строками не уходят (по чеклисту `note` — наблюдение на будущее).
Ни одного `blocker`, `major` или `minor` по существу не найдено.

## Что проверено и претензий нет

Проверено в этой итерации заново (правка задела общий CSS):

- **Инвариант копируемости METHOD §6.1 — живьём, в четырёх комбинациях.**
  Голая страница, один `ui/landings.css`, `document.styleSheets.length === 1`,
  разметка собрана мной из статей пакета (голые `<a>`/`<button>`/`<input>`,
  `.button`, `.button-hard`, `<a class="button">`, `.form-field-input`,
  `.form-field-box` отмеченный и промежуточный, `.form-field-box-round`,
  `.site-header-link`, `<a class="product-lockup">`, ссылка в
  `.form-section-note`). Обход настоящими Tab:

  | комбинация | `--state-focus-ring` | узлов | отклонений |
  |---|---|---|---|
  | без `data-product`, тёмная | `#f2f0eb` | 13 | **0** |
  | без `data-product`, светлая | `#302e38` | 13 | **0** |
  | `data-product="habr"`, тёмная | `#06d0ef` | 13 | **0** |
  | `data-product="habr"`, светлая | `#0b7f96` | 13 | **0** |

  Отклонением считалось любое расхождение с `(solid, 3px, offset 3px)`;
  `matches(':focus-visible')` истинно у всех.
- **Ни одного бесфоллбэчного `var(--product-*)` в `ui/`.** 9 объявлений,
  у всех девяти запасное значение; `--lockup-word-width` — единственная
  другая переменная вне слоёв `core`/`state`/`product`, и у неё тоже
  есть fallback (`143.803px`). `var(--concept-*)` в `ui/` нет вовсе.
- **Заливки и знаки состояний без продукта видимы, а не «формально есть»:**
  отмеченный чекбокс `rgb(90,108,124)` + белая галочка (5.43 : 1),
  промежуточный — та же заливка + белая черта, радиокнопка — белая точка,
  `.button` — `rgb(242,240,235)` с чёрной надписью (18.44 : 1 в тёмной,
  13.11 : 1 в светлой). Все числа пересчитаны формулой независимо.
- **Наведение — набором свойств, а не одним.** Без продукта: `.form-field-box`
  `borderColor` `rgb(90,108,124) → rgb(143,143,143)` (тёмная),
  `→ rgb(103,113,125)` (светлая); `.button` `backgroundImage`
  `none → linear-gradient(rgba(255,255,255,0.12)…)` в тёмной и
  `rgba(0,0,0,0.07)` в светлой; `.site-header-link` `textDecorationLine`
  `none → underline`. С `data-product="habr"` наведение на **отмеченный**
  переключатель не меняет ничего — это уже записано строкой `X-21`
  роадмапа, воспроизведено мной независимо.
- **Витрина.** Обход настоящими Tab: `showcase/core.html` — 36 фокусируемых
  узлов, 36 пройдено, **отклонений 0**; `showcase/primitives.html` — 23 из 23,
  отклонений 0; кольцо `solid 3px / offset 3px rgb(6,208,239)`.
- **Ширины.** `core.html` при 320 / 768 / 1024 / 1400: `scrollWidth ===
  clientWidth` (305 / 753 / 1009 / 1385). `primitives.html` при 320 и 1400 —
  305 и 1385. Горизонтального overflow нет ни на одной.
- **Сходимость словаря до 20 пересчитана после перевода `current`.**
  8 (`hover focus-visible pressed checked indeterminate disabled readOnly
  invalid`) + 1 (`default`) + 2 (`expanded`/`collapsed`) + 1 (`success`)
  + 7 (`loading selected open closed empty dragActive error`) + 1 (`current`)
  = 20. Одинаково в `components/STATES.md:258-283` и
  `ui/state-contract.css:273-294`; пересечений групп нет, двенадцать
  нереализованных названы и их двенадцать.
- **Матрица — восемь строк** (`STATES.md:50-58`), ровно как заявляет
  `ROADMAP.md:144`; критерий восьмёрки («есть правило в
  `ui/state-contract.css`») сходится: в файле ровно восемь различных имён
  словаря, правила `default` нет ни одного.
- **Согласие реестра, статей и разметки по лок-апу.** `manifest.json`
  `partial` / `missingStates: ["hover"]`; `INDEX.md:68` строка в таблице
  «Чего не хватает»; `product-lockup.md:173-182` раздел «Ограничения»;
  `ROADMAP.md:30,197`; `README.md:88`. Старая формулировка «лок-ап
  интерактивным не бывает» осталась только там, где названа ошибкой
  прежней редакции (`manifest.json:86`, `STATES.md:127`).
- **Литерал `#5a6c7c` — не новый цвет и не цвет продукта.** Равен
  `--core-border-field` тёмной темы (`ui/tokens.css:240`), подтверждён
  сохранённым evidence `evidence/source/figma/1324-7793.md:57` (узел
  `I1324:8114;…6948`), от всех трёх `--product-cta` отличается. Выбор
  литерала вместо `var(--core-border-field)` обоснован в `@normative`
  комментарии (`state-contract.css:89`): ядровая роль меняет светлоту
  с темой, а знак поверх белый решением Р-6 — на светлом `#acb8bf`
  белая галочка пропала бы. Обоснование проверяемое и верное.
- **Тексты правок непротиворечивы.** Абзац про `disabled` у ссылки
  (`STATES.md:59-86`) больше не спорит с `state-contract.css:150-156`;
  термин «ссылка-кнопка» употреблён во всех трёх местах пакета одинаково;
  раздел «Как проверить» (`STATES.md:322-330`) прямо требует мерить кольцо
  на пустой странице без `data-product` и **парой** свойств, потому что
  `3px` показывается и при `outline-style: none`; то же предупреждение
  стоит на витрине примитивов рядом с живым примером.
- **Шесть валидаторов — exit 0 каждый.** blocks (12 записей, `cssRoots`
  и якоря сошлись), classes (201 класс в 4 файлах против 11 таблиц;
  витрин 3 с префиксом `doc-`, файлов `ui/` без `doc-` — 8), counts
  (170 утверждений в 17 файлах), normative (777 объявлений в 8 файлах CSS,
  17 статей, 3 нормативные области), radius (8 пар, нарушений 0), tokens
  (572 ссылки в 11 файлах CSS и 20 статьях, 69 значений в таблицах,
  123 имени). Совпадает с `fix-3.*.log` построчно и с заявленной
  дельтой против `review-3` (775 → 777 объявлений, 565 → 572 ссылки).
  Логи: `.pipeline/R0-08/review-4.validate-{blocks,classes,counts,normative,radius,tokens}.log`.

Перенесено из review-1…review-3 без перепроверки, **не затронуто изменениями**:
подключение `state-contract.css` последней строкой `ui/landings.css`; отсутствие
`:focus` в `ui/foundations.css` и в оболочке витрины (`showcase/showcase.css`
кольца не назначает); состояния живут в одном файле; наличие классов-целей
состояний на разметке витрины; невыдуманность ARIA-механизмов
(`aria-expanded`, `aria-disabled`, `aria-invalid`, `role="img"`); контрасты
цвета ошибки `#FF7A70` / `#C53025`; источник GAP контраста белой галочки
на CTA Хабра (`tokens.css:378,453,470`, `GUIDE.md` §6); белизна
`--product-on-acid` у трёх продуктов в обеих темах; поимённый список голых
селекторов стенда в `tools/validate-classes.mjs`; запись `site-header`
(`partial`, `missingStates: ["expanded"]`, шаг R2-05); запись `footer-social`
(`missingStates: ["hover"]`, живьём наведение ничего не меняет);
`.form-field-textarea` покрыт правилами на `.form-field-input`, потому что
классы всегда идут парой; карточки, чипы и лента ссылками нигде не стали.

## Не проверено

- **Геометрия покоя против Figma и продакшена (A.1–A.4).** В объём R0-08
  не входит — шаг о состояниях. Из `evidence/source/figma/` открыт только
  `1324-7793.md` — под проверку источника литерала `#5a6c7c`. Новых снимков
  ни на проде, ни в Figma MCP не делалось.
- **Наведение мышью как жест.** `:hover` меряется `CSS.forcePseudoState`:
  в headless реального указателя нет. Для каскада это эквивалентно,
  событийная часть (тач без hover) не проверялась ничем.
- **`forced-colors`, `prefers-reduced-motion`, печать.** В объём шага
  не входят и в контракте не заявлены; находкой не считаю. Поведение
  `outline` при принудительных цветах не проверялось ни в одной итерации.
- **Длинный текст и пустое значение (D.16), переключение композиции
  на брейкпоинтах (D.17).** Ни одно правило `state-contract.css` не трогает
  размеры и потоки; полноценная проверка относится к шагам вёрстки блоков
  (R1-xx, R2-xx).
- **Содержимое `blocks/site-header.md`, `blocks/footer-social.md`
  и `blocks/product-lockup.md` за пределами точки стыка со STATES.md** —
  принадлежит R1-01 / R2-03 / R2-05.
- **Разделы F, G, H чеклиста** — страницы, принципы и машинный слой:
  в объём R0-08 не входят, соответствующих артефактов шаг не создаёт.

## Годится ли шаг для guide-accept

Да. Все пять находок review-3 закрыты и перепроверены независимо, а не
по прозе отчёта: blocker подтверждён замером в четырёх комбинациях темы
и продукта, major — сверкой матрицы, реестра и трёх файлов разметки,
оба minor — чтением спорных абзацев рядом с CSS, note — пересчётом
сходимости до 20. Шесть валидаторов зелёные. Инвариант копируемости,
который шаг объявляет закрытым, теперь действительно держится на голой
странице без `data-product` — 13 узлов из 13 в обеих темах. Две находки
итерации — `note`: устаревший номер строки в backlog-строке `X-20`
и неточная цитата `href` в `blocks/product-lockup.md:155`. Ни одна
из них не мешает пользоваться контрактом состояний и приёмку не держит.
