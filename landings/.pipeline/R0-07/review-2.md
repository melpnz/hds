# Ревью R0-07 · `evidence/coverage.md` — итерация 2

Вердикт: **вернуть на доработку**
Режим: **инкрементальный** (`review-1.md` + `fix-1.md`)

Основание для инкрементального режима: diff не трогает CSS entrypoint,
foundations, tokens, shared markup и состав batch. `git diff --stat` по
`landings/` — шесть файлов: `.pipeline/inventory.json`, `BRIEF.md`, `README.md`,
`ROADMAP.md`, `tools/README.md`, `tools/validate-counts.mjs` (188 вставок,
80 удалений); `evidence/coverage.md` и `.pipeline/R0-07/` — новые пути.
Ни одного изменённого байта в `ui/`, `blocks/`, `showcase/`, `components/`,
`manifest.json`, `career/`, `_sources/` — заявление `fix-1.md` проверено
`git status` и подтверждается.

Прогоны — свои, не по логам автора: шесть сторожей, логи в
`.pipeline/R0-07/review-2.validate-{blocks,counts,classes,normative,radius,tokens}.log`.
Все шесть **EXIT=0**. `validate-counts` — «Проверено утверждений: **196 в 26
файлах**. Все сходятся.» Оговорка: `validate-classes.mjs` флага `--strict`
не понимает — он трактует его как имя файла и падает с `EXIT=2` («Нет файлов:
--strict»); прогон без флага даёт `EXIT=0`. Это свойство инструмента, не шага,
и в объём R0-07 не входит; в лог положен прогон без флага.

Красные тесты нового правила прогнаны заново, шесть вариантов, каждый с
откатом и `cmp` (байт в байт) — раздел «Проверка правила сторожа» ниже.
Итоговый `md5sum` `evidence/coverage.md` и `.pipeline/inventory.json` после
всех тестов совпал с исходным.

Браузер не поднимался: шаг не даёт ни разметки, ни CSS, ни витрины.

---

## Находки

| # | ID | Severity | Пункт | Находка | Где | Как воспроизвести |
|---|---|---|---|---|---|---|
| 11 | coverage | **major** | A.1, E.18 | **Граница 16 называет число сторожа, которое эта же итерация сделала неверным.** Текст: «Сторож держит **188 утверждений в 25 файлах**». Фактический прогон — **196 в 26**: правка `validate-counts.mjs` в этом же fix добавила семь привязок и самопроверку, а `evidence/coverage.md` вошёл в область сканирования 26-м файлом. `fix-1.md:63` сам пишет «было 188 в 25, стало 196 в 26» и `capture.md:181` повторяет — то есть число известно автору правки и не перенесено в предмет шага. Утверждение самофальсифицируемо: «Чем подтверждается» этой же границы называет команду `node tools/validate-counts.mjs --explain`, и она опровергает написанное рядом. Собственное правило файла («Знаменатель считается на момент чтения, а не берётся из прежней редакции», `:31-32`) нарушено в самом файле. Сторожем это число не покрыто: ключа на «утверждений N в M файлах» в `validate-counts` нет, поэтому регрессия прошла зелёной | `evidence/coverage.md:725` | `node tools/validate-counts.mjs` → «196 в 26 файлах» против «188 в 25» в тексте |
| 12 | coverage | minor | B.10 | **`X-33` применён к документам, но не к реестру.** `sources.production.note` держит «**Шесть узлов** сходятся с проектами корпуса, но не теми же редакциями» — ровно ту формулировку, которую граница 10, `BRIEF.md:229-232`, `README.md:156` и строка `X-33` называют неверной («узлов семь, проектов шесть»). Машинное зеркало противоречит границе 10 в том самом числе, ради которого заведена строка роадмапа | `.pipeline/inventory.json:84` | `node -e "console.log(require('./.pipeline/inventory.json').sources.production.note)"` → «Шесть узлов сходятся…» |
| 13 | coverage | minor | B.10 | **`CL-7` — шестая запись реестра, которую файл объявляет неверной, и она не переписана.** Текст записи: «Переменных Figma в лендингах нет: `get_variable_defs` отдаёт только `logo/*`». Граница 7 отдельным абзацем пишет: «**Формулировка „переменных Figma нет“ неверна и исправлена**» — их 11 на `20319:366`, и одну пакет цитирует по имени. `README.md` п. 7 и `BRIEF.md` §5 п. 7 уже переписаны, реестр — нет. Расходится и адрес: `closedBy: "R0-02"` против «Не закрывается: в источнике переменных нет. Граница описательная» (`:323-324`) и «не закрывается» в сводной таблице (`:47`). Пять записей из шести, названных находкой 1 review-1, переписаны верно — эта осталась | `.pipeline/inventory.json:273`; `evidence/coverage.md:304-309`, `:323-324`, `:47` | `node -e "console.log(require('./.pipeline/inventory.json').coverageLimits[6])"` — сравнить с границей 7 |
| 14 | coverage | minor | E.19 | **Протокол шага после правок содержит два утверждения, которые правки отменили, и §7 их не снимает.** Шапка: «Результат шага — один новый файл… **Больше в пакете не изменено ничего**» (`:7-8`) — изменено шесть файлов. §4, четвёртый пункт: «**Не заводил ключей `validate-counts`** на числа нового файла: правка сторожа в объём шага не входит, а `evidence/coverage.md` в область сканирования не входит» (`:91-94`) — заведено семь привязок и самопроверка, файл в области сканирования. §7 честно снимает «пункт 1 §4», «пункты 1–2 §6» и «последнюю фразу §5», но эти два места не называет. Приёмка судит о шаге по протоколу, не открывая терминал (это утверждает сама граница 14, `:665`) | `.pipeline/R0-07/capture.md:7-8`, `:91-94`, `:167-186` | прочесть §7 и сверить со списком «Изменённые файлы» `fix-1.md:26-48` |
| 15 | coverage | note | A.1 | **Прозаический вариант тех же границ перенос чисел не получил.** `GUIDE.md` §14 — файл называет его прозаическим вариантом (`:20`) — держит: п. 1 «Сплошь сняты только `18085:3890` (**117.7 КБ**) и `1324:7793` (**52.8 КБ**)» — те самые числа, которые note 10 review-1 снял, а fix заменил на измеренные 116 604 и 54 199 байт; п. 4 «Нижние ≈2460 px **не видел никто**» — против границы 2, где пять блоков нижней зоны названы и измерены досъёмкой R0-00. `GUIDE.md` не значится ни в хендоффе `.pipeline/R0-00/capture.md:562-570`, ни в списке изменённых файлов fix, поэтому это не невыполненная работа шага, а незамеченный адрес того же переноса. Просится строкой `X-*`, а не молчаливой правкой | `GUIDE.md:1382`, `:1388`; `evidence/coverage.md:68-71`, `:138-143` | `grep -n "117.7 КБ\|не видел никто" GUIDE.md` |

---

## Прежние находки: перепроверка каждой

| # | Прежняя severity | Статус | Чем проверено |
|---|---|---|---|
| 1 (перенос чисел) | major | **закрыто** | `coverageLimits.length` = **17** (`node -e` по реестру). `CL-1`, `CL-3`, `CL-4`, `CL-8`, `CL-9` переписаны и сошлись с границами 1, 3, 4, 8, 9 дословно по числам; `CL-13`…`CL-17` заведены и сошлись с границами 13–17. `capturedCss` и `capturedMetadata` = **18**, `capturedCssFull` = **2**, `capturedNote` называет происхождение и прежнее значение; `pageNote` описывает `0:1` перечислением семи детей — сошлось с границей 4 и `.pipeline/R0-00/capture.md:199-203`. Синхронность: `ROADMAP.md:25` («Снято CSS»), `:40` (мобильные артборды), `:77` (перечень сторожимых чисел), `:144` (строка шага), `:185` (exit criteria п. 5), `:371` (`P0-4`); `README.md:85-86` (таблица Coverage — структура и CSS 18 из 18), `:99-100` (шапка Known limits, 17 границ), Known limits п. 1, 3, 4, 12, 15; `BRIEF.md:142-155` (§4.1), `:229-232` (§4.4), `:244-300` (§5 п. 1–4, 8, 9), `:611` (схема реестра), `:658-660` (exit criteria 11). Ни одного противоречия с `.pipeline/R0-00/capture.md` §1–§8 не нашёл: сравнил построчно с таблицей хендоффа `:562-570` — перенесены все семь её строк, включая «83 стыка из 85» (проверил `grep` по `ROADMAP.md`, `BRIEF.md`, `README.md`, реестру: прежних «86 из 88» нет нигде). Незакрытые остатки переноса вынесены находками 12 и 13 |
| 1 (правило сторожа) | major | **закрыто** | Правило структурное, не прозаическое; шесть красных прогонов ниже. Двойного счёта нет: до правки на ключе `coverageLimits` висела одна привязка (`BRIEF.md`, строка схемы реестра — `git show HEAD:landings/tools/validate-counts.mjs` даёт ровно её), стало семь. «Шесть привязок» `fix-1.md` — это шесть **новых**, счёт верен. Правило `themePairs`, переанкеренное на переписанную строку R0-07, живо: у сторожа ненайденная формулировка — ошибка, а не пропуск (`tools/validate-counts.mjs:464-468`), и прогон зелёный, значит все семь новых и переанкеренная старая привязки нашлись |
| 1 (владелец досъёмки) | major | **закрыто строкой** | `ROADMAP.md:478` — `X-31`, статус `planned`, формулировка «владельца у неё нет: назначить владельца или признать, что опора остаётся такой», в колонке источника — «владелец **не назначен**». Прямо сказано «Перенос чисел R0-07 эту дыру не закрывает — он её именует». Ссылки из границ 1 (`:115-116`) и 8а (`:377-378`) стоят |
| 2 (три `partial`) | major | **закрыто** | `blocks/manifest.json`: `product-lockup` `["hover"]`, `footer-social` `["hover"]`, `site-header` `["expanded"]`; `complete` 9 · `partial` 3 из 12. Совпало поимённо с `evidence/coverage.md:434-442`, `README.md:88`, `ROADMAP.md:30`. Причины названы через матрицу видов (`Ссылка` ×2, `Раскрывающийся контрол`) |
| 3 (имена фреймов) | minor | **закрыто** | `grep -c "GAP — не снималось" .audits/landings-2026-09-08-figma-landings.md` → **8**; прозаическая сводка того же аудита `:42` — «сняты у 9 узлов из 18»; `1284:3362` в таблице `:35` назван `1400`. Граница 3 теперь пишет «Имена известны у **10** узлов из 18; у остальных восьми», расхождение вынесено строкой `X-32` (`ROADMAP.md:479`, статус `done`, «аудит не правится»). Итог «18 из 18» разложен: 10 из таблицы + 9 доснятых, одно пересечение — арифметика верна |
| 4 (production) | minor | **закрыто в документах** | Граница 10 (`:516-518`) — «**7 узлов из 18** сходятся с **шестью** проектами… `17473:826` и `6509:20474` ведут на один `technotext.habr.com`», перечисление даёт семь node id; `BRIEF.md:229-232` и `README.md:156` переписаны; `X-33` — `ROADMAP.md:480`, `done`. В реестре не доведено — находка 12 |
| 5 (протоколы съёмки) | minor | **закрыто** | Пересчитал сам по диску: `ls -d .pipeline/R0-*/ \| wc -l` → **8**, `ls .pipeline/R0-*/capture.md \| wc -l` → **3** (`R0-00`, `R0-01`, `R0-07`). Граница 14 несёт «3 из 8», заголовок «нет у пяти шагов» (8 − 3 = 5), перечислены пятеро поимённо; знаменатель в тексте задан обеими командами, а не списком имён. Состав `R0-05` — действительно один `accept.md` |
| 6 (`X-12`) | minor | **закрыто** | `README.md` Known limits — **пункт 15** про канон ширин, 15 узлов из 18, с ширинами артбордов и ссылкой на `X-12`; список вырос с 14 пунктов до 15. `ROADMAP.md:459` — «**Обе записи сделаны 9 сентября 2026 шагом R0-07**», владельцы разделены («R0-07 (записи, сделано), R2, R3 (решения на съёмке)»), статус `in-progress`. «Адрес закрытия» границы 15 (`:712-716`) переписан на то же |
| 7 (четыре части) | minor | **закрыто** | Разобрал файл скриптом по диапазонам заголовков. «Чем подтверждается» и «Адрес закрытия» есть у всех семнадцати границ, считая 8а, 8б, 8в по отдельности; у зонтичного `## 8.` их нет — и это ровно то исключение, которое «Как читать» теперь оговаривает прямым текстом (`:35-37`: «четыре части несёт каждый подпункт, а в реестре… одна запись — `CL-8`»). Восполнено против прошлой итерации: 8а `:377-381`, 8б `:402-405`, 14 `:674-677`. Части «что неизвестно» и «число» кое-где несут другие заголовки по смыслу («Было / Стало» у 3, «Что стало известно / Что осталось неизвестным» у 8б, «Сколько материала» у 9) — содержание на месте, к формулировке «Как читать» это не противоречит |
| 8 (оценочное и будущее) | note | **закрыто** | `grep -nE "будет\|должно\|планируется\|хорошо\|отлично\|красиво\|удачно" evidence/coverage.md` → **0 попаданий**. «снята хорошо» → «снята поимённо, с кеглем и начертанием, в 4 узлах из 18» (`:596-597`); «раздела о голосе не будет» → «раздела о голосе в нём нет» (`:582-583`) |
| 9 (имена 3D-экспортов) | note | **закрыто** | `:550-558` называет происхождение: слои узла `20572:37377` и соседних. Сверил с источником — `.audits/landings-2026-09-08-figma-landings.md:149` (§3.2, признак B) и `:208` (§4, строка 18) действительно несут `робот-Camera 1`, `звезды-Camera 24` и остальные четыре. Сказано прямо: «к нулю на бренд-досках отношения не имеет» |
| 10 (размеры дампов) | note | **закрыто** | `wc -c` мой собственный: `raw-18085.txt` — **116 604**, `raw-1324.txt` — **54 199**. Ровно эти числа в `:68-69`. Прежние 117.7 / 52.8 КБ названы числами `coverage-notes.md` §1 об объёме ответа MCP, с оговоркой, что единица там не названа (`:69-71`). Тот же адрес в `GUIDE.md` — note 15 |

---

## Проверка правила сторожа

Шесть красных прогонов, каждый — подделка, прогон, откат, `cmp` байт в байт.
После всех шести `md5sum` затронутых файлов совпал с исходным
(`evidence/coverage.md` `e83ef3249cb6dd3651a1a41487d451ba`,
`.pipeline/inventory.json` `3c7f4476ae7940fa9fae5208307ca03d`), прогон зелёный.

| # | Подделка | Ожидание | Факт |
|---|---|---|---|
| 1 | проза `coverage.md:8-9`: «Границ в файле **17**» → **12** | 1 | `evidence/coverage.md: заявлено 12, на самом деле 17` · EXIT=1 |
| 2 | из `coverage.md` вырезан раздел `## 17.` (разделов 16) | 1 | `inventory.coverageLimits — длина против разделов = 17, пересчёт даёт 16` · EXIT=1 |
| 3 | из реестра удалена запись `CL-17` | 1 | **8 расхождений разом**: семь привязок (ROADMAP · coverage.md · README ×2 · BRIEF ×2 · tools/README) + самопроверка · EXIT=1 |
| 4 | `ROADMAP.md:25`: «Снято CSS \| **18 узлов из 18**» → **9** | 1 | `заявлено 9, на самом деле 18 (measuredCss)` · EXIT=1 |
| 5 | `coverage.md:134`: «83 %» → «55 %» (неанкеренное число) | 0 | EXIT=0 — **ожидаемо**: правило сторожит число границ, а не каждое число файла. Совпадает с тем, что заявляют и файл (`:14-16`), и `tools/README.md`, и `fix-1.md`; заужения области не заявлено нигде |
| 6 | **согласованная подделка**: проза `coverage.md`, `ROADMAP.md`, `README.md` ×2, `BRIEF.md` ×2, `tools/README.md` и реестр — все говорят **12**, разделов в файле по-прежнему 17 | 1 | `inventory.coverageLimits — длина против разделов = 12, пересчёт даёт 17` · EXIT=1 |

Прогон 6 — решающий: он опровергает единственный способ, которым правило
могло бы оказаться сверкой документа с самим собой. Согласованная ложь во всех
семи прозаических местах разом ловится структурой файла. Правило считает
заголовки `^## \d+\. `, подпункты `### 8а/8б/8в` буквой не считаются —
проверено прямым `grep -c`: 17 против 20 заголовков всего.

**Задвоения старых правил нет.** До правки на ключе `coverageLimits` висела
одна привязка (`BRIEF.md`, строка схемы реестра); стало семь, из них шесть
новых — счёт `fix-1.md` верен. Прирост прогона 188 → 196 раскладывается точно:
+7 привязок и +1 самопроверка; 25 → 26 файлов — вошёл `evidence/coverage.md`.
Ненайденная формулировка у этого сторожа — ошибка, а не пропуск
(`tools/validate-counts.mjs:464-468`), значит зелёный прогон доказывает, что
нашлись все семь новых **и** переанкеренная старая привязка `themePairs`.

---

## Что проверено и претензий нет

**Не затронуто изменениями** — переношу из `review-1.md` без перепроверки:
состав каркаса (двенадцать границ `BRIEF.md` §5 на месте, номера сохранены),
числа границ 1, 2, 5, 6, 7, 8а, 8б, 8в, 9, 13, 15, 17, сверенные там с
открытым источником; поимённые списки тем 9 / 6 / 3; арифметика 83 стыка
из 85; разложение состояний 8 + 1 + 2 + 1 + 7 + 1 = 20; распределение баннеров
11 · 10 · 1 · 1 = 23; ширины артбордов 8 + 5 + 3 + 1 + 1 = 18.

**Проверено заново, потому что затронуто diff.**

- **Реестр против файла, запись за записью.** Прошёл все семнадцать
  `coverageLimits` против соответствующих границ. Сошлись `CL-1`…`CL-6`,
  `CL-8`…`CL-17` — по числам, по перечням узлов и по адресу закрытия.
  `CL-1.withoutCssDump` — семь узлов, и это ровно те 3 + 4, что граница 1
  делит на «только `get_metadata`» и «ничего». Разошлась одна запись, `CL-7`
  (находка 13).
- **Реестр против `.pipeline/R0-00/capture.md`.** Таблица хендоффа `:562-570`
  перенесена целиком; «Итоговое число после этого шага» `:543-549` совпало с
  границей 1 и `capturedNote` слово в слово по смыслу. Предупреждение R0-00
  «ЧИСЛО ОЖИДАЕТ ПЕРЕПИСЫВАНИЯ В R0-07» больше не действует — перенос сделан.
- **Три `partial`** — против `blocks/manifest.json` программно, не на глаз.
- **`X-31`…`X-34`** — все четыре строки на месте (`ROADMAP.md:478-481`),
  статусы `planned` / `done` / `done` / `planned` соответствуют содержанию.
  `X-31` и `X-34` вынесены строками, а не починены молча: `tools/README.md`
  по-прежнему пишет «40 находок, 4 вопроса» при пяти — и `X-34` называет это
  своими словами, включая непокрытость сторожем.
- **Стиль.** Активный залог, настоящее время, дроби со знаменателем; ноль
  попаданий по запрещённым словам. Оценочных усилителей в новом тексте нет.
- **Границы 16 «ключей на `L-*` — 0»** — подтверждаю: `--explain` не печатает
  ни одного ключа вида `L-N`. Ключей `cover:*` — **19**, ровно по числу типов
  блоков в реестре. Оговорка: команда review-1 `grep -c "L-[0-9]"` теперь даёт
  1 — она ловит новый комментарий про `CL-8` в сторожe; по существу ничего не
  изменилось. К числу 188/25 в той же границе — находка 11.
- **Объём.** Ни одного изменённого байта вне шести файлов и двух новых путей.
  Figma MCP и прод не трогались — новых снимков в `evidence/source/` нет,
  `git status` это подтверждает.

---

## Что делать шагу fix

1. Находка 11 (**major**) — переписать число границы 16 на фактическое
   (**196 в 26 файлах**) и заодно решить, не заводить ли на него ключ:
   у `X-11` есть ровно этот адрес. Если ключ не заводить — сказать в границе,
   что число не сторожится, чтобы следующая правка сторожа не оставила его
   позади ещё раз.
2. Находка 12 — переписать `sources.production.note` на «семь узлов сходятся
   с шестью проектами», как уже сделано в `BRIEF.md` и `README.md`.
3. Находка 13 — переписать `CL-7` под границу 7 (собственных нет, чужих 11 на
   `20319:366`) и поправить `closedBy` на «не закрывается».
4. Находка 14 — снять в §7 протокола шапку `:7-8` и четвёртый пункт §4, как
   уже сняты пункт 1 §4 и пункты 1–2 §6.
5. Находка 15 — строкой `X-*` в роадмап: `GUIDE.md` §14 п. 1 и п. 4 остались
   на числах, которые перенос отменил. Молча править `GUIDE.md` в этом шаге
   не надо — он в хендофф не входил.
