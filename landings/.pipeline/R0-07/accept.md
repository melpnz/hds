# Приёмка R0-07 — границы покрытия, первая редакция

Вердикт: **ПРИНЯТО**
Дата: 9 сентября 2026. Приёмщик: `guide-accept`.
Статус шага в `ROADMAP.md` — `done`.

Основание: `review-3.md` — «принять с замечаниями», blocker 0, major 0,
minor 1, note 1. Минор закрыт этой приёмкой (см. §4), note действий
не требует.

---

## 1. Автоматические гейты

`\.claude/guide/validate-step.mjs` **не применим**: у шага нет id вида
`component | page | principle | machine` — результат шага это документ
границ покрытия и реестр `.pipeline/inventory.json`, а не запись манифеста.
Та же ситуация, что у `R0-05` и `R0-08`; применимая автоматика пакета —
шесть его собственных сторожей, прогнаны приёмкой заново.

| Инструмент | Итог | Exit | Лог |
|---|---|---|---|
| `validate-blocks.mjs` | Записей 12: статьи на месте, cssRoots сошлись, якоря сошлись | 0 | `accept.validate-blocks.log` |
| `validate-blocks.mjs --strict` | то же, строгий режим | 0 | `accept.validate-blocks-strict.log` |
| `validate-counts.mjs` | Проверено утверждений: 197 в 26 файлах. Все сходятся | 0 | `accept.validate-counts.log` |
| `validate-classes.mjs` | классов 201 в 4 файлах против 11 таблиц; витрин с `doc-` — 3, файлов `ui/` без `doc-` — 8 | 0 | `accept.validate-classes.log` |
| `validate-normative.mjs` | объявлений 771 в 8 файлах CSS, статей 17; нормативных областей 3 | 0 | `accept.validate-normative.log` |
| `validate-radius.mjs` | Ф-3 (Р-8): пар 8, нарушений 0; лестница `0 · 6 · 16 · пилюля` | 0 | `accept.validate-radius.log` |
| `validate-tokens.mjs` | ссылок 593 в 11 файлах CSS и 20 статьях; сверено значений 77, имён 122 | 0 | `accept.validate-tokens.log` |

Прогон повторён **после** правок этой приёмки (`ROADMAP.md`, `CHANGELOG.md`) —
все шесть остались 0, число утверждений не сдвинулось.

---

## 2. Красный тест сторожа — свой прогон

Шаг менял `tools/validate-counts.mjs`, поэтому сторож проверен собственным
прогоном приёмки, а не по логам автора. Лог — `accept.red-test.log`.
Каждый вариант с откатом; `md5` шести файлов до и после всех тестов совпал
(`evidence/coverage.md` — `49e40ee2b207888653021158039c167f`, то же число,
что записал `review-3.md`).

| # | Подделка | Ожидание | Факт |
|---|---|---|---|
| A | `ROADMAP.md` «называет все 17 границ» → 18, прочее не тронуто | EXIT=1 | **EXIT=1**: «`ROADMAP.md`: заявлено 18, на самом деле 17 (`coverageLimits`)» |
| B | **согласованная подделка всех семи прозаических привязок** (`ROADMAP.md`, `evidence/coverage.md`, `README.md` ×2, `BRIEF.md` ×2, `tools/README.md`) → 18 **плюс** восемнадцатая запись в `coverageLimits`; разделов в файле по-прежнему 17 | EXIT=1 | **EXIT=1**: «`.pipeline/inventory.json`: `inventory.coverageLimits` — длина против разделов `evidence/coverage.md` = 18, пересчёт даёт 17» |
| C | заголовок `## 17.` в самом `coverage.md` понижен до `### 17.`; реестр и проза не тронуты | EXIT=1 | **EXIT=1**: «длина против разделов = 17, пересчёт даёт 16» |

Вариант B — тот, ради которого правило заведено: подделать согласованно всю
прозу и реестр можно, но число берётся из **структуры** файла, и подделка
падает на самопроверке. Вариант C показывает симметрию: правка структуры
без правки реестра тоже красная. Контрольный зелёный прогон после откатов —
197 в 26, EXIT=0.

---

## 3. Смысловые гейты

| # | Гейт | Результат |
|---|---|---|
| 1 | Валидаторы | **зелёный**, семь прогонов, все 0 (§1) |
| 2 | Браузерные тесты пакета | **не применим**: скрипта `test` в `package.json` нет; шаг не даёт ни разметки, ни CSS |
| 3 | Ревью закрыто | **зелёный**: три итерации ревью, две итерации правок, `review-3.md` — «принять с замечаниями», major 0 |
| 4 | Реестр сходится | **зелёный**. `coverageLimits` — 17 записей, id `CL-1`…`CL-17` подряд без пропусков; разделов `## N.` в `evidence/coverage.md` — 17; `capturedCss` 18, `capturedMetadata` 18, `capturedCssFull` 2, `pageNote` у страницы `0:1` на месте. Единственный `closedBy: null` — `CL-12` (tone of voice), и это заявленное «не закрывается», а не пропуск |
| 5 | Evidence на месте | **зелёный**: у каждой границы названы файл, node id или команда; новых снимков шаг не делал и не заявлял |
| 6 | Копируемость | **не применим**: шаг не производит разметки |
| 7 | Exit criteria волны | **частично**, разбор в §5. Пункт 5 закрыт этим шагом |
| 8 | Честность статуса | **зелёный**: незакрытое названо размером, а не оговоркой — `X-31` (досъёмка семи узлов без владельца), `X-34`, `X-35`, `X-36`, Q-5 |
| 9 | Две проекции | **не применим**: машинного слоя нет, его заводит R5 |
| 10 | Правила доказаны | **не применим**: шаг правил не выводит |
| 11 | Страница из принятого | **не применим** |

---

## 4. Минор `review-3.md` — закрыт правкой, а не строкой

Находка 16: колонка владельца `X-35` называла владельцем `R5-01`, тогда как
тот же `ROADMAP.md` определяет `R5-01` как `docs/guide/composition.md`
(`:296`), его результат уже собран вне очереди (`W-07`), а планового адреса
самого `GUIDE.md` роадмап не предусматривает (`W-01`).

Владелец исправлен на **`R5-03`** — финальная редакция границ, на неё же
ссылается сам предмет шага (`evidence/coverage.md:17`). В той же ячейке
названо и отсутствие планового адреса у `GUIDE.md` (`W-01`), и почему прежнее
`R5-01` было неверно. Прогон после правки — зелёный.

Note 17 (формулировка `fix-2.md:24` сильнее факта) действий не требует:
в самом пакете сказано аккуратно, расхождение только в прозе отчёта итерации.

---

## 5. Exit criteria волны R0 — что закрыто, что нет

Волна **не закрыта**. Из семи условий закрыты шесть, седьмое частично;
но сверх условий волна требует, чтобы её шаги были приняты, а два шага
стоят `in-progress`.

| # | Условие | Состояние |
|---|---|---|
| 1 | `ui/landings.css` подключается одной строкой и отдаёт токены, шкалу и шрифт | **закрыто** (R0-01…R0-03), подтверждено копируемостью в приёмке R0-08 |
| 2 | В `ui/tokens.css` каждое значение помечено снятым или спроектированным; светлая тема целиком нормативна | **закрыто** (R0-02), `validate-normative` = 0 |
| 3 | Имена токенов светлой и тёмной тем совпадают один в один | **закрыто** (R0-02), `validate-tokens` = 0 |
| 4 | Витрина открывается по `npm run serve`; CSS витрины не пересекается с `ui/` | **закрыто**, `validate-classes` = 0: витрин с префиксом `doc-` — 3, файлов `ui/` без `doc-` — 8 |
| 5 | `evidence/coverage.md` называет все 17 границ, включая ноль веб-форматов и ноль пар тем | **закрыто этим шагом** |
| 6 | `validate-blocks --strict` проходит; `validate-counts` зелёный на всех числах сводки | **закрыто**, оба 0 |
| 7 | Досъёмка R0-00 либо закрыла девять узлов, либо назвала поимённо, что не открылось | **закрыто** (R0-00, `accept-2.md`): CSS точечно 18 из 18, сплошь 2 из 18, семь узлов опираются на запись диалогового чтения — пробел назван, а не закрыт |

**Чего не хватает для закрытия волны:**

1. **`R0-05` — `in-progress`.** `blocks/INDEX.md` и `blocks/manifest.json`
   на 12 записей заведены, **шаблона статьи о блоке нет**; сам реестр завела
   приёмка, а не шаг, и ревью у шага не было.
2. **`R0-06` — `in-progress`.** `showcase/blocks.html` и `tools/serve.mjs`
   есть, `validate-classes` зелёный, но **smoke-спеки и скрипта `test`
   в `package.json` нет** — гейт браузерных проверок пакету пока нечем
   выполнить.
3. **Открытый вопрос Q-5** (три конфликтующих источника палитры Курсов)
   и частичный Q-2 остаются на владельце; границы 9 и 13 описывают их как
   GAP. Условием выхода волны они не названы, но упираются в них волны R1–R2.
4. **`X-31` без владельца**: досъёмка семи узлов с сохранением дампа никому
   не назначена. Перенос чисел R0-07 эту дыру именует, а не закрывает.

Пункты 1 и 2 — единственное, что стоит между волной и её закрытием;
оба про недостающий результат шага, ни один не про качество R0-07.
Файл `.pipeline/wave-R0.md` не пишется: граница волны не достигнута.

---

## 6. Изменённые файлы и коммит

Шагом (итерации 1–2, вне этой приёмки):
`evidence/coverage.md` (новый), `.pipeline/inventory.json`, `ROADMAP.md`,
`BRIEF.md`, `README.md`, `tools/validate-counts.mjs`, `tools/README.md`,
`.pipeline/R0-07/` (протоколы, отчёты ревью и правок, логи).

Приёмкой: `ROADMAP.md` (статус R0-07 → `done`, владелец `X-35` → `R5-03`),
`CHANGELOG.md` (запись в `Unreleased`), `.pipeline/R0-07/accept.md`
и её логи (`accept.validate-*.log`, `accept.red-test.log`).

Ни одного байта вне `landings/`. Коммит атомарный, поимённым `git add`;
чужие изменения рабочего дерева (`career/`, `courses/`) не тронуты.
