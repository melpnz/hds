# Ревью R0-03 «Типографическая шкала» — итерация 4

Вердикт: **принять**
Режим: **инкрементальный** (проверка `fix-3.md` против `review-3.md`; diff трогает
общие токены `ui/tokens.css` и `ui/blocks/core.css`, поэтому области шкалы,
литералов и пробела гарнитуры перепроверены целиком, а не по прозе fix-3)

Находок: **blocker 0 · major 0 · minor 0 · note 1** (note относится к оболочке
витрины, старше шага и на вердикт не влияет).

Проверено: `review-3.md`, `fix-3.md`, содержимое всех одиннадцати изменённых
файлов, собственный греп по `ui/blocks/*.css`, красный тест валидатора в четырёх
вариантах с откатом, живой замер CDP на пяти ширинах на четырёх голых страницах
и четырёх витринах, все шесть валидаторов.

---

## Находки

| # | ID | Severity | Пункт | Находка | Где | Как проверить |
|---|---|---|---|---|---|---|
| 03-24 | showcase-shell | note | D.15 | На 320 `showcase/blocks.html` растягивается горизонтально на 7 px: `scrollWidth` **312** при `clientWidth` 305. Растягивает не таблица (она заперта `.doc-scroll{overflow-x:auto}`), а неразрывная строка URL в подвале стенда. Оболочка витрины, `doc-*`; строка стоит в `HEAD` и этим шагом не трогалась — `review-3` утверждала обратное («на четырёх витринах `scrollWidth === clientWidth` на всех четырёх ширинах»), и это утверждение прежнего ревью, а не утверждение пакета | `showcase/blocks.html:153` (`<span class="doc-foot-code">http://127.0.0.1:4180/showcase/blocks.html</span>`) | CDP на 320: единственный элемент с `right > clientWidth` вне `overflow`-контейнера — `SPAN.doc-foot-code`, `right` 308.7 при `w` 288.7. `git show HEAD:landings/showcase/blocks.html \| grep -n 127.0.0.1` → строка 148, то есть до R0-03. `core.html` и `primitives.html` на 320 дают 305/305 |

Находка не блокирует и в объём R0-03 не входит: типографической шкалы она не
касается, живёт в `doc-*` и принадлежит тому же семейству, что уже отложенная
`X-27` (витрина показывает свой кегль вместо кегля пакета). Просится строкой
рядом с `X-27`, владелец **R0-06**.

---

## Перепроверка находок review-3

### 03-17 (major, норматив §5) — **закрыт**

Формулировка переписана непротиворечиво во всех трёх местах, и её проверяли
не чтением, а пересчётом.

**Разведение двух случаев выдерживает проверку на всех семи литералах.**
Собственный греп `grep -n "font-size" ui/blocks/*.css` даёт 28 объявлений:
20 через `var(--core*)`, 7 литеральных вне `@media`, 1 внутри `@media`.
Литералы и их маркеры прочитаны поштучно — у каждого стоит `@snapshot`
с node id, и объявленное число совпадает с числом внутри маркера:

| Правило | Объявлено | Маркер |
|---|---|---|
| `.site-header-link` `core.css:144` | 15px | `@snapshot 10943:2457` — «ALS Hauss Medium 15/1.5» |
| `.site-header-toggle` `core.css:165` | 15px | `@snapshot 17473:826` |
| `.program-date` `core.css:575` | 120px | `@snapshot 10929:1114` — «120/1.0, трекинг −2» |
| `.glass-chip-text` `primitives.css:299` | 44px | `@snapshot 5149:6511` — «кегль 44 при интерлиньяже 1.2» |
| `.glass-chip-text-l` `primitives.css:307` | 49.897px | `@snapshot 5164:8043` — «49.897 при интерлиньяже 1.4» |
| `.card-stat` `primitives.css:503` | 128px | `@snapshot 8056:20999` — «128/1.0, трекинг −2» |
| `.marquee-item` `primitives.css:571` | 30px | `@snapshot 7880:20726` — «30/1.2» |

Решения в этих семи нет: каждый воспроизводит снятое число, и правило «литерал
остаётся под `@snapshot`» на них выполняется без исключений. Обратный случай —
семь маркеров `@normative подстановка роли` (`core.css:285, 296, 412, 448, 546,
592, 601`), и семь классов, названных `GUIDE.md:332`, совпадают с ними один
в один. Пересечения между двумя списками нет ни одного, то есть разделение
разрезает пакет полностью и без остатка.

Шорткада `font:` с кеглем в `ui/blocks/` нет (`grep -n "^\s*font\s*:"` даёт
единственное `font: inherit` в `ui/foundations.css`), так что счёт по
`font-size` полон.

**Три места сходятся.** `GUIDE.md:330-338` (7 подстановок · 7 литералов ·
1 мобильная ступень, все семь литералов названы поимённо), `ui/tokens.css:198`
(тот же перечень семи и та же единица) и `ui/tokens.css:230-235` (комментарий
мобильного медиазапроса, «ровно 1 блок из 7»), `CHANGELOG.md:288-301`.
Противоречия между ними нет; прежняя редакция названа в каждом из трёх
как ошибочная, с номером находки.

**Живой замер подтверждает все семь чисел.** Голая страница
`fix-3-probe/literals.html`, один `ui/landings.css`, `document.styleSheets.length === 1`:
15 · 15 · 120 · 44 · 49.897 · 128 · 30 на 1400 и те же числа на 320,
кроме `.program-date` — 56 px, единственная мобильная ступень, и она работает.

### 03-18 (minor, узел кегля 180) — **закрыт**

`GUIDE.md:254` — ряд дисплейных содержит `180 (14871:1968)`.
Источник сверен: `evidence/source/figma/14871-1319.md:43` — «Рукописный акцент
"habr vibe" | `14871:1968` | Permanent Marker Regular | **180** | 1.0 (180) |
**−5 px** …». Ложная оговорка «собственный узел надписи не снят» снята, и на её
месте (`GUIDE.md:266-273`) стоит объяснение, что раньше стоял id блока-чат-бота
`14871:1962`. Пересчитал ряд сам: 19 дисплейных + 6 «ниже дисплея» = 25,
node id стоит у всех двадцати пяти, «Оговорок нет ни одной» соответствует
содержимому.

### 03-19 (minor, трекинг `.program-item-title`) — **закрыт**

`blocks/program.md:145-152` — правило внесено третьей строкой в таблицу «Что
подставлено ролью»: снято «24 / 1.2 Inter Medium, **трекинг 0** (`18085:4062`)»,
отрисовано «роль h3: 24 / 28.8 / **−0.24 px**». Обещание «значение в значение»
из текста ушло, вместо него — «подставлено одно значение из четырёх».
`ui/blocks/core.css:592` несёт `@normative подстановка роли … трекинг — нет:
в узле 0, роль даёт --core-h3-ls −0.01em, то есть −0.24 px на кегле 24»,
и там же названо, что трекинг приходит с голого тега `h3`.

Замер CDP на трёх страницах и двух ширинах: `.program-item-title` =
`24px / 28.8px / ls −0.24px / w500 / Inter` — совпадает с таблицей статьи
до сотой на 320 и на 1400.

### 03-20 (minor, пробел ALS Hauss 4 → 8) — **закрыт**

Пересчитал сам тем же критерием, который теперь записан в определении:
правила `ui/blocks/`, чей `@snapshot` называет ALS Hauss — прямо снятым
начертанием либо ссылкой на блок-пробел. Восемь, поимённо:

```
core.css:142  .site-header-link              10943:2457
core.css:160  .site-header-toggle            17473:826   (ссылкой на блок-пробел)
core.css:229  .hero-meta                     10929:1115
core.css:545  .program-title                 10948:680
core.css:573  .program-date                  10929:1114
primitives.css:169  .form-field-input::placeholder  10943:2465
primitives.css:410  .product-lockup-word-type       20216:92
primitives.css:568  .marquee-item                   7880:20726
```

Список сошёлся с перечнем `GUIDE.md:1432-1449` символ в символ, включая разбор,
каким способом каждое из восьми набирается Inter (три явным правилом, два
собственным `font-family`, три наследованием). Синхронны и остальные места:
`ui/blocks/core.css:666-679` («Пять правил этого файла» + «Ещё три … в
primitives.css — всего по пакету 8»), `:700` («переопределены 3 из 8»),
`evidence/coverage-notes.md:304`, `showcase/core.html:51`,
`showcase/primitives.html:137`. Оговорка на витринах написана так, что
действует на каждую строку страницы с именем гарнитуры, а не на три выписанные.

Отдельно проверено, что восьмёрка не выдаётся за потолок: `GUIDE.md` прямо
говорит «Восемь — нижняя граница, а не потолок», и называет четыре узла,
набранные ALS Hauss целиком. Слабое место списка — `.site-header-toggle`,
попавший в него по ссылке, а не по снимку, — не сглажено: fix-3 сам завёл на это
`X-28`.

Живьём: `getComputedStyle(el).fontFamily` с подстрокой `hauss` — **0 попаданий**
на 20 комбинациях (4 голые страницы × 5 ширин) и ещё на 16 (4 витрины ×
4 ширины). Пробел действует.

### 03-21 (note, витрина без оговорки) — **закрыт**

`showcase/blocks.html:67-72` — абзац «Второй, мобильной шкалы нет, а одна
мобильная ступень есть», с числами 7 и 1 и с `.program-date` «120 px на
десктопе, 56 px до 640». Оба числа замерены живьём и сходятся.

### 03-22, 03-23 (note) — **перенесены строками, перенос проверен**

`ROADMAP.md:470` — `X-25` расширена до четырёх адресов, третий и четвёртый
названы поимённо; четвёртый (`showcase/primitives.html` про «отдельную строку
подключения») проверен чтением: абзац на месте (`primitives.html:139-142`),
а `ui/blocks/index.css:28` `@import url("primitives.css")` несёт.
`ROADMAP.md:472` — `X-27` заведена; расхождение воспроизведено собственным
замером: `.site-header-link` = **15 px** на `showcase/core.html` и **14 px**
на `showcase/index.html` на всех четырёх ширинах.

---

## Проверка четвёртого семейства измерений `validate-counts.mjs`

Прогон вырос как заявлено: **170 утверждений в 17 файлах → 187 в 23**
(`review-3.validate-counts.log` против `review-4.validate-counts.log`).
Прирост разложен: 17 новых правил (GUIDE 4 · tokens.css 4 · core.css 2 ·
CHANGELOG 2 · showcase/blocks.html 2 · showcase/core.html 1 ·
showcase/primitives.html 1 · coverage-notes 1) и 6 новых файлов под сторожем.

**Задваивания нет.** Прогнал все 17 правил отдельно: каждое даёт ровно одно
совпадение, ни одно не находит ноль (что само по себе было бы красным —
валидатор считает устаревшее правило проблемой). Два правила `ui/tokens.css:232`
стартуют на одной фразе, но вынимают из неё разные числа («ровно **1** блок
из **7**»), то есть сторожат оба числа одного предложения, а не одно дважды.
Ключей `literalFontSizes`, `mobileFontSizes`, `roleSubstitutions`,
`alsHaussRules` до этой правки в файле не было — со старыми семействами
пересечения нет.

**Красный тест — четыре прогона, каждый с откатом до байта.**

| Что подменил | Результат |
|---|---|
| `GUIDE.md`: «Литеральных кеглей … **7**» → **1** | EXIT=1, «GUIDE.md: заявлено 1, на самом деле 7 (literalFontSizes)» |
| `primitives.css:571`: `font-size: 30px` → `var(--core-body)` | EXIT=1, **5 расхождений сразу** — GUIDE, tokens.css ×2, CHANGELOG, showcase/blocks.html: «заявлено 7, на самом деле 6» |
| `core.css:642`: `font-size: 56px` → `var(--core-h1)` **и** порча маркера `@normative подстановка роли` у `.footer-corp-item` | EXIT=1, 6 расхождений: `mobileFontSizes` 1→0 в четырёх местах и `roleSubstitutions` 7→6 в GUIDE |
| `core.css:142`: «ALS Hauss Medium 15/1.5» → «Hauss-Medium-15/1.5» | EXIT=**0** — счёт не изменился |

Первые три доказывают, что измерение берётся из самого CSS, а не из другого
документа: правка кода роняет прогон во всех местах прозы разом.

Четвёртый прогон показывает границу сторожа, и она совпадает с записанным
определением: регулярка `/@snapshot[^*]*ALS Hauss/gi` регистронезависима,
поэтому маркер `.site-header-link` продолжил считаться через хвост
«см. блок «ALS HAUSS — ПРОБЕЛ ПАКЕТА»». Это не дефект: и `GUIDE.md:1433-1435`,
и `evidence/coverage-notes.md:307-310` определяют восьмёрку именно так —
«прямо снятым начертанием **либо ссылкой на блок-пробел**». Сторож меряет
ровно то, что объявлено. После отката прогон снова зелёный, 187 в 23.

Мелочь без последствий: подпись измерения говорит «без `var(--core*)`»,
а код отбрасывает любой `var(`. Сегодня разницы нет — не-ядерных переменных
в `font-size` блоков нет ни одной, — и код при этом строже подписи, а не мягче.
Находкой не считаю.

После всех четырёх откатов файлы сверены с резервными копиями:
`GUIDE.md`, `ui/tokens.css`, `ui/blocks/core.css`, `ui/blocks/primitives.css` —
идентичны; `git status` по `landings/` показывает тот же состав из 19 файлов,
что и до ревью.

---

## Проверка новой находки самого fix-3 (X-29)

Воспроизведена независимо, на своей странице
(`.pipeline/R0-03/review-4-probe/card.html`, разметка скопирована из
`blocks/card.md:158-172` буквально, один `ui/landings.css`,
`document.styleSheets.length === 1`):

| Ширина | `scrollWidth` / `clientWidth` | Ширина надписи «1500+» |
|---|---|---|
| **320** | **368 / 320** | **347.56 px**, правый край 367.56 |
| 375 | 375 / 375 | 347.56 |
| 768 | 768 / 768 | 347.56 |
| 1024 | 1024 / 1024 | 347.56 |
| 1400 | 1400 / 1400 | 347.56 |

Числа `fix-3` (368 при 320, надпись 348 px) подтвердились до десятой.
Дефект не замаскирован: он назван в `GUIDE.md` §5 внутри самого норматива
(«у остальных шести ступени нет, и это не значит, что она им не нужна»),
записан строкой `ROADMAP.md:474` `X-29` с воспроизведением, причиной и двумя
способами решения, и передан владельцу R1-03. Объём шага не расширен —
`blocks/card.md` не тронут, что для шага о шкале правильно.

На странице семи литералов тот же `.card-stat` — единственный элемент, который
растягивает страницу; остальные восемь «вылезающих» на 320 принадлежат бегущей
строке (`.marquee-group`, `.marquee-item`) и заперты `overflow-x: clip`
на `.marquee`: на 768/1024/1400 `scrollWidth === clientWidth`.

---

## Что проверено и претензий нет

- **Восемь ролей — токены, гайд и браузер сходятся.** `getComputedStyle`
  на `:root` голой страницы возвращает 24 объявленных значения (8 ролей ×
  размер + `-lh` + `-ls`) — `display` `clamp(40px, 7.5vw, 96px)` / 1.05 /
  −0.02em, `h1` `clamp(32px, 5vw, 64px)` / 1.1 / −0.02em, `h2`
  `clamp(28px, 3.125vw, 40px)` / 1.15 / −0.02em, `h3` 24 / 1.2 / −0.01em,
  `lead` 20 / 1.5 / 0, `body` 16 / 1.5 / 0, `small` 14 / 1.4 / +0.01em,
  `caption` 12 / 1.3 / +0.02em, — и таблица `GUIDE.md:301-310` повторяет их
  роль в роль. Значений сверх восьми ролей нет: `--core-h4` … `--core-h6`,
  `--core-xl`, `--core-title`, `--core-sub` резолвятся в пустую строку.
  На 320 / 768 / 1024 / 1400 объявления одни и те же — второй лестницы нет.
  Раскладка по тегам сверена живым замером голых тегов: `h1` 64/70.4/−1.28/w600,
  `h2` 40/46/−0.8/w600, `h3` 24/28.8/−0.24/w600, `h4` 20/30/w500,
  `h5` 16/24/w500, `h6` 14/19.6/+0.14/w500 — сходится с `GUIDE.md:318-324`.
- **Ось `data-play` живая.** Переключение атрибута на `<html>`:
  вес дисплея 500 · 600 · 700 · 900, вес заголовков 600 на всех четырёх
  ступенях, гарнитура заголовка Inter → Unbounded между 2 и 3, основной текст
  Inter на всех четырёх. Правки шкалы ось не задели.
- **Локальность шрифтов.** Полный сетевой лог голой страницы — 14 запросов,
  11 из них на `127.0.0.1:4180` (`ui/landings.css` и семь файлов `ui/`, плюс
  `ui/assets/fonts/inter-normal-latin.woff2` и `…-cyrillic.woff2`). Три внешних
  принадлежат локальному антивирусу (`gc.kis.v2.scr.kaspersky-labs.com`,
  инъекция браузера), а не разметке. `document.fonts` — только Inter
  и Unbounded. `--core-font-wordmark` резолвится в пустую строку: токен удалён.
- **Конфликта с `ui/state-contract.css` нет.** Настоящая табуляция клавишей Tab
  по CDP: фокус на `a.product-lockup`, `:focus-visible` истинно,
  `outline: rgb(242, 240, 235) solid 3px`, `outline-offset: 3px` — единственным
  правилом контракта состояний через запасной путь `var(--product-accent,
  var(--core-text-primary))`.
- **Overflow на 320 / 768 / 1024 / 1400 (плюс контрольные 375).** Голая
  страница футера (`probe/footer.html`) и страница шести блоков ядра
  (`probe/blocks.html`): `scrollWidth === clientWidth` на всех пяти ширинах,
  обход всех элементов через `getBoundingClientRect` и всех текстовых узлов
  через `Range` — **пустой список** на каждой из десяти комбинаций.
- **Копируемость.** У всех четырёх голых страниц (`probe/footer.html`,
  `probe/blocks.html`, `fix-3-probe/literals.html`, `review-4-probe/card.html`)
  `document.styleSheets.length === 1`, собственного CSS ни строки, разметка
  из статей.
- **Витрины.** `showcase/index|core|primitives` — `scrollWidth === clientWidth`
  на 320 / 768 / 1024 / 1400; за вьюпорт выходят только элементы бегущей строки
  и таблицы внутри `.doc-scroll{overflow-x:auto}`, страницу они не растягивают.
  Исключение — `blocks.html` на 320, находка 03-24 выше.
- **Все шесть валидаторов зелёные** (логи рядом с отчётом):

  ```
  node tools/validate-blocks.mjs --strict   EXIT=0   Записей 12: статьи, cssRoots и якоря сошлись
  node tools/validate-counts.mjs            EXIT=0   187 утверждений в 23 файлах
  node tools/validate-classes.mjs           EXIT=0   витрин 3 с префиксом doc-, файлов ui/ без doc- — 8
  node tools/validate-normative.mjs         EXIT=0   нормативных областей 3
  node tools/validate-radius.mjs            EXIT=0   лестница Р-7: 0 · 6 · 16 · пилюля
  node tools/validate-tokens.mjs            EXIT=0   сверено 77 значений, объявлено 122 имени
  ```

- **`blocks/manifest.json` не менялся** — `git status` его не показывает,
  `validate-blocks --strict` зелёный. Ни одно состояние правками fix-3
  не появилось и не пропало.

**Перенесено без перепроверки как `не затронуто изменениями`** (проверено
в review-2 и review-3, diff fix-3 этих областей не касается): правила Т-1 … Т-6
и их знаменатели; разведённые знаменатели Unbounded; диапазон дисплея 40 … 630
и доля холста; порядок «СНЯТО» перед «РЕШЕНИЕ»; изоляция оболочки витрины
от `data-play`; осознанно некруглые 49.897, 110.883, 121.972 под маркерами;
`ui/assets/README.md` против содержимого диска; `--concept-play-step`
с пометкой МЕТАДАННЫЕ; `--core-h2` с двумя живыми пользователями;
`font-size` внутри `@media` во всём `ui/` — ровно одно вхождение.

---

## Полный чеклист R0-03 — финальный проход

| Пункт | Итог |
|---|---|
| **A.1** геометрия против снятого | проходит: семь литералов воспроизводят числа своих `@snapshot`; семь подстановок несут `@normative` со снятым числом рядом; отрисованное сверено с обоими списками замером CDP |
| **A.2** типографика против источника | проходит: ряд из 25 снятых кеглей с node id у каждого, последний пробел (180) закрыт `14871:1968` |
| **A.3** скриншоты | **не выполнялся**: кропов 320/1400 в `.pipeline/R0-03/` нет и не было ни в одной итерации; шаг о шкале сверялся числами `getComputedStyle`, а не глазомером. Честнее сказать это, чем поставить галочку |
| **A.4** состав | вне объёма шага (шкала, не композиция блока) |
| **A.5** иерархия источников | проходит: расхождения с Figma записаны нормативом либо пробелом (§14 п. 14), выбора Figma поверх runtime без GAP нет |
| **B.6** копируемость | проходит: четыре голые страницы, `styleSheets.length === 1` |
| **B.7** литералы вместо токенов | проходит **и теперь сторожится**: семь литеральных кеглей названы поимённо в трёх местах, каждый под `@snapshot` с node id, число считает валидатор по CSS |
| **B.8** протечка витрины | проходит: `validate-classes` зелёный, `doc-*` в `ui/` не встречается |
| **B.9** локальные ассеты | проходит: 11 из 14 запросов на `127.0.0.1`, остальные три — инъекция антивируса |
| **B.10** manifest | проходит: `validate-blocks --strict`, 12 записей |
| **C.11–C.14** состояния | вне объёма шага; контракт фокуса не задет — проверено табуляцией |
| **D.15** responsive | проходит для `ui/`: голые страницы футера и шести блоков чисты на пяти ширинах. Два известных исключения записаны строками роадмапа, а не скрыты: `X-29` (`.card-stat` на 320) и note 03-24 (подвал стенда) |
| **D.16** длинный текст | проходит: `.footer-social-display` в списке переноса, длинное слово на 768 не выносит страницу |
| **D.17** брейкпоинты | проходит: единственный `@media (max-width: 640px)` переключает `.program-date` 120 → 56, замерено |
| **E.18** структура | проходит: §5 и §14 п. 14 без заглушек |
| **E.19** источник у каждого факта | проходит: последние два неисточниковых утверждения (180 и «такой блок один») закрыты, четыре числа переведены из прозы в машинную проверку |
| **E.20** назначение | проходит: роли разведены по назначению (Display / H1 / H2 / H3), раскладка по тегам объяснена |
| **F, G, H, I** | вне объёма шага |

**Годится ли шаг для `guide-accept`: да.** Blocker и major нет, minor нет.
Три вещи, которые приёмке стоит знать и которые шаг честно о себе говорит:
`.pipeline/R0-03/capture.md` отсутствует четвёртое ревью подряд (протокол
съёмки восстановить нечем, сверка шла по маркерам, evidence и живому замеру);
кропов витрины 320/1400 у шага нет; в роадмапе шаг оставляет четыре живые
строки — `X-25` (расширена), `X-27`, `X-28`, `X-29`, — и все четыре
воспроизводимы по описанию.

---

## Не проверено

- **Скриншотов не снимал** — все утверждения отчёта опираются на
  `getComputedStyle`, `scrollWidth`, обход геометрии через
  `getBoundingClientRect`/`Range` и чтение файлов.
- **За новыми снимками в Figma и на прод не ходил** — источник ревью
  evidence на диске (`evidence/source/figma/14871-1319.md`, `18085-3890.md`).
- **`.product-lockup-word-type` живьём по-прежнему не измерен** — класса нет
  ни на одной витрине и ни на одной пробной странице; его участие в восьмёрке
  ALS Hauss проверено чтением `primitives.css:410-415` и грепом.
- **Светлая тема не мерилась** — типографика от темы не зависит ни одним
  объявлением, правки fix-3 этого не изменили.
- **`.pipeline/R0-03/capture.md` нет** — см. выше.

## Служебное

Браузер поднят одним процессом (порт 9451, профиль `r4-profile` в скретчпаде)
и погашен по PID своего процесса через `Win32_Process` с фильтром по строке
запуска; `taskkill /IM chrome.exe` не применялся. Локальный сервер
`tools/serve.mjs` (порт 4180) погашен тем же способом — вместе с ним закрылись
два процесса `serve.mjs`, оставшиеся от прежних итераций. После прогона порты
9451 и 4180 свободны, процессов с профилем `r4-profile` нет.

Артефакты ревью рядом с отчётом: `review-4.probe-pages.json` (4 страницы ×
5 ширин: прокрутка, обход элементов и текстовых узлов, гарнитуры, кегли),
`review-4.probe-scale.json` (роли на четырёх ширинах, теги, ось игривости,
фокус, витрины), `review-4-probe/card.html`, шесть логов
`review-4.validate-*.log`.
