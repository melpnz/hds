# FormField · Поле формы

| | |
|---|---|
| **Категория** | forms |
| **Корневой класс** | `form-field` |
| **CSS** | `ui/blocks/primitives.css`, раздел 2 |
| **Живая реализация** | `showcase/primitives.html#form-field` |
| **Snapshot** | форма и заливка — 7 узлов; состояний снято 0 из 4 |

## Когда использовать

Используйте поле формы, когда лендинг собирает контакт. Это не редкая
секция: **форма есть у 11 узлов из 18** — по покрытию это четвёртый блок
пакета после hero, шапки и футера.

Из измеренного видно две сборки, и обе повторяются:

- **подписка** — одно поле и кнопка рядом (`6509:20474`, `17473:826`,
  `9519:2095`, `10929:505`, `7869:20760`);
- **заявка** — от четырёх до пяти полей столбиком (`20216:120` — 5,
  `15673:318` — 5, `19319:7049` — 4, плюс форма в `1324:7793`).

Используйте `.form-field-box`, когда нужно согласие: чекбокс встретился
в двух узлах — `20775:861` и `1324:7793` (18 вхождений чекбоксов
и радиокнопок 20 × 20).

## Когда не использовать

Не собирайте из полей поиск или фильтр: ни одного поискового поля
в 18 макетах нет, и форма этого примитива описывает только сбор контакта.

## Примеры

| Узел | Радиус | Заливка | Обводка | Высота |
|---|---|---|---|---|
| `10943:2464` | **16** | `#222222` | нет | не снята |
| `9519:2169` | **8** | `#333333` | нет | не снята |
| `6509:20509` | **0** | `#4b5558` | `2px solid #4b5558` | не снята |
| `7885:21625` | **40** | `#fffdf5` | `3px solid #524b3d` | **68** |
| `18085:3953` | **50** → пилюля | `#ffffff` | нет | **60** |
| `I1324:8108;1311:6801` | нет `rounded-*` | не снята | `border-2` | **40** |
| `20775:861` | **6**, из ДС Хабра | не снята | не снята | не снята |
| `I1324:8114;1311:6957` | **10** (круг) | не снята | `1px solid #acb8bf` / `2px solid #5a6c7c` | 20 × 20 |

Поле `I1324:8108;1311:6801` — инстанс `ui/input` дизайн-системы Хабра.
Радиуса у него нет вовсе, есть `border-2` и `h-[40px]`. Это один
из трёх узлов участка, где интерфейс строится на прямых углах.

## Как работает

Поле — `<input>` с плейсхолдером. **Отдельной подписи над полем
в измеренном материале нет ни у одного из 11 узлов с формой**: имя поля
несёт плейсхолдер. Это факт об источнике, а не рекомендация — доступность
такой формы держится на `aria-label`, и его в разметке ниже пришлось
дописать, потому что снять было не с чего.

Кнопка отправки в сборке «подписка» стоит в одну строку с полем
(`18085:3953…3959` — три поля и кнопка подряд). Зазор между ними
не измерен ни разу.

## Название и текст

Плейсхолдер набран прописными с положительным трекингом в трёх узлах
из четырёх измеренных:

| Узел | Гарнитура | Кегль / интерлиньяж | Трекинг | Цвет |
|---|---|---|---|---|
| `10943:2465` | ALS Hauss Medium | 18 / 1.0 | **+0.5 px**, uppercase | `#999999` |
| `9519:2170` | ALS Hauss Medium | 16 / 1.0 | **+0.5 px**, uppercase | `#999999` |
| `7885:21626` | ALS Hauss Bold | 16 / 1.0 | **+0.5 px**, uppercase | `rgba(91,85,72,0.6)` |
| `6509:20510` | Inter Medium | 18 / 1.4 | 0 | `#a0a6a7` |
| `18085:3954` | Inter Medium | 18 / 1.3 | 0 | `rgba(0,0,0,0.7)` |

Цвет плейсхолдера `#999999` совпал ровно дважды (`10943:2465`,
`9519:2170`) — оба узла карьерной линии. В остальных трёх он свой.

**Подпись под формой** — обязательный сосед поля: она есть во всех четырёх
узлах, где форма снята CSS, и всегда несёт ссылку на согласие.

| Узел | Кегль / интерлиньяж | Цвет | Ссылка |
|---|---|---|---|
| `10943:2468` | 12 / 1.5, +0.5 px, uppercase | `#777777` | `#8e5eed` |
| `9519:2173` | 12 / 1.5, +0.5 px, uppercase | `#333333` | `#6742d7` |
| `7885:21629` | 12 / 1.5, +0.5 px, uppercase | `#524b3d` | `#0077ff` |
| `6509:20515` | 14 / 1.6 | `#a0a6a7` | `#f2612c` |

Три узла из четырёх — 12/1.5 с трекингом +0.5 и прописными. Это самое
устойчивое типографическое совпадение во всём участке формы.

## Размер и расположение

Измеренные высоты поля: **40** (`I1324:8108;1311:6801`), **60**
(`18085:3953`), **68** (`7885:21625`). Три значения на три узла —
общей высоты в источнике нет.

**Горизонтальный внутренний отступ поля не измерен ни в одном узле.**
`0 24px` в `.form-field-input` — норматив.

Чекбокс и радиокнопка — 20 × 20 (`1324:7793`, 18 вхождений).

## Управление клавиатурой

Ничего не снято. Ни порядка обхода, ни поведения Enter, ни ловушки фокуса
в источниках нет: Figma отдаёт кадр, а не поведение. Разметка ниже даёт
нативный порядок обхода `<form>` — это свойство HTML, а не описание
продукта.

## Валидация

**Ни одного состояния ошибки в 18 макетах нет.** Ни `invalid`, ни текста
ошибки, ни рамки-предупреждения. Это самая крупная дыра примитива: форму
без валидации нельзя сдавать в работу, а взять её описание неоткуда.

Правило: состояние ошибки дописывается в `ui/state-contract.css` (шаг
R0-08) под маркером `@normative` и в этой статье появится не раньше, чем
там окажется. Дорисовать его здесь значило бы выдать проект за снимок.

## Токены

| Переменная | Значение | Статус |
|---|---|---|
| `--core-bg-field` | `#4b5558` | снято, `6509:20474` |
| `--core-radius-s` | 8 | снято, `9519:2095` |
| `--core-radius-control` | 6 | снято, `20775:861` — импорт ДС Хабра |
| `--core-radius-l` | 40 | снято, `7885:21625` |
| `--core-radius-none` | 0 | снято, `6509:20509` |
| `--core-radius-pill` | 999px | **норматив** — 50 при высоте 60 обрезается браузером |
| `--core-text-secondary` | `#8f8f8f` | снято, `17473:869` |
| `--core-caption` | 12 | **норматив** — шкала спроектирована; опора 12/1.5 в трёх узлах |

## Доступность

`aria-label` в разметке ниже **дописан**: подписи над полем в источнике
нет, а поле без доступного имени нечитаемо экранным диктором. Это
единственное место статьи, где разметка идёт дальше снятого, и оно
названо.

## Разметка

```html
<form class="form-field-row">
  <label class="form-field">
    <input class="form-field-input form-field-r8" type="email"
           placeholder="ваша почта" aria-label="Ваша почта">
  </label>
  <button type="submit" class="button button-r50 button-caps">Подписаться</button>
</form>

<p class="form-field-hint">Нажимая кнопку, вы соглашаетесь с политикой обработки данных</p>

<label class="form-field-row">
  <input class="form-field-box" type="checkbox">
  <span>Согласен получать письма</span>
</label>
```

## Ограничения

- **Состояний нет ни одного** — focus-visible, invalid, disabled, filled.
  Чтобы закрыть, нужна съёмка живой формы на продакшене; Figma этого
  не отдаёт.
- **Подписи над полем нет ни в одном из 11 узлов с формой.** Чтобы
  подтвердить обратное, нужен `get_design_context` по формам семи узлов,
  где форма опознана только рендером: `20572:37377` (две формы),
  `16418:3180`, `18085:3890`, `9588:1223`, `1284:3362`, `17473:826`,
  `6509:20474`.
- **Горизонтальный отступ и ширина поля не измерены нигде.**
- **Зазор между полем и кнопкой не измерен нигде** — снято только их
  соседство.
- **Внешний вид чекбокса во включённом состоянии не снят**: измерена
  только выключенная граница (`1px solid #acb8bf` на белом,
  `2px solid #5a6c7c`).

## Источники

- Figma `02_Landings`, fileKey `eSJbNN1A788KEHaSBr2AfM1u`: `20216:120`,
  `15673:318`, `19319:7049`, `17473:826`, `20775:861`, `9588:1223`,
  `10943:2464`, `10943:2465`, `10943:2468`, `9519:2169`, `9519:2170`,
  `9519:2173`, `6509:20509`, `6509:20510`, `6509:20515`, `7885:21625`,
  `7885:21626`, `7885:21629`, `18085:3953`, `18085:3954`,
  `I1324:8108;1311:6801`, `I1324:8114;1311:6957;1311:6948`
- `evidence/source/figma/10929-505.md`, `9519-2095.md`, `6509-20474.md`,
  `7869-20760.md`, `18085-3890.md`, `1324-7793.md`
- `.audits/landings-2026-09-08-figma-landings.md` §5 — покрытие 11/18
- `.pipeline/inventory.json`, запись `form-field`
