# HABR Storybook — catalog

Иерархия сохранена как в Storybook. Не переработана.

`https://mr-7288-f.habr.habratest.net/storybook/` · Storybook 10.3.5 · 86 записей (73 stories, 13 docs) · 13 компонентов

Обозначения: `N props` — число controls; `rendered` — есть локальный snapshot DOM; `captured` — есть локальная docs-страница; `src` — сохранён исходник story.

---

## Block
  props (13):
    - `tag` :text  (default 'section')  [props]
    - `title` :text  [props]
    - `titleTag` :text  (default 'h2')  [props]
    - `headerVariant` :select = borderless | borderless-large | null  [props]
    - `titleVariant` :select = large | null  [props]
    - `bodyVariant` :select = balanced | condensed | condensed-slim | stucked | stucked-reversed | island | equal | no-padding | null  [props]
    - `spacing` :select = top | bottom | around | none  (default 'bottom')  [props]
    - `variant` :select = border-bottom | null  [props]
    - `block-settings` :object  [slots]
    - `header-aside` :object  [slots]
    - `body` :object  [slots]
    - `default` :object  [slots]
    - `footer` :object  [slots]
  - `Docs` — `block--docs`  · docs · captured 24662B
  - `Minimal` — `block--minimal`  · rendered 132B · src
  - `Full` — `block--full`  · rendered 412B · src
  - `Condensed H 3` — `block--condensed-h-3`  · rendered 374B · src
  - `Large Article Header` — `block--large-article-header`  · rendered 399B · src
## Buttons
  - **ButtonBase**
    props (9):
      - `label` :text  [slots]  — Содержимое default slot.
      - `styleType` :select = solid | transparent | text  [props]  — Визуальный вариант базовой кнопки.
      - `styleSize` :select = small | middle | large  [props]  — Размер базовой кнопки.
      - `disabled` :boolean  [props]  — Отключает кнопку и блокирует click.
      - `isLoading` :boolean  [props]  — Показывает loading-состояние кнопки.
      - `as` :select = button | a  [props]  — HTML-тег корневого элемента.
      - `href` :text  [attrs]  — Атрибут ссылки для режима as="a".
      - `click` :object  [events]  — Эмитится при клике по активной кнопке.
      - `default` :object  [slots]
    - `Docs` — `buttons-buttonbase--docs`  · docs · captured 31250B
    - `Default` — `buttons-buttonbase--default`  · rendered 88B · src
    - `Transparent` — `buttons-buttonbase--transparent`  · rendered 102B · src
    - `Text` — `buttons-buttonbase--text`  · rendered 93B · src
    - `Disabled` — `buttons-buttonbase--disabled`  · rendered 122B · src
    - `Loading` — `buttons-buttonbase--loading`  · rendered 104B · src
    - `As Link` — `buttons-buttonbase--as-link`  · rendered 110B · src
    - `Variants` — `buttons-buttonbase--variants`  · rendered 1243B · src
  - **Button**
    props (10):
      - `label` :text  [slots]  — Содержимое default slot.
      - `styleType` :select = solid | transparent | text  [props]  — Визуальный вариант кнопки.
      - `styleSize` :select = small | middle | large  [props]  — Размер кнопки.
      - `color` :select = horizon | fuzzy-wuzzy-brown | christi | desert-storm  [props]  — Цветовая схема кнопки.
      - `disabled` :boolean  [props]  — Отключает кнопку и блокирует click в ButtonBase.
      - `isLoading` :boolean  [props]  — Показывает loading-состояние кнопки.
      - `as` :select = button | a  [props]  — HTML-тег корневого элемента.
      - `href` :text  [attrs]  — Атрибут ссылки для режима as="a".
      - `click` :object  [events]  — Эмитится при клике по активной кнопке.
      - `default` :object  [slots]
    - `Docs` — `buttons-button--docs`  · docs · captured 33460B
    - `Default` — `buttons-button--default`  · rendered 112B · src
    - `Transparent` — `buttons-button--transparent`  · rendered 126B · src
    - `Text` — `buttons-button--text`  · rendered 117B · src
    - `Disabled` — `buttons-button--disabled`  · rendered 146B · src
    - `Loading` — `buttons-button--loading`  · rendered 128B · src
    - `As Link` — `buttons-button--as-link`  · rendered 134B · src
    - `Variants` — `buttons-button--variants`  · rendered 2354B · src
## Dialog
  - `Docs` — `dialog--docs`  · docs · captured 37953B
  - `Minimal` — `dialog--minimal`  · 12 props · rendered 878B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `With Cover Image` — `dialog--with-cover-image`  · 12 props · rendered 1783B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `With Default Action` — `dialog--with-default-action`  · 12 props · rendered 1081B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `With Custom Action` — `dialog--with-custom-action`  · 12 props · rendered 1098B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `Disabled Action` — `dialog--disabled-action`  · 12 props · rendered 1165B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `Loading` — `dialog--loading`  · 12 props · rendered 1157B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `With Custom Close Button` — `dialog--with-custom-close-button`  · 12 props · rendered 1139B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `With Custom Close Button Slot` — `dialog--with-custom-close-button-slot`  · 12 props · rendered 1138B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
  - `Scrollable Body` — `dialog--scrollable-body`  · 12 props · rendered 3354B · src
      - `drawerInitialHeight` :number  [props]  — Начальная высота BottomDrawer.
      - `title` :text  [props]  — Текст заголовка диалога.
      - `coverImage` :object  [props]  — Настройки изображения для верхней обложки диалога.
      - `modalWidth` :text  [props]  — Ширина ModalWindow на tablet и desktop.
      - `actionButtonLabel` :text  [props]  — Текст дефолтной кнопки действия. Если не передан, дефолтная кнопка действия не отрисовывается.
      - `closeButtonLabel` :text  [props]  — Текст дефолтной кнопки закрытия.
      - `disabled` :boolean  (default false)  [props]  — Блокирует дефолтную кнопку действия. Кнопка закрытия остаётся доступной.
      - `loading` :boolean  (default false)  [props]  — Показывает загрузку на дефолтной кнопке действия и блокирует закрытие диалога кнопкой, оверлеем и клавишей Esc
      - `action` :object  [events]
      - `close` :object  [events]  — Эмитится при нажатии на кнопку закрытия или при закрытии оверлея.
      - `cover-image` :object  [slots]
      - `body` :object  [slots]
## Dropdowns
  - **Dropdown**
    props (11):
      - `align` :select = left | center | right  [props]  — Выравнивание body относительно head.
      - `bodyItems` :object  [slots]  — Пункты в body slot.
      - `bodyOffset` :number  [props]  — Горизонтальный отступ body в пикселях.
      - `bodyTitle` :text  [slots]  — Заголовок содержимого body slot.
      - `headLabel` :text  [slots]  — Содержимое head slot.
      - `opened` :boolean  [story]  — Story-only состояние: открывает dropdown через публичный ref-метод open().
      - `variant` :select = default | inline | noBodyPadding  [props]  — Вариант отображения body.
      - `head` :other  [slots]
      - `body` :other  [slots]
      - `close` :other  [expose]
      - `open` :other  [expose]
    - `Docs` — `dropdowns-dropdown--docs`  · docs · captured 44589B
    - `Default` — `dropdowns-dropdown--default`  · rendered 354B · src
    - `Opened` — `dropdowns-dropdown--opened`  · rendered 939B · src
    - `Right Aligned` — `dropdowns-dropdown--right-aligned`  · rendered 934B · src
    - `Center Aligned` — `dropdowns-dropdown--center-aligned`  · rendered 904B · src
    - `Inline` — `dropdowns-dropdown--inline`  · rendered 893B · src
    - `No Body Padding` — `dropdowns-dropdown--no-body-padding`  · rendered 930B · src
    - `Variants` — `dropdowns-dropdown--variants`  · rendered 5056B · src
## Hints
  - **BaseHint**
    props (16):
      - `activatorLabel` :text  [slots]  — Текст activator slot.
      - `disableTeleport` :boolean  [props]  — Оставляет popover в локальном дереве вместо Teleport.
      - `hintText` :text  (default '')  [props]  — HTML-строка для default-содержимого, если slot не передан.
      - `linkHref` :text  [story]  — Ссылка для story-only демонстрации slot `link`.
      - `linkLabel` :text  [story]  — Текст для story-only демонстрации slot `link`.
      - `observeShift` :boolean  [props]  — Включает отслеживание layout shift в floating-ui.
      - `offset` :object  (default {
  mainAxis: 8,
  crossAxis: 0
})  [props]  — Отступы floating-ui по осям.
      - `placement` :select = top-start | top-end | bottom-start | bottom-end  (default 'bottom-start')  [props]  — Позиция hint относительно activator.
      - `preventClickOutside` :boolean  (default false)  [props]  — Не закрывает подсказку по клику вне popover.
      - `showArrow` :boolean  (default true)  [props]  — Показывает декоративную стрелку подсказки.
      - `visible` :boolean  [props]  — Контролируемая видимость подсказки. Если не передан, BaseHint управляет открытием сам по клику на activator.
      - `update:visible` :object  [events]  — Эмитится только в controlled-режиме, когда компонент просит внешнее состояние изменить видимость подсказки.
      - `interaction` :object  [events]  — Эмитится при закрытии подсказки: по кнопке, клику вне или по Escape.
      - `activator` :object  [slots]
      - `default` :object  [slots]
      - `link` :object  [slots]
    - `Docs` — `hints-basehint--docs`  · docs · captured 31184B
    - `Default` — `hints-basehint--default`  · rendered 454B · src
    - `With Link` — `hints-basehint--with-link`  · rendered 454B · src
    - `Without Arrow` — `hints-basehint--without-arrow`  · rendered 454B · src
    - `Controlled` — `hints-basehint--controlled`  · rendered 1583B · src
  - **InformerHint**
    props (7):
      - `buttonStyle` :object  (default {
  bottom: '-4px',
  right: '-7px'
})  [props]  — Позиционирование горячей точки относительно контейнера.
      - `hintWidth` :text  (default '340px')  [props]  — Ширина контейнера подсказки.
      - `offset` :object  (default {
  mainAxis: 0,
  crossAxis: 0
})  [props]  — Отступы BasePopover.
      - `visible` :boolean  (default false)  [props]  — Начальное состояние видимости hint в story preview.
      - `close` :object  [events]  — Эмитится при закрытии hint.
      - `update:visible` :object  [events]  — Эмитится при открытии и закрытии hint.
      - `default` :object  [slots]
    - `Docs` — `hints-informerhint--docs`  · docs · captured 25104B
    - `Default` — `hints-informerhint--default`  · rendered 1648B · src
    - `Narrow` — `hints-informerhint--narrow`  · rendered 1648B · src
    - `Custom Content` — `hints-informerhint--custom-content`  · rendered 1687B · src
  - **RestrictionHint**
    props (9):
      - `anchorLabel` :text  [story]  — Подпись кнопки, выступающей target-элементом в story.
      - `anchorNote` :text  [story]  — Контекст рядом с target-кнопкой в story.
      - `eventLabel` :text  [props]  — Имя gtag-события для клика по registration link.
      - `hasRegistrationLink` :boolean  (default true)  [props]  — Показывает ссылку регистрации перед сообщением.
      - `messageKey` :select = PROFILE_U_FOLLOW_HINT | PACK_LOGIN_TO_BOOKMARK | PUBLICATION_FAVORITE_HINT | SUBSCRIPTION_HUB_HINT  [props]  — Ключ локализованного текста ограничения.
      - `offset` :object  (default {
  mainAxis: 0,
  crossAxis: -34
})  [props]  — Смещение popover.
      - `placement` :select = top-start | bottom-start  (default 'top-start')  [props]  — Позиция popover относительно target.
      - `target` :object  [props]
      - `close` :object  [events]  — Эмитится при клике вне popover или по Escape.
    - `Docs` — `hints-restrictionhint--docs`  · docs · captured 24884B
    - `Default` — `hints-restrictionhint--default`  · rendered 766B · src
    - `Bookmark Action` — `hints-restrictionhint--bookmark-action`  · rendered 711B · src
    - `Bottom Placement` — `hints-restrictionhint--bottom-placement`  · rendered 764B · src
  - **VoteHintPopup**
    props (4):
      - `anchorNote` :text  [story]  — Контекст рядом с кнопками рейтинга в preview.
      - `hint` :select = registration | chargeTooLow | isMineArticle | karmaTooLow | needPublications  [props]  — Тип подсказки для конкретного ограничения.
      - `root` :other  [props]
      - `close` :other  [events]  — Эмитится при клике вне popup или по Escape.
    - `Docs` — `hints-votehintpopup--docs`  · docs · captured 21341B
    - `Default` — `hints-votehintpopup--default`  · rendered 1021B · src
    - `Karma Too Low` — `hints-votehintpopup--karma-too-low`  · rendered 1021B · src
    - `Need Publications` — `hints-votehintpopup--need-publications`  · rendered 1021B · src
    - `Variants` — `hints-votehintpopup--variants`  · rendered 5206B · src
## Pagination
  - **PaginationButton**
    props (7):
      - `direction` :select = prev | next  [props]  — Направление навигации.
      - `id` :text  [props]  — ID корневого элемента.
      - `label` :text  [slots]  — Содержимое default slot.
      - `rel` :text  [props]  — Значение атрибута rel для активной ссылки.
      - `route` :object  (default () => null)  [props]  — Vue Router route object. Если null, кнопка рендерится как недоступная.
      - `click` :other  [events]
      - `default` :other  [slots]
    - `Docs` — `pagination-paginationbutton--docs`  · docs · captured 31197B
    - `Previous` — `pagination-paginationbutton--previous`  · rendered 202B · src
    - `Next` — `pagination-paginationbutton--next`  · rendered 200B · src
    - `Disabled Previous` — `pagination-paginationbutton--disabled-previous`  · rendered 148B · src
    - `Disabled Next` — `pagination-paginationbutton--disabled-next`  · rendered 146B · src
    - `No Follow` — `pagination-paginationbutton--no-follow`  · rendered 206B · src
    - `Variants` — `pagination-paginationbutton--variants`  · rendered 1038B · src
  - **Pagination**
    props (9):
      - `currentPage` :number  [props]  — Текущая страница. Если не передана, компонент читает pageNum из текущего роута.
      - `local` :boolean  [props]  — Использует query-параметр вместо params.pageNum.
      - `localParamName` :text  (default 'page')  [props]  — Имя query-параметра для локальной пагинации.
      - `pagePadding` :number  (default 2)  [props]  — Количество соседних страниц на desktop.
      - `pagePaddingMobile` :number  (default 1)  [props]  — Количество соседних страниц на mobile.
      - `routeName` :text  [props]  — Имя route для глобальной пагинации с params.pageNum.
      - `skipNavigation` :boolean  [props]  — Эмитит update:currentPage без перехода по route.
      - `totalPages` :number  [props]  — Общее количество страниц. При значении меньше 2 пагинация скрыта.
      - `update:currentPage` :object  [events]
    - `Docs` — `pagination-pagination--docs`  · docs · captured 55908B
    - `Middle With Ellipses` — `pagination-pagination--middle-with-ellipses`  · rendered 1808B · src
    - `First Page` — `pagination-pagination--first-page`  · rendered 1404B · src
    - `Last Page` — `pagination-pagination--last-page`  · rendered 1404B · src
    - `Few Pages` — `pagination-pagination--few-pages`  · rendered 1304B · src
    - `Hidden Single Page` — `pagination-pagination--hidden-single-page`  · rendered 98B · src
    - `Local Mode` — `pagination-pagination--local-mode`  · rendered 1644B · src
    - `Custom Local Param` — `pagination-pagination--custom-local-param`  · rendered 1614B · src
    - `Mobile` — `pagination-pagination--mobile`  · rendered 1808B · src
    - `Variants` — `pagination-pagination--variants`  · rendered 8287B · src
## Popovers
  - **BasePopover**
    - `Docs` — `popovers-basepopover--docs`  · docs · captured 43114B
    - `Default` — `popovers-basepopover--default`  · 10 props · rendered 742B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Common` — `popovers-basepopover--common`  · 10 props · rendered 776B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Dropdown` — `popovers-basepopover--dropdown`  · 10 props · rendered 734B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Hub` — `popovers-basepopover--hub`  · 10 props · rendered 767B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Tooltip` — `popovers-basepopover--tooltip`  · 10 props · rendered 644B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Composed Content` — `popovers-basepopover--composed-content`  · 10 props · rendered 1108B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `With Custom Arrow` — `popovers-basepopover--with-custom-arrow`  · 10 props · rendered 908B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Variants` — `popovers-basepopover--variants`  · 10 props · rendered 2487B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `root` :object  [props]
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
    - `Placements` — `popovers-basepopover--placements`  · 10 props · rendered 3058B · src
        - `contentText` :text  [slots]  — Базовый текст slot-контента в preview.
        - `observeShift` :boolean  [props]  — Передаёт `layoutShift` в `autoUpdate`, чтобы отслеживать смещение anchor-элемента.
        - `offset` :object  [props]  — Смещение относительно anchor-элемента.
        - `placement` :select = top-start | top | top-end | right-start | right | right-end | bottom-start | bottom | bottom-end | left-start | left | left-end  [props]  — Положение popover относительно anchor-элемента.
        - `triggerLabel` :text  [story]  — Текст кнопки-якоря в story.
        - `variant` :select = common | dropdown | hub | tooltip  [props]  — Опциональное встроенное оформление контейнера. Для полностью сырого режима story `Default` показывает компонен
        - `root` :object  [props]
        - `additionalMiddlewares` :object  [props]
        - `close` :object  [events]
        - `default` :object  [slots]
## Tokens
  - `Docs` — `tokens--docs`  · docs · captured 12535B
  - `Minimal` — `tokens--minimal`  · rendered 8503B · src
