# Общий roadmap гайдов

Единая очередь активных задач по пакетам. Фактический порядок исполнения задан
ниже; подробные задачи после него сгруппированы по общим работам и продуктам.
Roadmap партнёрских спецпроектов и сам незавершённый пакет перенесены в локальный
архив и больше не входят в активный план.

## Выполнено · Шапка и подвал Habr

Шапка и подвал заново собраны 17.09.2026 по реальным production DOM/CSS и
контрольным скриншотам. В каталоге закреплены отдельные адаптивные примеры:
основная шапка, вариант с рекламным инвентарём и подвал с desktop/mobile
композициями. Зафиксированы брейкпоинты и sticky-поведение.
Готовые примеры страниц теперь используют общие компоненты шапки и подвала,
поэтому их разметка не расходится между разделами витрины.

## Текущий порядок выполнения

1. **Закрывать продуктовые пробелы:** Courses → оставшиеся задачи Habr →
   Landings и его темы → Career. Срочные дефекты конкретного продукта могут
   подниматься выше этой очереди отдельным решением.
2. **После стабилизации страниц добавить visual regression.** Сначала небольшой
   локальный неблокирующий набор; эталоны утверждать только явным ревью.
3. **Архивировать исследования `00–07` по карте переноса.** Аудит завершён,
   но архив зависит от закрытия адресных продуктовых пробелов. `05–07`
   остаются источниками будущих тем единого Landings-гайда.
4. **Вводить минимальное общее ядро machine-схем последним.** Сначала должны
   стабилизироваться знания, точки входа и регламент обновления; существующие
   схемы до этого не ломать и не унифицировать формально.

## Общие задачи

### P0 · Сетки и правила построения страниц

Статус: **выполнено для четырёх пакетов**. Для Habr
зафиксированы контейнеры, поверхности, переход к двум колонкам на 1024 px,
машинный layout-контракт и проверки ключевых ширин. Для Courses закреплены
production-оболочка `.app-container`/`.app-content`, контейнер 1124/24,
одноколоночная секционная сетка, исключения персональных страниц, живой пример
и браузерные проверки шести page-pattern. Для Landings закреплены шесть
диапазонов, контейнер 1440/1340, тематическая поверхность, живой пример и
browser-проверка overflow/ширины. Для Career закреплены контейнер 1100/12,
сетка 752+24+300, переходы 768/1024, foundation-пример и browser-проверки
центрирования и overflow.

1. Для Habr, Courses, Landings и Career описать контейнеры, внешние поля,
   колонки, интервалы, поверхности, ограничения ширины и переходы между
   брейкпоинтами.
2. Отделить общие понятия (`container`, `gutter`, основная и боковая колонка,
   рабочая поверхность) от конкретных значений каждого продукта. Не создавать
   универсальную сетку ценой потери продуктовых различий.
3. Зафиксировать правила сборки страницы: порядок шапки, навигации, основного
   контента, сайдбара и подвала; условия появления, переноса и скрытия областей.
4. Убрать из эталонных страниц одноразовые inline-поправки геометрии и связать
   примеры с общими layout-компонентами и токенами соответствующего пакета.
5. Для каждого гайда сделать наглядный адаптивный пример и проверить минимум
   320, 480, 768, 1024 и Auto; для тематизируемых пакетов проверить все темы.
6. Добавить машинные контракты и автоматические проверки ширины контейнера,
   отсутствия горизонтального переполнения, расположения колонок и различия
   между фоном страницы и рабочими поверхностями.

Готово, когда по правилам каждого пакета можно собрать новую страницу без
догадок и локальных заплаток, а визуальная и автоматическая проверки ловят
нарушение сетки на ключевых ширинах.

### P0 · Регламент обновления гайдов и повышения версии

Статус: **выполнено 18.09.2026**. Создан [`MAINTENANCE.md`](MAINTENANCE.md), который
показывает все зависимые артефакты и не позволяет оставить спецификацию,
витрину, machine-данные и номер версии в разных состояниях.

1. Создать корневую инструкцию по сопровождению и определить источники истины
   для каждого пакета: что редактируется вручную, что генерируется и что нельзя
   исправлять напрямую.
2. Описать матрицу изменений `что изменилось → что ещё проверить/обновить` как
   минимум для токена, компонента, состояния, блока, страницы, ассета,
   навигации, правила композиции и границы покрытия.
3. Для каждого типа изменения перечислить зависимые слои: спецификация,
   runtime CSS/JS, standalone-пример, собранная страница, machine-каталог и
   index, viewer и поиск, evidence/provenance, README/AGENTS, проверки и
   визуальные эталоны.
4. Описать отдельный release-checklist повышения версии: выбрать тип изменения
   и уровень версии, обновить все места объявления версии, статус готовности,
   changelog/release notes, migration notes и совместимость публичных путей.
5. Сначала провести аудит фактических мест хранения версии в четырёх пакетах и
   корневой навигации; затем выбрать по одному каноническому источнику версии
   на пакет и генерировать либо валидировать остальные упоминания.
6. Добавить команды проверки рассинхронизации: каталог ↔ файлы, спецификация ↔
   пример, токены ↔ CSS, версия пакета ↔ README/index, а также битые ссылки во
   входных документах.
7. Ввести короткий обязательный pre-commit/release checklist, не заставляя
   запускать тяжёлую полную пересборку для простого текстового исправления.

Корневой `tools/validate-guides.mjs` проверяет входные ссылки, версии пакетов,
machine index/package расхождения и локальный статус review-квиза.

### P0 · Локальный квиз для точечного ревью

Статус: **рабочий локальный инструмент, обновлён 18.09.2026**. Страница
`.review-quiz/index.html` сейчас содержит только вопросы по Courses: спорные
решения по источникам и поэтапную визуальную приёмку страниц. Ответы хранятся
только в `localStorage` и копируются обычным текстом в конце списка. Весь каталог
исключён через `.gitignore`; `PUBLICATION.md` и корневой валидатор не позволяют
считать его публикуемой частью гайдов. Закрытые вопросы заменяются новой пачкой,
а принятые решения вручную переносятся в guide, machine-контракты и roadmap.

### P0 · Аудит и последующая архивация исследований `00–07`

Статус: **аудит выполнен 18.09.2026; архивация отложена до закрытия пробелов**.
Создана [`RESEARCH-MIGRATION.md`](RESEARCH-MIGRATION.md)
с судьбой каждого исследования и семейств тезисов. Корневые исследования во многом стали историческими
источниками для актуальных пакетов, но удалять их по ощущению нельзя. Сначала
нужно доказать, что полезные правила, ограничения и evidence либо перенесены в
гайды, либо явно поставлены в очередь.

1. Разбить каждое исследование на проверяемые тезисы и составить карту
   `исследование → актуальная спецификация/правило/пример/evidence` со статусами
   `перенесено`, `перенесено частично`, `устарело`, `конфликтует`, `не
   перенесено`.
2. Для каждого частичного и неперенесённого тезиса принять решение: добавить в
   актуальный гайд, завести конкретную задачу в разделе соответствующего
   продукта или документированно отклонить как устаревшее наблюдение.
3. Отдельно проверить `00-research-and-cross-product-findings.md`: актуальные
   правила маршрутизации, source authority, границы продуктов и метод работы с
   уверенностью должны жить в корневых инструкциях, а не зависеть от старого
   служебного исследования.
4. Сверить `01-habr.md`, `03-habr-career.md` и `04-habr-courses.md` с пакетами
   Habr, Career и Courses. Исследования перестают быть рабочими точками входа
   только после полной карты переноса и добавления всех обнаруженных пробелов в
   этот roadmap.
5. `02-habr-qna.md` не архивировать, пока у Q&A нет самостоятельного гайда,
   токенов, спецификаций и примеров: сейчас это единственный доступный материал
   направления.
6. Исследования `05-company-landings.md`, `06-product-landings.md` и
   `07-brand-specials.md` использовать как три источника одного гайда
   **Landings**, а не создавать три изолированные системы. Общая архитектура,
   API блоков и базовые токены должны быть едиными; корпоративный, продуктовый
   и брендовый регистры оформляются темами с собственными токенами,
   типографикой, геометрией, ассетами и допустимыми композициями.
7. Для объединённого Landings-гайда явно разделить общее и тематическое,
   добавить переключение темы в витрину и минимум по одной эталонной странице
   каждого направления. Свободный art direction брендовых спецпроектов не
   усреднять до корпоративного шаблона.
8. После переноса прогнать ссылки, machine-маршруты, примеры и визуальные
   проверки. Исследования переносить в версионируемый архив с манифестом
   происхождения; окончательное удаление допустимо только если архив и история
   Git обеспечивают восстановление, а активные инструкции на них не ссылаются.

Готово, когда у каждого существенного тезиса `00–07` есть явная судьба,
непокрытые области находятся в продуктовых разделах roadmap, Q&A не теряет
единственный источник знаний, а `05–07` представлены единым тематизируемым
Landings-гайдом.

### P1 · Понятная точка входа для ИИ и корректная маршрутизация

Статус: **выполнено 18.09.2026**. Корневые README/AGENTS задают дерево решений,
все четыре пакета имеют короткий `AGENTS.md`, а входные ссылки и версии
проверяются `node tools/validate-guides.mjs`. Ранее часть маршрутизации была описана в корневых
`README.md` и `AGENTS.md`, а пакетные инструкции устроены неодинаково. Нужен
короткий и однозначный путь от пользовательского запроса до конкретной записи
гайда без загрузки всего репозитория.

1. Перепроверить корневую разводящую инструкцию как дерево решений:
   `интерфейс сервиса / лендинг / поддержка самих гайдов` → продукт или тип
   лендинга → статус направления → точка входа выбранного пакета.
2. Зафиксировать правило уточнения контекста: ИИ спрашивает пользователя,
   какой интерфейс и для какого сервиса он собирается верстать, только если
   этого нельзя надёжно определить из запроса и доступного контекста. Если
   ответ уже известен, повторный вопрос не задаётся. Название продукта само по
   себе не должно автоматически означать интерфейс сервиса, если возможен
   лендинг этого продукта.
3. Для Habr, Career, Courses и Landings сделать короткую страницу для ИИ с
   одинаковой структурой: назначение и границы пакета, статус, источник истины,
   точный порядок чтения, где брать токены, компоненты, блоки, страницы,
   ассеты и evidence, как действовать при пробеле и какие файлы не нужно
   загружать целиком.
4. Использовать существующие `AGENTS.md` как предпочтительные пакетные точки
   входа либо дать новому файлу одну чёткую роль без дублирования. Добавить
   отсутствующую эквивалентную точку входа Habr и выровнять ссылки из корня.
5. Проверить маршрут на наборе типовых запросов: явно названный сервис,
   неоднозначная «страница Хабра», интерфейсная форма, форма на лендинге,
   продуктовый лендинг, брендовый спецпроект, Q&A и изменение самого гайда.
6. Добавить автоматическую проверку существования всех файлов и якорей,
   указанных во входных маршрутах.

Готово, когда по одному короткому входному документу ИИ выбирает правильный
пакет и минимальный набор файлов, не смешивает системы, не задаёт уже отвеченный
вопрос и явно предупреждает о неполном или исследовательском покрытии.

### P1 · Стабильные скриншотные regression-проверки

Статус: **не начато**. Структурные и браузерные проверки уже существуют, но не
сравнивают итоговый рендер с эталоном.

1. Выбрать для каждого пакета оболочку витрины, одну характерную страницу и
   несколько ключевых компонентов на desktop/mobile.
2. Для Habr снимать обе темы, для Landings — текущую тёмную; фиксировать тему,
   viewport, данные и отключённые анимации.
3. Добавить отдельную команду `test:visual`, не смешивая её с быстрым
   статическим `validate`.
4. Сначала запускать сравнение локально и неблокирующе; включать CI-gate только
   после стабилизации шрифтов и рендера на целевой ОС.
5. Обновлять эталоны только явной командой и после визуального ревью.

Готово, когда корпус проходит три последовательных запуска без случайных
диффов и ловит намеренное изменение геометрии или темы.

### P2 · Минимальное общее ядро machine-схем

Статус: **не начато**. Источник: `GUIDES-REVIEW.md`, пункт О-2.

Нужно определить небольшое версионируемое ядро, одинаковое для Habr, Career,
Courses и Landings: идентификатор и тип сущности, назначение, визуальный
контракт, разметка, состояния, правила, примеры, неизвестное и происхождение
знания. Специфичные данные пакетов остаются расширениями.

1. Описать общий контракт и правила расширения без изменения текущих JSON.
2. Добавить проверку ядра в валидаторы четырёх пакетов.
3. Подключить адаптеры к существующим схемам и измерить ошибки на полном корпусе.
4. Мигрировать только поля с однозначным соответствием.

Готово, когда все пакеты проходят текущие проверки и проверку общего ядра, а
существующие пути, поля и публичные точки входа не удалены.

### Общие ограничения

- Не превращать внутренние Webflow-источники в публичный контракт только ради
  формальной валидности ссылок.
- Не унифицировать все поля четырёх пакетов и не удалять package-specific
  расширения ради одинаковой формы JSON.
- Не переименовывать опубликованные пути без алиасов и периода совместимости.

## Courses

Статус пакета: **v1.0, пригоден к использованию; развитие продолжается**.

**С чего начать следующую сессию:** после подтверждения владельца опубликовать
подготовленные ZIP и SHA-256 в GitHub Releases и проверить скачивание в чистый
проект. Инструкции для коллег и ИИ завершены и опираются на единый
machine-readable consumer workflow. Связка
версий, manifest, changelog и compatibility matrix уже проверяется автоматически;
CI проверяет drift, чистое копирование layer, браузерный smoke и visual regression;
техническую часть дальнейшей синхронизации можно выполнять
автономно. Неоднозначные токены и публикация релиза требуют решения владельца. Подробная очередь
находится в начале [`courses/ROADMAP.md`](courses/ROADMAP.md#точка-продолжения--следующая-сессия).

### P0 · Интегрировать Courses Nuxt Kit как реализацию гайда

**В работе с 23.09.2026.** Стабильная исходная версия зафиксирована; владелец и
read-only граница оформлены. Копируемый layer теперь содержит собственные
ассеты, manifest `1.0.0`, changelog и SHA-256 для 148 файлов. Проверка обновления
работает только по явному запросу, отличает чистую копию от локально изменённой
и не скачивает файлы; manifest привязан к commit SHA содержимого. Следующий шаг
закрыт технически: release-команда создаёт чистый ZIP и SHA-256, а отдельный
minimal-consumer успешно собирается из локальной копии без сети. Осталось
опубликовать архив `v1.0.0`; далее — нейтральный provider contract и сведение
инвентарей.

Нейтральная граница provider начата 23.09.2026: index объявляет активную
реализацию, kit manifest экспортирует 72 public и 2 internal компонента, а
generated mapping подтверждает 72 прямых соответствия. Публичный API всех
компонентов (`props`, `slots`, `emits`, `models`) извлекается из `.vue` в manifest
и mapping автоматически. Guide-only записей больше нет: исторический
`RubricationBar` свёрнут в page-pattern `courses-listing`; отдельно видны три
state-gap. Нейтральное
сопоставление токенов завершено 25.09.2026: все 146 provider token связаны без
конфликтов; новые семантические значения отделены от исторического
production-слоя в `machine/semantic-tokens.json`.

Версии guide/provider теперь связаны проверяемым
`machine/compatibility.json`: Courses v1.0 совместим с
`courses-nuxt-kit@1.0.0`. Витрина читает provider mapping, адресно загружает API
открытого компонента и для всех 72 прямых соответствий переключается на реальный
Vue-preview kit. Аудит preview завершён 25.09.2026: 69 компонентов используют
`provider-primary`, три сохраняют `hybrid` из-за state-gap, статический HTML
остаётся GitHub fallback, а восемь дублей старых id заменены redirect.
Шесть page-pattern также синхронизированы с provider: их machine-записи явно
хранят оболочку и дерево прямых компонентов, а автоматическая проверка не даёт
сослаться на отсутствующий или непубличный export. Статические страницы
сохранены как evidence и GitHub fallback.

Рабочий kit в `courses/courses-nuxt-kit/` изучен и подключён как активный provider. Целевая модель:
версионируемый локально копируемый Nuxt layer является источником
Vue-компонентов, токенов, ассетов и визуала; machine-гайд отвечает за поведение,
доступность, композицию и page-pattern. Необходимо публиковать неизменяемый
release-архив, добавить manifest версии и необязательную проверку обновлений по
явному запросу. Скопированный kit не обращается к GitHub при разработке или
сборке. Также нужно добавить явный маршрут для людей и ИИ, связать версии кита и
гайда и ввести CI-проверку их manifest/token/state coverage.

Nuxt Kit считается текущим временным implementation provider, а не вечным ядром
гайда. Provider-specific API и пути хранятся в отдельном mapping; стабильные id,
поведение и page-pattern остаются нейтральными. Будущая официальная библиотека
frontend-команды подключается вторым provider, проходит coverage/API/visual
сверку и только затем заменяет активный kit без переписывания самого гайда.

Канонический Nuxt Kit изменяет и выпускает только его владелец. Копия в проекте
считается vendored/read-only layer: проектные расширения делаются снаружи, а
недостающий публичный API запрашивается у владельца и выходит новой версией.
Manifest и checksums должны выявлять локально изменённые, неподдерживаемые копии.
При этом коллеги свободно создают собственные application-компоненты рядом с
layer и собирают их из компонентов кита; ограничение действует только на файлы и
публичные exports самого Courses Kit.

Первый sync свёл 72 общие записи; provider-only и guide-only записей больше нет,
внутренними остаются два компонента. `RubricationBar` перенесён в композицию
страницы как `nav` из четырёх `Link`. Kit уже экспортирует 146 `--crs-*` в структурированный manifest;
все 146 значений сопоставлены с нейтральным контрактом без конфликтов. Подробные
этапы и критерии завершения находятся в
[`courses/ROADMAP.md`](courses/ROADMAP.md#интеграция-courses-nuxt-kit--запланировано-23092026).

### Точка остановки · 21.09.2026

Три review-квиза разобраны и внедрены. Основы, оболочка, шапки, подвал,
контролы, формы, каталог, карточки, модалки и принятые страницы собраны в живых
примерах; Carousel обновлён в компонентах и на страницах. Из production выделен
`ProfileHistory` для опыта работы и образования автора и подключён к странице
профиля. Актуальный корпус содержит 84 записи и 88 примеров; автоматические
статическая и браузерная проверки проходят.

Последняя проверка закрыла механику Carousel: AdSlot использует бесшовный цикл,
отдельные mobile-креативы и production-маркировку рекламы, контентные карусели
показывают disabled-стрелки на границах, а один общий controller работает и в
компонентах, и на собранных страницах. Исправлены также горизонтальный скролл
FilterBar и responsive-геометрия PriceSheet. `Carousel · AdSlot` и `PriceSheet`
визуально приняты владельцем 21.09.2026.

Аудит 58 `partial`-записей завершён 21.09.2026. Пятьдесят записей закрыты без
изменения визуального контракта, а последние 8 решений применены по итогам
Courses-only квиза: адаптивны DemandChart, PageToc и phone-карточки,
FeedbackForm дополнен телефоном и success-экраном, Loader анимирован,
EmptyState и Tooltip приняты как нормативные. Все 84 machine-записи имеют
статус `complete`. Дубликат `star-rounded-small` удалён; все размеры
рейтинговой звезды используют единый `star-rounded`.

Следующий порядок работ после выпуска v1.0:

1. Проверить по рабочим макетам Courses и библиотеке `education-lib`,
   действительно ли применяются цвета из групп «Предположительно используются
   только в Figma» и «Предположительно нигде не используются». Для каждого
   цвета зафиксировать Figma-файл и node/component либо отсутствие вхождений;
   после проверки сократить палитру и слой `--fig-*`, если подтверждённых
   потребителей нет.
2. Продолжать продуктовую сверку 84 записей / 88 примеров при изменениях
   production и Figma.
3. Расширять покрытие адресно, не меняя принятые контракты без новой сверки.

1. Провести продуктовую сверку 84 записей / 88 примеров viewer.
2. ~~Расширить проверки утверждений из Markdown и числовых рядов.~~
   **Выполнено 18.09.2026:** валидатор синхронизирует `content.json`, machine-правила
   `CT-1…CT-3b`, Markdown-таблицу, числовые predicates и итог 191 подписи кнопок.
3. ~~Разобрать недоснятые состояния по компонентам в следующем review-квизе.~~
   **Выполнено 18.09.2026:** закрыты решения по 12 группам компонентов;
   Breadcrumbs и HeaderDropdown сверены с production, принятые состояния
   добавлены в примеры, документацию и автоматические проверки.
4. ~~Описать вариант сетки курсов школы с отличающимся gap и скрытием карточек.~~
   **Выполнено 18.09.2026:** вариант `CardGrid / school-courses` закрепляет
   gap 16, колонки 4/3/1 и ограничение хвоста all/6/4; страница школы ссылается
   на компонент через машинный и DOM-контракт.
5. ~~Завести самостоятельную запись повторяемого блока опыта работы и образования
   профиля автора.~~ **Выполнено 21.09.2026:** production-композиция оформлена
   как `ProfileHistory`, использует `EntityLogo` и переиспользуется страницей автора.
6. ~~Доснять недостающие ассеты раздела экспертов.~~ **Выполнено 18.09.2026:**
   production-иллюстрация и sprite подключены локально, для пользователей
   применяется канонический Avatar.
7. ~~Исследовать раскладку Swiper либо оставить её явной границей покрытия.~~
   **Выполнено 18.09.2026:** Carousel сверён с Figma `15074:257173`, навигация
   использует общий IconButton, активный рекламный слайд центрируется с видимыми
   соседями, а варианты AdSlot/CourseCard/ArticleCard/ReviewCard обновлены на
   страницах; production runtime Swiper явно отмечен как внешняя граница реализации.
8. ~~Применить решения второго review-квиза.~~ **Выполнено 18.09.2026:**
   зафиксированы размеры Button и SiteHeader, вложенная mobile-навигация CatalogMenu,
   единая адаптивная версия FaqBlock, нормативное имя `pressed` у TileFilter,
   горизонтальная прокрутка рекомендаций FilterModal и общие Button в PriceSheet.
   Статус `complete` теперь автоматически запрещён при непустых `unknowns` или
   `stateCoverage.uncaptured`.

Исторический план из 95 шагов и подробные GAP остаются в
`courses/docs/history/v0.1-roadmap.md`; выполненные шаги в активный план не
возвращаются.

## Habr

Приоритет определяется тем, насколько пробел мешает собрать экран.

### P0 · Системные пробелы

| # | Задача | Источник | Объём |
|---|---|---|---|
| 1 | **Повторно разобрать и довести до приёмки страницы Article Detail, Article Comments, Search, Editor, Modal / Overlay, Settings / Forms, Admin Form и Admin List** | Текущая компонентная пересборка улучшила структуру, но визуально страницы ещё не приняты. Повторно сверить каждую с указанным production/Figma-источником на 320/480/768/1024, устранить композиционные и типографические расхождения; существующее переиспользование компонентов сохранить | L |
| 2 | Сверить шкалы `radius/*` и `iconsize/*` с production | Figma-переменные и production-замеры | S |
| 3 | Свести семь уровней типографики с токенами Figma | Figma и production | S |
| 4 | ~~Перепроверить форму аватара~~ **Выполнено 18.09.2026:** production использует скруглённый квадрат 3px, в гайде он осознанно нормализован до токена 4px | живой habr.com | S |
| 5 | Задокументировать 11 реально используемых теней production | production-корпус | S |
| 6 | ~~Причесать контентную сетку и правила построения страниц, включая 768–1023 и переход к двум колонкам на 1024~~ | **Выполнено 18.09.2026:** семь семейств страниц переведены на общую оболочку; закреплены серый фон, белые поверхности, одноколоночный режим до 1024 px, колонка 300 px на desktop, layout-контракт и браузерные проверки 320/480/768/1024/Auto | M |

### P1 · Дыры DESIGN MODE

| # | Задача | Источник | Объём |
|---|---|---|---|
| 7 | Select / Dropdown-поле | Figma `textfileds → select` | M |
| 8 | Management/table row | Figma admin и `pages.html` §09.3 | M |
| 9 | Loading / skeleton | production и Figma loading-состояния | M |
| 10 | ~~Страницы ошибок как паттерн~~ | **Выполнено 18.09.2026:** 403/404/500 и общий адаптивный каркас добавлены по Figma, остальные ошибки используют локальные SVG | M |

### P2 · Только по требованию

| # | Задача | Почему не раньше |
|---|---|---|
| 11 | Полная таксономия publication labels | Это Product Copy, а не визуальный слой |
| 12 | Datepicker / timepicker | Узкий admin-сценарий |
| 13 | Интерактивное поведение Chip | Статическая витрина не является Storybook |
| 14 | ~~Редактор статьи как непустой пример~~ | **Выполнено 18.09.2026:** полный экран 1024/768/320 перенесён из Figma; визуальная приёмка вынесена в P0 |

Открытые расхождения не усреднять: цвет ошибки формы, метрики пагинации,
система теней, Figma-only `--icon-primary-40`, Checkbox hover и форма аватара
остаются явно классифицированными до появления подтверждённого источника.

## Landings

Статус пакета: **v0.2, в разработке**.

1. По результатам аудита `05–07` разделить общую архитектуру и тематические
   правила корпоративных, продуктовых и брендовых лендингов.
2. Завершить пересборку и визуальную сверку 11 корпоративных страниц по
   фактическому составу и порядку Webflow-блоков.
3. Проверить и стабилизировать общий API, базовые токены и блоки до добавления
   новых тем.
4. Собрать продуктовую тему и одну эталонную страницу по исследованию `06`.
5. Собрать брендовую тему и одну эталонную страницу по исследованию `07`, не
   усредняя свободный art direction до корпоративного шаблона.
6. Добавить переключение тем в витрину и визуальные проверки каждой темы.
7. Затем уточнить редкие варианты навигации, таблиц, модальных сценариев,
   слайдеров и сложных Webflow-анимаций.

Уже завершены архивирование прежнего пакета, собственный reset, основа темы
«О компании», разделение библиотеки по слоям, интерактивные стенды и
схематичные прототипы 11 страниц.

## Career

### P1 · Четыре модуля без достоверного preview

Не восстанавливать DOM по одному CSS и не показывать другой компонент как
будто это нужный модуль.

| Модуль | Что уже есть | Что нужно для закрытия |
|---|---|---|
| `JournalBlockSidebar` | назначение, Storybook-имя и наблюдаемые тексты | production или Storybook snapshot: `outerHTML`, скриншот, размеры и состояния |
| `PromotionCard` | CSS и варианты `header` / `resumes` | DOM обоих вариантов, изображения, иконки и характерные ширины |
| `ResumeCard` | назначение и небольшой фрагмент CSS | DOM и скриншоты трёх Storybook-вариантов |
| `TestResultModals` | CSS содержимого и готовый базовый `Modal` | DOM и состояния `ScreeningsListModal`, `SkillsTestResultModal`, `SpecsTestResultModal` |

Предпочтительный источник: `career-web`, затем рабочий Storybook, production
`outerHTML` со скриншотом и только потом Figma. Для каждого модуля нужны
props/варианты, состояния, ассеты и доступность. Готовность означает
подтверждённый DOM, визуальный эталон, standalone preview и provenance.

### P1 · Облегчить активный machine-маршрут

Статус: **частично выполнено**. Источник: `GUIDES-REVIEW.md`, пункт C-1.

Встроенный Markdown уже заменён ссылками, реальная разметка подключена, а
provenance вынесен из основного каталога. Осталось:

1. Измерить размер и токен-бюджет адресного маршрута.
2. Вынести `legacySource` в отдельный migration/provenance-реестр.
3. Оставить совместимый указатель или адаптер на переходный период.
4. Запретить валидатором повторное встраивание Markdown и разметки.
5. Удалять legacy-поля только после проверки известных потребителей.

### P2 · Остальные неподтверждённые сценарии

- Авторизованные страницы и личный кабинет.
- Живые ошибки форм и модальные потоки.
- Раскрытые состояния `CustomSelect`, `MultiSelect` и `ContextMenu`.
- DOM тоста `notify()`.
- Соответствие имён иконок Figma и спрайта реализации.

### P2 · Кириллические legacy-пути — только при подтверждённой проблеме

Если кириллические пути действительно мешают потребителю, сначала составить
карту `старый путь → новый путь`, затем ввести латинские id и файлы с алиасами
минимум на один релизный цикл. Русские названия остаются в `title`. Без
зафиксированной несовместимости миграцию не начинать.
