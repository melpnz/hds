# Career — роадмап

Что в пакете уже закрыто, что осталось и что придётся исправить. Статусы ставит
приёмка шага; всё, что найдено по ходу и не входило в шаг, попадает сюда строкой,
а не чинится молча.

Журнал изменений — [`CHANGELOG.md`](CHANGELOG.md). Границы покрытия и их причины —
[`README.md`](README.md) и [`evidence/coverage.md`](evidence/coverage.md).

Дата сводки: **7 сентября 2026**.

---

## Где пакет сейчас

| | |
|---|---|
| Компонентов в manifest | **72**, все `complete`; записей `partial` и `planned` нет |
| Каркасных модулей R4 | 9, закрыты |
| Entity-модулей R5 | 1 из 7 семейств (`VacancyCard`) |
| Stateful-модулей R6 | 0 |
| Проверки | strict-валидатор, `sync-showcase-icons --check`, browser suite 17/17 на 375·768·1024·1440 |

---

## Волны

| Волна | Что закрывает | Статус |
|---|---|---|
| **R0** | контракт, словарь состояний, manifest, шаблон и проверки | закрыта |
| **R1** | общая модель состояний | закрыта — [`components/STATES.md`](components/STATES.md) |
| **R2** | доработка существующих базовых компонентов (A–E) | закрыта |
| **R3** | недостающие базовые компоненты, **Core Gate** | закрыта |
| **R4** | каркасные модули, **R4 Gate** | закрыта |
| **R5** | entity-модули | **в работе:** 1 из 7 |
| **R6** | stateful-продуктовые модули | не начата |

Волны не смешиваются: `R6` не начинается, пока не закрыта `R5`.

### R5 — что осталось

Приоритет и то, что мешает каждой задаче, — по evidence на 7 сентября 2026.

| Шаг | Статус | Что мешает |
|---|---|---|
| `VacancyCard` | **готово** | — |
| `QuickApply` | ждёт evidence | публичное действие «Откликнуться» видно, но форма и её состояния требуют авторизации и не сняты |
| `ResumeCard` | ждёт evidence | 0 из 3 Storybook snapshots, нет снятой DOM-разметки, controls и устойчивого CSS root |
| `SpecialistCard` | ждёт решения | сначала определить, одно ли это семейство с `ResumeCard` |
| `CompanyCard` | ждёт решения | нет отдельной канонической спецификации; сначала отделить от `CompanyRatingCard` |
| `TestCard` | ждёт evidence | 7 из 59 snapshots; сценарии owner/guest/payment требуют отдельной business-state модели |
| `SalaryReportCard`, `CourseCard`, `JournalArticleCard` | не начаты | — |

**Exit criteria R5.** У каждого семейства один канонический component API; Figma
business flags сопоставлены с domain props; карточки собираются только из
готовых primitives R2–R3.

### R6 — что предстоит

Profile sections и social connections · отзывы о компаниях и официальные ответы ·
диалоги (список, шапка, композер, вложения, типы сообщений) · сохранённые фильтры
и двухуровневая рубрикация · owner/guest flows тестов · pricing и баланс ·
продуктовые баннеры, брендированные зоны и реклама.

Этим модулям нужны **отдельные business-state диаграммы**: общий UI-словарь не
заменяет `unread`, `sent`, `failed`, `expired`, `rejected`, `archived`,
`paymentFailed`.

**Exit criteria R6.** Описаны loading, empty, error, success и unauthorized на
уровне сценария; retry и destructive actions имеют явные правила; business states
тестируются отдельно от visual interaction states; модуль не вводит новый
primitive незаметно для core library.

---

## Чего в пакете нет

Это границы, а не задолженность: исследование велось гостем по публичной части.
Пока строка отсюда не закрыта, задача в этой зоне получает от пакета верный
визуальный язык и компоненты — и **не** верную структуру экрана.

| Зона | Состояние | Чем закрывается |
|---|---|---|
| Личный кабинет и всё, что видит авторизованный пользователь | не покрыто | доступ к учётной записи |
| Полноценные формы ввода | правила `F-*` выведены из фильтров листинга, а не из форм | снять формы продукта |
| Модальные потоки | компонент есть, сценарии не наблюдались | то же |
| Загрузка и скелетоны на уровне страницы | не покрыто | то же |
| Ошибки формы в живом продукте | не покрыто | то же |
| Сценарии успеха и подтверждения | не покрыто | то же |
| Тексты ошибок, подтверждений и успеха | не покрыто | то же |
| Дашборд и рейтинг | по одной странице каждый, правила `DB-*` помечены ненадёжными | снять ещё страницы семейства |
| Главная `/` | отсутствует в таксономии страниц | снять и описать как `HOME/MARKETING-HYBRID` |
| Брендированный профиль | помечен исключением, но не описан как управляемая система | описать разрешённые зоны кастомизации |

---

## Что нужно исправить

Найдено по ходу работы и не входило в шаг, на котором нашлось. Порядок — по
убыванию отдачи.

### Мешает генерации нового кода

| # | Что | Почему это важно |
|---|---|---|
| ~~1~~ | ~~Нет точки входа для агента~~ — **исправлено.** [`AGENTS.md`](AGENTS.md) — контракт на страницу, `machine/` — слой, по которому можно генерировать: 121 токен, 72 компонента, 57 правил и 16 Decision Guides в JSON | |
| ~~2~~ | ~~Нет acceptance checklist~~ — **исправлено.** [Чеклист готовности экрана](docs/development/checklist.md); `validate-classes.mjs` принимает свою разметку | |
| 3 | **`utilities.css` — подмножество Tailwind.** Ловится проверкой `node tools/validate-classes.mjs`; она же принимает свою разметку: `node tools/validate-classes.mjs фича.html`. Осталось восстановить семь классов, которые есть в снятой разметке Career, но не попали в извлечённый CSS: `bg-blue-600`, `text-white`, `leading-4`, `focus:outline-3`, `phone:opacity-1`, `text-font-black-60`, `base-section__title--padding-medium` — значения нужно снять с продукта, а не восстанавливать по умолчаниям Tailwind | валидная утилита молча не применяется, вёрстка едет без видимой причины |
| ~4~ | **Частично.** В `machine/components.json` появилось поле `code`: имя Vue-компонента выведено из Storybook у 42 записей из 72. `importPath` и `props` остаются `null` — **нужен доступ к репозиторию `career-web`** | вёрстка выглядит верно, но в `career-web` не ложится |
| ~~5~~ | ~~Нет feature-level контракта состояний~~ — **исправлено.** [`docs/guide/feature-states.md`](docs/guide/feature-states.md): цепочка состояний, правила сборки, доступность. Помечен как нормативный контракт, а не наблюдение | |
| ~~6~~ | ~~Не определён нормативный default~~ — **исправлено.** Раздел «Нормативный default для нового кода» в [`docs/guide/coverage.md`](docs/guide/coverage.md) | |

### Машинный слой

Собран `machine/` — `index.json`, `tokens.json`, `components.json`, `rules.json`
(сборщик `tools/build-machine.mjs`, проверка `tools/validate-machine.mjs`).
Проверка на пригодность к генерации показала, что собрать экран **по одному
машинному слою пока нельзя**:

| # | Чего не хватает | Следствие |
|---|---|---|
| ~~15~~ | ~~`patterns.json` не собран~~ — **исправлено.** 8 паттернов с замеренной сеткой и адаптивом; геометрия снята в браузере и лежит в `patterns.overrides.json` | |
| ~~16~~ | ~~У девяти модулей R4 нет раздела «Разметка»~~ — **исправлено.** Разметка снята с живых примеров витрины и внесена в спецификации | |
| ~17~ | **Стало 49 из 72.** Оставшиеся 23 — компоненты, у которых разметка не снята (портальные списки, сломанные в Storybook story). Закрывается пересъёмкой, то есть пунктами 8 и «Не снято» ниже | |
| ~18~ | **Появились у 4 правил** — `SH-4`, `SH-5`, `L-1`, `C-1`, всего 9 проверок; исполняются тестом `Executable rule predicates hold`. Значения сняты замером. Остальные 53 остаются `manual`: часть правил про смысл композиции, а не про число | |
| ~~19~~ | ~~`content.json` не собран~~ — **исправлено.** 6 наблюдений с примерами и 6 явных пробелов; собирается из `content.overrides.json` | |

### Расхождения и долги внутри пакета

| # | Что | Где |
|---|---|---|
| ~~7~~ | ~~Метаданные разъезжаются~~ — **исправлено.** `validate-counts.mjs` сверяет 12 заявленных чисел с реальными; при добавлении сразу нашёл расхождение 87 против 95 | |
| ~8~ | **Инструмент готов.** `node tools/check-evidence-age.mjs` печатает возраст 16 снимков и их URL; порядок пересъёмки — в `coverage.md`. **Сама пересъёмка не сделана**: нужен доступ к продукту | |
| ~9~ | **Ждёт пересъёмки** страницы рейтингов, то есть пункта 8. Допущение помечено в самой витрине | |
| ~~10~~ | ~~Обводка `empty-star` прибита к `#929CA5`~~ — **исправлено**, переведена на `currentColor` | |
| ~~11~~ | ~~Стрелки календаря — текстовые символы~~ — **исправлено**, поставлены `arrow-back` / `arrow-forward` | |
| ~~12~~ | ~~Глиф `telegram-bot` крупнее соседей~~ — **не дефект.** Сверено с `single/social-v3.1/telegram-bot.svg`: глиф побайтово совпадает с продуктовым, он и задуман во всю плитку | |
| ~~13~~ | ~~Стенд Menu объявляет `data-state="open"`, но скрипт закрывает его~~ — **исправлено**, разметка приведена к закрытому состоянию | |
| ~~14~~ | ~~Нет проверки на обрезку оверлеев~~ — **исправлено**, тест `Overlays fit inside their specimen` в browser suite; проверен на заведомо сломанном стенде | |

### Открытые конфликты источников

Расхождения, которые не решены и **не входят в reusable-набор**. Показ — раздел
«Отложено» в [`showcase/components.html#s-deferred`](showcase/components.html#s-deferred),
разбор — `research/system-audit.md` §13.

| Конфликт | Уверенность |
|---|---|
| Какая из трёх Figma-библиотек Career — источник истины | открыт |
| Промо-состояние карточки вакансии | открыт, средняя |

Расхождение имён между двумя реализациями (`checkbox__box` ↔ `checkbox-button`)
конфликтом не является: оба набора действующие, продукт переезжает по частям.

### Не снято, блокирует UI kit

- Разметка ленты сообщений — `Messages`, `MessagesGroup`, `ConversationMessages`.
  34 story сломаны в самом Storybook Career: сборка не отдаёт два чанка
  (`-iqz-73v.js`, `-2YzXgxt.js`, оба HTTP 404). Закрывается одним снимком
  страницы `/conversations` в production.
- Разметка `ResumeCard` — по той же причине. Закрывается снимком `/resumes`.
- Существует ли Figma-файл со страничными макетами Career.

### Не снято, не блокирует

- Раскрытые состояния списков: `CustomSelect`, `MultiSelect`, `ContextMenu`
  рендерят выпадающую часть в портал — в snapshot попала только закрытая форма.
- Разметка тоста `notify()`: CSS есть, DOM нет.
- Разметка `ButtonRange`, `PromotionCard`, `JournalBlockSidebar`,
  `TestResultItem`, `TestResultAdditional` и окон результатов тестов.
- Соответствие имён иконок Figma и спрайта реализации.

### Закрытые границы, которые не являются пробелом

`Хабр Аккаунт` (`/users/sign_in`) — другая дизайн-система, у Career нет своих
экранов входа. `Хабр Курсы` и основной `habr.com` — отдельные продукты.
Смешивать их с Career нельзя, и это не задолженность пакета.

---

## Правила ведения

- Статус `done` ставит только приёмка шага.
- Соседняя проблема, найденная по ходу, уходит сюда строкой, а не чинится молча.
- Догадка вместо факта — провал шага; отсутствие раздела лучше вымысла.
- Три итерации без приёмки — статус `blocked` с причиной, шаг откладывается.
