# Icon — подготовка источников R2-01

2026-09-08. Только чтение Figma; реализация и реестры пакета не изменены. Файл `m2O8xRs2aEU8NfUpyNiAks`, страница `253:77`, component set `2099:19546`.

Подтверждены по287 компонентов в размерах20/24/32/48: всего1148. Однако имена children дают317 уникальных Type, а componentPropertyDefinitions перечисляет287 Type options. Это несогласованность источника: числовые суффиксы Edit16/20/21, Link9/11 и другие отличаются по размерам. `icons-details.json` содержит точный nodeID, исходное имя, размеры и путь оригинального SVG; nameGaps перечисляет имена без полного набора четырёх размеров. Суффиксы сохранены; одинаковой формой, семантическими различиями или взаимозаменяемостью они не объявлены. Нельзя обещать287 канонических glyph с четырьмя размерами до сопоставления этих алиасов.

Каждый компонент экспортирован отдельно через поддерживаемый `exportAsync({format:'SVG_STRING'})`. В `icon-assets/` сохранены исходные векторы, не нарисованные аналоги. Ответы API ограничены15KB данных и максимум25 вариантами; 1148 не сведены к одному условному размеру. Повторяющиеся SVG не дедуплицировались. Варианты содержат собственные viewBox и stroke; source stroke не заменён currentColor. Цветовая адаптация будет отдельным решением реализации.

`icons-overview.png`: один снимок всего набора, 1600×1144 при исходном5920×4232. Просмотрен: четыре размерные группы, контурные пиктограммы. Это обзор структуры, не доказательство различимости каждой детали в уменьшенном PNG. При использовании отдельного glyph нужно сверяться с его SVG.

`icons-context.md` сохраняет релевантные результаты get_design_context Activity32 и Point done_circle24. Начальный эксперимент figma.io.write не вернул локальный downloadable artifact; сохранение выполнено из SVG_STRING ответа, исходный файл Figma не изменён.

## Дополнение для Typography Point

Point `2085:319` содержит не instances, а frame done_circle в трёх размерах и три ellipse. Они экспортированы отдельно, поскольку принадлежность к Icons set не подтверждена. Шесть записей `pointShapes` содержат nodeID, parent, позиции и размеры; asset имеет префикс `point-`. Done_circle24: x0/y6,32: x0/y10,16: x0/y6; bullets6/10/4: y15/21/12. Это отступы внутри Point, не новое правило Icon.

## Проверка

1148 уникальных Icon nodeID; у каждого размер совпадает с XML metadata. По287вариантов каждого размера,317 уникальных исходных Type names; несогласованность имён выше остаётся открытой. Все1154 SVG (с Point) разбираются XML-парсером, суммарный объём699573 байта. `icons-asset-check.json` содержит SHA256 каждого локального файла для контроля последующих изменений; это хеш сохранённых exports, не внешняя подпись Figma.

R2-01 не реализован и не принят. Социальная библиотека страницы вне set2099:19546 не включена: она относится к отдельному направлению SocialButton и требует своего подтверждения. Production не проверялся.
