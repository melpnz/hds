# R1-03 — план клиентского оформления

До подтверждения done R1-01 и R1-02 реализация не меняется. Источники задания, текущие TOKENS.md/CLIENT-MATERIALS.md, исходный редактор showcase/tokens.js и принятые CSS-контракты прочитаны.

## Объём

1. Дополнить TOKENS.md контрактом Button, Typography и Layout. Прямо различить оформление, геометрию и runtime; сохранить ограничения Inter, source Layout и неподтверждённой клиентской палитры.
2. Переиспользовать существующий Button-редактор через импорт tokens.js в отдельную showcase/theme.html. Не копировать его реализацию и не менять старый каталог 39 ролей. В ту же редактируемую область добавить Typography и Layout, связать семейство текста с кнопками, дать отдельный цвет текста и единый CSS-экспорт. Соседний исходный пример остаётся изолирован.
3. Показать связь с 78 ролями Typography и 14 ролями Layout ссылками на их реальные каталоги. Layout не получает цветовых токенов: оформление показывается содержимым внутри сетки.
4. Добавить переносимый ui/client-theme.example.css как явно демонстрационную необязательную тему. Общий entry не импортирует её автоматически.
5. Связать список материалов клиента с конкретными настройками. Отсутствующие брендовые данные остаются предложением, не утверждённым стилем. Геометрия шрифтов может менять ширину и переносы, обещание полной неизменности размеров не давать.
6. Тестировать взаимодействие font/color/radius на всех трёх примитивах, изоляцию исходного примера, reset/export, перенос single-entry CSS+theme на пустую страницу, фиксированные размеры/поля/gap, ширины 320/768/1024/1400. Старые tests Button39, Typography и Layout сохранить.

Передача через integration.json; shared nav/ROADMAP/manifest ведёт root. Ни commit, ни done builder не выставляет.
