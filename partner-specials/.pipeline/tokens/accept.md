# Приёмка P0-T01 — токены и кастомизация

Дата: 2026-09-08. Вердикт: **ПРИНЯТО** в пределах дополнения к пилоту.

Приёмщик самостоятельно прочитал scope и критерии ROADMAP, audit/capture, TOKENS.md, независимое review-1 и тестовый сценарий; запустил все три проверки и просмотрел сохранённые Tokens.320.png и Tokens.1400.png. Реализация не менялась, новые снимки не делались.

| Гейт | Результат |
|---|---|
| 1. Валидатор | PASS: strict проверил существующий Button, реестр, пути, CSS и состояния |
| 2. Браузер | PASS: старый пилот 30 вариантов × 4 ширины; дополнительно каталог, редактирование, экспорт, индикатор и адаптивность |
| 3. Независимое ревью | review-1: принять; blocker/major/minor нет, fix-отчёт не требуется |
| 4. Реестр | Новый компонент не добавлен. Существующий manifest/INDEX Button и CSS-root согласованы строгим валидатором; семейство partial |
| 5. Evidence | Сохранены audit/capture, review, Tokens320/1400 и report.json. Исходный SVG не изменён, использован как mask. Источником текущих значений служит ui/tokens.css; имена --ps-* обозначены новым контрактом, Figma/production не переснимались |
| 6. Копируемость | PASS: экспорт редактора перенесён в отдельную страницу с одним ui/partner-specials.css, scoped контейнером; цвет, радиус и высота проверены. Старый copy-тест спецификации также PASS |
| 7. Exit criteria | PASS: ровно39 уникальных публичных имён совпадают с CSS, значения следуют обеим темам. Цвет/радиус не меняют размеры примеров и основную матрицу. Смена шрифта сохраняет высоты, padding, кегль и интерлиньяж; изменение ширины текста явно оговорено. Переполнения320/1400 нет; на сохранённых PNG формы, примеры, CSS и каталог не обрезаны |
| 8. Статус | Только P0-T01 → done. Полный Button partial; остальные семейства, основные волны и ограничение Inter P0-F01 не закрыты |
| 9. Машинная проекция | N/A: слой machine ещё не создан и не затронут; каталог читает текущий CSS, не объявлен полной машинной проекцией пакета |
| 10. Принципы | N/A: вывод правил композиции не входит в дополнение |
| 11. Страница | N/A: редактор в витрине, не приёмка production-страницы |

Дословные результаты самостоятельных прогонов:

```text
npm run validate
PASS: 1 component; registry, CSS, local dependencies and state examples agree.

npm test -- --no-screenshots
Playwright Chromium is not installed; testing with installed Chrome.
PASS: 30 variants × 4 widths; themes, keyboard, disabled/loading, reduced motion, copyability and overflow.

npm run test:tokens -- --no-screenshots
PASS: 39 tokens; theme catalog, live edits, isolated CSS export, spinner color and responsive layout.
```

Релиз не объявлен. Полная библиотека Figma и утверждённое оформление клиента не входят в приёмку; существующие ограничения метрик Inter и контраста исходной secondary остаются.
