# R1-01 Typography — приёмка

2026-09-08. **ПРИНЯТО** в текстовом объёме; полное семейство partial.

Самостоятельно прочитаны спецификация, capture, исходные ссылки и независимое review-1 без находок. Просмотрены сохранённые Typography320/1400: текст целиком, переносы без обрезки. Источники не переснимались. Реализация на приёмке не менялась.

Гейты: strict validate PASS (3 записи текущего дерева); test:typography PASS: 28 semantic +10 fixed +64 пары ×4ширины,78 токенов, шрифт/цвет, source heights и буквальная копия HTML с единым CSS. Старые Button и tokens также PASS. Реестр и CSS-root соответствуют спецификации, evidence node keys сохранены. Независимое ревью подтверждает отдельный экспорт темы и auto границу1024. Новая HTML-семантика, сброс margin, перенос слов и auto-порог не выданы за production API. Inter version GAP сохранён.

Machine и production-страницы N/A. Это исходные пары компонентов Figma, не вывод правил продакшен-композиции. List/Point явно перенесены в R2-12, Core Gate ждёт этот шаг. Принимается R1-01, не вся R1. Общая интеграция в коммите содержит только Typography; Layout сохраняется в рабочем дереве для отдельной приёмки.
