# История изменений

## Unreleased

- Принят R1-03 и закрыта R1: единая тема меняет оформление Button и Typography внутри Layout, экспортируется на отдельный контейнер и сохраняет геометрическую базу. Реальные материалы клиента остаются необходимыми, списки Typography запланированы в R2-12; релиз не объявлен.

- Принят R1-02: контейнер и сетка воспроизводят семь опорных ширин с отдельными native/drawn профилями; 14 токенов видны в каталоге. Расхождение нарисованных и native полей не скрыто, браузерная адаптация явно новая; Layout остаётся partial.

- Принят R1-01: 28 семантических стилей, 10 фиксированных и 64 пары с точными промежутками, 78 токенов и переносимой темой. Списки и Point вынесены в явный R2-12 после Icon; Typography остаётся partial.

- Принят R0-03 и закрыта волна R0: каркас, реестр и оболочка витрины проходят строгую проверку и браузерные тесты. Это разрешает сборку основы R1, но не подтверждает готовность остальных компонентов.

- Принят R0-02: проверены 16 семейств, 26 узлов и зависимости без циклов; восстановлены пояснения инвентаря и согласован kind Button. Предварительные имена требуют визуального подтверждения.

- Принят R0-01: ТЗ и корпус 18 проектов разделяют геометрию Figma, материалы клиента и наблюдения исследования; свежая проверка production не заявлена.

- Начат отдельный пакет партнёрских спецпроектов по исследованию 08 и UI-kit Figma.
- Сохранена структурная инвентаризация Figma, составлен план сборки и список референсов.
- Разделены базовая геометрия, оформление клиента и сценарий проекта; добавлен список клиентских материалов.

- Принят пилот P0: текстовые main/secondary в трёх размерах и пяти состояниях переносятся с одним CSS, меняют оформление без изменения геометрии и поддерживают клавиатуру, disabled/loading и reduced motion. Проверены четыре канонические ширины и независимое ревью. Неизвестная версия Inter остаётся ограничением P0-F01; полный Button и остальные семейства не приняты.

- Принято дополнение P0-T01: каталог показывает 39 публичных токенов текущей темы, а редактор 12 настроек позволяет примерить оформление и перенести CSS на отдельный контейнер. Исходная матрица и базовая геометрия сохраняются; индикатор загрузки следует цвету подписи через маску исходного SVG. Проверки пилота и токенов пройдены, независимое ревью без находок.

Релиз не объявлен. Приёмка ограничена пилотом P0 и дополнением P0-T01; весь пакет остаётся в разработке.
