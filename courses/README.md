# Habr Courses — Design System Knowledge

Пакет знаний о визуальном языке **Хабр Курсов**. Собирается обратной разработкой:
инструментальные замеры продакшена, Figma-библиотека и макет, локальный снимок
Storybook.

> **Статус: в работе.** Закрыта инвентаризация: снято 10 страниц на шести
> ширинах, собран реестр из 55 элементов, составлен роадмап на 78 шагов.
> Заведён каркас: точка подключения `ui/courses.css` и слои под ней, скрипты
> проверок.
> **Вёрстка компонентов не начата** — спецификаций 0. Заполнены
> `ui/tokens.css` (R0-02), `ui/fonts.css` и `ui/foundations.css` (R0-03),
> `ui/layout.css` и новый `ui/utilities.css` (R0-04); `ui/components/*.css` —
> пустые каркасы, R2–R5.
> `components/` держит реестр `manifest.json` и шаблон спецификации (R0-05).
> `showcase/`, `machine/` не приняты. Пользоваться пакетом как источником
> разметки компонентов ещё нельзя.

Что предстоит — [`ROADMAP.md`](ROADMAP.md). Зачем и в каких границах —
[`BRIEF.md`](BRIEF.md).

---

## С чего начать

| | |
|---|---|
| Понять объём и границы | [`BRIEF.md`](BRIEF.md) |
| Понять порядок работ | [`ROADMAP.md`](ROADMAP.md) |
| Посмотреть, что найдено | [`.pipeline/inventory.json`](.pipeline/inventory.json) |
| Посмотреть снимки продакшена | `evidence/source/production/pages/<id>/` |

---

## Что это за продукт

Каталог онлайн-образования на домене `career.habr.com`: курсы, образовательные
организации, отзывы, промокоды, рейтинг школ, эксперты и редакция.

**Вид продукта — интерфейс**, один регистр. Единица работы: компонент → модуль →
страница. Обоснование и проверка гипотезы о лендинге — [`BRIEF.md`](BRIEF.md) §1.

---

## Не смешивать

| Продукт | Почему нельзя |
|---|---|
| **Хабр Карьера** (`career.habr.com`, `/vacancies`, `/companies`) | тот же домен, но другое приложение на другом стеке: контейнер 1100 против 1124, шапка 112 против 64, холодная сине-серая шкала против нейтральной, прямоугольные чипы против pill. Пакет — [`../career/`](../career/README.md) |
| **Основной Хабр** (`habr.com`) | другая шкала, серый фон, тёмная тема |
| **Макет `02_Education-NEW`** | описывает следующую итерацию продукта: другая схема URL, разделы профессий и распродажи, которых в проде нет. Источник состава и вариантов, но не структуры страниц |

---

## Границы

Пакет описывает **публичную часть Курсов, снятую гостем на 10 страницах**.
Логин нигде не потребовался.

Покрыты оболочка, пять семейств страниц (листинг, таблица, сущность,
промо-раздел, профиль), карточки сущностей, примитивы и адаптив на восьми
ширинах: 320 · 375 · 479 · 480 · 744 · 768 · 1024 · 1440. Полный набор файлов
(dom · computed · tokens · meta · png) — на шести из восьми: 320 · 375 · 479 ·
480 · 744 · 768; для 1440 есть PNG и общий снимок без суффикса, для 1024 —
только PNG (`docs/guide/tokens.md`, GAP-8).

Не покрыты: страница отдельного курса (её в продукте нет), формы ввода, пустая
выдача, ошибки, загрузка, открытые оверлеи, личный кабинет, тёмная тема,
доступность как система. Причины по каждому пункту — [`BRIEF.md`](BRIEF.md) §4.

**Если задача попадает в непокрытую зону, пакет даст визуальный язык и
компоненты — и не даст верной структуры экрана.**

---

## Структура пакета

Карта показывает состояние **на сегодня**: что уже лежит, что заведено пустым
и что появится позже. Шаг, который наполняет заготовку, назван рядом.

```
courses/
  README.md                 эта карта
  BRIEF.md                  ТЗ: продукт, источники, границы, критерии готовности
  ROADMAP.md                волны и шаги со статусами
  CHANGELOG.md              ## Unreleased и релизы
  package.json              скрипты validate / serve / capture
  playwright.config.mjs     браузерные проверки витрины (порт 4179)

  ui/                       слой Курсов — файлы заведены, содержимое на R0–R5
    courses.css             точка подключения — готова, 15 @import
    fonts.css               14 @font-face переменного Inter, файлы локальные;
                            разбор — docs/guide/typography.md
    tokens.css              51 переменная :root продукта; разбор —
                            docs/guide/tokens.md
    foundations.css         normalize v8.0.1, база документа, шкала из
                            9 ступеней, интерлиньяж поверх ступени
                            (.leading-none), фокус; разбор —
                            docs/guide/typography.md. Утилит переноса,
                            обрезки и выравнивания здесь НЕТ — GAP-9
                            статьи, их место в ui/utilities.css
    layout.css              оболочка (app-container/app-content), контейнер
                            1124 + 24, сетка секций gap-12, брейкпоинты —
                            R0-04; разбор — docs/guide/layout.md
    utilities.css           новый (R0-04): десять утилит переноса, обрезки
                            и выравнивания текста, GAP-9 typography.md;
                            разбор — docs/guide/layout.md
    components/*.css        10 файлов по категориям, пока только заголовки
    assets/fonts/           14 woff2 Inter, 711 КБ
    assets/icons/           пусто; спрайт и одиночные иконки — R2-01

  components/               manifest.json (55 записей), INDEX.md, шаблон
                            спецификации — R0-05. Словарь состояний — R1-01,
                            спецификации — R2–R5
  showcase/                 пусто; components.html — R0-06, pages.html — R6
  pages/                    пусто; по файлу на ключевую страницу — R6
  docs/guide/               tokens.md — разбор переменных продукта;
                            typography.md — шкала, база документа, шрифт,
                            фокус; layout.md — оболочка, контейнер, сетка
                            секций, брейкпоинты; composition, decisions,
                            coverage — R7
  docs/development/         заметки по работе с пакетом
  machine/                  пусто; машиночитаемая проекция — R8
  tests/                    пусто; первый спек витрины — R0-06
  evidence/                 снимки продакшена, Figma, Storybook, покрытие
    source/production/css/    корпус CSS: 10 инлайновых + 12 внешних .css,
                            по которым проверяются все счётчики шага R0-03
    source/production/pages/  10 страниц; dom · computed · tokens · meta · png
                            на 320 · 375 · 479 · 480 · 744 · 768; для 1440 —
                            PNG и снимок без суффикса, для 1024 — только PNG
    source/production/media-queries.json  media-запросы сборки и их активность
  tools/                    capture.mjs, serve.mjs, validate-components.mjs,
                            validate-classes.mjs, validate-counts.mjs
  .pipeline/                рабочее состояние конвейера
    inventory.json          реестр: 55 элементов, 10 страниц, 8 недостижимых мест
```

Пустой файл здесь — не забывчивость. Пустой слой честнее правдоподобных
значений: `tokens.css` наполняется снятыми числами, а не похожими.

---

## Чего этот пакет не делает

Он не является утверждённой дизайн-системой, UI Kit или компонентной
библиотекой. Он не нормализует противоречия источников и не достраивает
неизвестное: там, где прод, Figma и Storybook расходятся, пакет показывает
расхождение, а не выбирает молча.

Если решение не подтверждено источником, его здесь нет. Надёжный пакет
с пробелами полезнее полного пакета с догадками.
