# Ревью R7-01, R7-02 · итерация 2

12 сентября 2026, независимый ревьюер (guide-reviewer). Текст отчёта без
правок; скрипты ревьюера — во временной папке сессии, в пакет не входят.

**Вердикт: вернуть R7-01/R7-02 на доработку.** Блокеров нет, 1 major,
5 minor, 3 note. Вторая редакция закрыла все 9 major, 8 minor и 6 note первой
(проверено поимённо, см. «Что закрыто»). Осталась одна новая ошибка факта,
внесённая во второй редакции, и три знаменателя, которые не выводятся из
файла замеров.

Режим ревью инкрементальный по `review-1.md`, но все ключевые числа второй
редакции пересчитаны заново. Ничего в пакете не менял. Рисовка: снятый
`dom.html` (на 375 — `dom-375.html`) с CSS корпуса без `[data-v-…]`, Inter
перехвачен, скрипт выключен, ширины 1440, 1024, 768, 375. Витрина —
http://127.0.0.1:4179/showcase/pages.html.

## Major

| # | Где | Что утверждается | Что на самом деле |
|---|---|---|---|
| 1 | `composition.md:160`, `decisions.md:164-167`, `gen-rules-showcase.mjs:50` → блок C-6 витрины, `capture.md:81-82` | Класс `phone:hover:bg-none` «в CSS корпуса не скомпилирован», «в CSS корпуса его нет» | Правило скомпилировано во всех десяти `evidence/source/production/css/inline/*.css`: `@media (max-width:767px){ … .phone\:hover\:bg-none:hover{background-image:none} }` (например `rating.css`, смещение 39455 и 90674). Вывод «фон не отменяет» верен, но по другой причине: `bg-none` задаёт `background-image`, а `hover:bg-ui-black-50` — `background-color`, и они не пересекаются. Из-за ошибки GAP DG-6 («хотел ли продукт убрать фон строки на телефоне, по снимку не установить») построен на несуществующем пробеле: по снимку это устанавливается. В `capture.md` та же ошибка записана как «собственная ошибка, пойманная сверкой» — то есть сверка дала неверный результат |

## Minor

| # | Где | Находка |
|---|---|---|
| 2 | `composition.md:112`, `decisions.md:110-111` | «Радиуса 16 нет ни у одного узла; 8 — только у логотипов 32 и значка 28». Радиус 8 на 1440 у 188 видимых узлов: логотип 32 (96 + 30), значок 28 (30) и **обложка статьи** `div.aspect-video.w-full.rounded-t-lg.bg-cover` 348×196 — 32 узла. Ревью 1 (major 6) называло её прямо; во второй редакции закрыты два случая из трёх. По существу утверждение держится: обложка стоит в верхнем углу карточки, предок `overflow-hidden rounded-3xl` обрезает её на 24 — свои 8 не видны. Но так это нигде не сказано, и в таблицах §4 обложки нет вовсе: скан требует рамку, заливку или `IMG`, а `div` с `background-image` не проходит ни по одному признаку. Радиуса 16 действительно нет — проверено |
| 3 | `composition.md:196`, замеры `principles-evidence.md:507-515` | CT-2b «23 из 23 счётчиков от тысячи, 4 страницы» не выводится из §17. Слитно написанные подписи от тысячи, не цены и не годы: `+N` 16 + `прочее` 7 + `отзывов` 1 + `выпускник` 1 = **25**. 23 получается только как 16 + 7, то есть без «1419 отзывов» и «18073 выпускника» — двух примеров, которые правило приводит в своей же формулировке. Мой независимый проход по DOM даёт **23** другим составом: `education-centers-listing` 8, `reviews` 8, `education-center` 4, `courses-listing` 3. Три числа на одно место: либо таблица, либо дробь ошибается на 2 |
| 4 | `composition.md:196` | У CT-2b не назван единственный контрпример по форме: `principles-evidence.md:513` — «8 800 700-93-29» (`education-center`), подпись от тысячи с пробелом в разрядах, не цена. Он исключён из знаменателя молча. Колонка «Исключение или нарушитель» говорит «В корпусе нет» |
| 5 | `composition.md:103`, замеры `principles-evidence.md:212-223` | C-3c «267 логотипов из 268» не выводится из таблицы «квадрат»: в ней 299 узлов, из них не четверть стороны — `40 → 14` (1, назван исключением), `24 → 0` (30) и `36 → 0` (1). Тридцать `24 → 0` — это внутренний `img` значка `28 → 8`: предок скругляет и обрезает, так что это тот же объект, посчитанный дважды. Исключать их правильно, но ни таблица, ни правило этого не говорят: у «квадрата», в отличие от «плоскости» (`:183`), нет колонки «обрезает предок». Дробь 267/268 читателю не воспроизвести |
| 6 | `composition.md:101`, замеры `principles-evidence.md:226-238` | C-3a «223 из 232» собирается из пяти строк «прочего», а другие строки той же высоты 40–56 отброшены без записанного признака: `заливка · радиус 0 · высота 56` (28), `картинка · радиус 0 · высота 56` (14), `картинка · радиус 0 · высота 42` (14). Отбрасывать их правильно (растушёвка, картинки, плашка 1074×56 — не кнопки и не поля), но «кнопка и поле» нигде не задано селектором, и знаменатель 232 из файла замеров не собирается. Отдельно: 14 узлов `input.cursor-pointer.min-h-[38px]` высотой 40+ имеют радиус 0 и в замерах не показаны — у них нет ни рамки, ни заливки, видимый край даёт обёртка `span` (те самые 9 узлов исключения). Это стоит одной фразы в тексте правила |

## Note

- **7.** C-1, `composition.md:99`: «цветная заливка — только у hero и у обложек внутри карточек (134)» — сошлось. Я насчитал 147 плоскостей с непрозрачной заливкой, но 6 из них — `div.absolute.bottom-[20px].left-0.h-[60px]` с градиентом `from-transparent to-[--color-ui-white]`, то есть белая растушёвка, а не цвет; она названа в `:109`. Цветных ровно 7 hero + 134 обложки.
- **8.** `composition.md:4-5`: «Каждое правило — число или бинарный признак с покрытием дробью». У C-2, C-6, SH-2 и SH-4 в колонке «Покрытие» стоит счёт, а не дробь. Косметика.
- **9.** L-5a в тексте правила говорит «начинается в 16», а в 23 входит «ТОП онлайн-курсов» с замером 17. Допуск честно объявлен в §5 («Голоса («видимо», ±1)») и прослеживается; в тексте правила допуска нет.

## Что закрыто из первой итерации

Все 9 major, 8 minor и 6 note первой итерации проверены поимённо и закрыты.

- **major 1** — колонка «Исключение или нарушитель» есть у всех 35 правил; названные нарушители проверены выборочно и нарушают.
- **major 2** — L-3a 7/10 (листинг 5/5, таблица, сущность; нарушитель — профиль 40), L-3b 2/2 с оговоркой про клон. Сходится с `principles-evidence.md:101-113`.
- **major 3** — отбор L-5 по тегу `section` с `h2`: 28 секций вместо 15 по селектору `Section`. L-5a 23/24 вне промо-раздела на 7 страницах, L-5b 3/4. Пересчитал сам: «видимо» 16 — 23, 24 — 4, 12 — 1 из 28. Разбор 15 `gap-4` + 8 `mt-4` сошёлся, из 15 у 5 лента поднята `-mt-4`.
- **major 4** — C-1 254/306. Пересчитал: корней 315, под правило подходят 254 (7 типов с рамкой), `AdCard` 52 без рамки, `NumberedCourseItem` 9 вынесен как не карточка. Скан плоскостей от 200×60 даёт ровно **690**, тени нет ни у одной, радиусы 360 × 24 и 330 × 0 (317 обрезаны предком 24 + 13 без радиуса).
- **major 5** — C-3 разбит по форме. C-3a 223/232, C-3b **787 из 807** (пересчитал: 1003 узла ≤36 со скруглением, минус 196 квадратных логотипов = 807; пилюлей 787; не пилюля — 20, плашка школы `ReviewCard` 32 → 6), C-3c круг **138 из 138** (54 `Avatar` + 84 в стопке `SchoolCard`, все 9999).
- **major 6** — радиус 8 в `decisions.md` признан. Остаток — п. 2 выше.
- **major 7** — CT-2 разбит. CT-2a **209 из 210**, нарушитель «Скидка 1000 ₽» (`promocodes`) — сошлось точно, прослеживается до `principles-evidence.md:506` и `:516`.
- **major 8** — синий переписан: градиент шапки 10/10 и hero 7/7, текст ссылок, кнопки «Показать ещё», метка промокода. Сошлось с §8 замеров.
- **major 9** — карусели: **148 из 315** корней карточек на 7 страницах, в GAP DG-3 и в «Чего правила не задают». Пересчитал, сходится и постранично.
- **minor 10** — 1024 и 768 сняты, §1 «По ширинам» есть. Порядок блоков один на четырёх ширинах — 10/10, пересчитал по `axes.json` независимо.
- **minor 11, 12** — A-1 получил «вне области правила» (остальные `<button>`, выбранный `FilterChip`); 4 тёмные кнопки вне карточки и поиска названы и совпадают с §8.
- **minor 13** — CT-3 разбит: CT-3a **144 из 144** («Далее» 96 + «Подробнее» 48, пересчитал точно), нарушители `PromoCard` «Посмотреть» 4 и «Открыть код» 16; CT-3b **25 из 27** вне карточки, исключения «Каталог» и «Больше об эксперте».
- **minor 14** — «Как применять» и «Примеры» есть во всех семи областях, пары «неправильно — правильно» на месте.
- **minor 15** — SH-1 называет все три случая; текст витрины больше не противоречит себе. Проверил сам: **за контейнер не выходит ни один узел на всех четырёх ширинах** на 10/10 страницах. Выход за колонку в поле 24 — стрелки карусели (по 2 на ленту) и панель экспертов; таблица рейтинга и шапка школы — на 375.
- **minor 16** — чип «Git» в образце C-3b больше не схлопнут: 32×24, подпись видна.
- **minor 17** — «крупная плоскость» получила число: от 200 в ширину и от 60 в высоту (`composition.md:105`).
- **note 18, 19** — звёзды отнесены к `ui-yellow-500`; A-4 снят как повтор SH-6, номер не переиспользован.
- **note 20** — C-4 показывает оба знаменателя: 0 из 254 и 0 из 315.
- **note 21** — §11 «секций с h2» теперь 28 и совпадает с §5.
- **note 22, 23** — формулировка про 96 переписана (пустой слот adfox); «Когда» в DG-2 без рекомендации.

## Что проверено и сошлось во второй редакции

- **A-5 (новое правило): 41 из 42.** Пересчитал все текстовые узлы крупнее 18 вне шапки и подвала: 28 `h2` 24/600, 7 `h1` 44/600, 4 `div.text-h2` 24/600, 2 `div.text-h1-mobile` 30/600 и один `div.text-h3` 20/600 — имя эксперта в «Больше об авторах» на витрине курсов. Ровно 42, нарушитель ровно тот, что назван. Прослеживается до `principles-evidence.md:363-378` (24px/600 — 32, 44px/600 — 7, 30px/600 — 2, 20px/600 — 1).
- **C-6: 37 ссылок на 5 страницах** — 30 строк таблицы (`courses-listing` 10, `rating` 20) и 7 номеров листания (2 + 2 + 2 + 1). Сошлось точно.
- **C-5** — `hover:opacity-90`, `hover:bg-ui-black-100` и `hover:bg-ui-black-50` в CSS корпуса скомпилированы, проверено по тексту таблиц стилей.
- **A-1: 191 из 191** (173 тёмных, 18 светлых); C-2: 32 узла с тенью из 32; C-4: 0 из 315.
- **Витрина.** 35 блоков `data-rule` на 35 правил, id совпадают с `composition.md`, битых якорей 0, ошибок консоли 0. Живых образцов ровно **8**: C-1, C-3a, C-3b, C-3c, A-1, A-2, C-4, C-5. Вычисленные стили каждого соответствуют своему правилу: C-1 — рамка 1px, радиус 24, без заливки и тени; C-3a — 12 у кнопки 40 и ссылки-кнопки 48; C-3b — чипы 24 и фильтры 36 с 9999, выбранный «Все» тёмный, бейдж −50% 20/9999; C-3c — логотип 32 → 8, фото 68 → круг, значок 28 → 8; A-2 — #ff7e47 только на бейдже; C-4 — у корня карточки 320×420 hover-классов нет. Ряд образца C-3b шире блока, но его родитель прокручивается (`overflow-x: auto`) — это не обрезка.
- **Язык.** Оценочных прилагательных в `composition.md`, `decisions.md` и `capture.md` нет (единственное «плохо» в `decisions.md:11` — о переносимости рекомендации, не оценка продукта).
- **Прослеживаемость «число → таблица замеров»** держится у 254/306, 690, 787/807, 138/138, 209/210, 144/144, 25/27, 41/42, 148/315, 23/24, 3/4, 37, 173/18, «за контейнер 0» и «порядок один на 4 ширинах». Не держится у 23 (CT-2b), 268 (C-3c) и 232 (C-3a) — п. 3, 5, 6.

## Можно ли принимать

Пока нет, но правки локальные: новой съёмки и пересчёта не нужно.

1. Убрать утверждение, что `phone:hover:bg-none` не скомпилирован, из `composition.md`, `decisions.md`, `gen-rules-showcase.mjs` и `capture.md`. Правило есть; фон строки оно не отменяет, потому что задаёт `background-image`, а не `background-color`. Пересобрать GAP DG-6.
2. Дописать в `composition.md:112` и `decisions.md:111`, почему обложка статьи с `rounded-t-lg` не считается: её обрезает предок с радиусом 24.
3. Свести CT-2b: либо 25 из 25, либо 23 с записанным признаком, что в счёт идёт; назвать «8 800 700-93-29» в исключениях.
4. Дать в таблице «квадрат» колонку «обрезает предок» (как у «плоскости») — тогда 267 из 268 воспроизводится.
5. Записать признак отбора «кнопка и поле» для C-3a.

Файлы ревьюера в `C:\Users\life5\AppData\Local\Temp\claude\d--work-guides\0e3d23a3-0e77-4627-9b00-5f2c784200a7\scratchpad\r7rev2\`:
`n1.mjs` → `n1.json` (карточки, плоскости, радиусы, секции, числа, подписи,
карусели, выход за контейнер на 4 ширинах), `n2.mjs` → `n2.json` (квадраты,
узлы с радиусом 8, правила hover), `n3.mjs` (растушёвка и обрезка обложки),
`a1.mjs`, `a2.mjs` → `a1.txt`, `a2.txt` (сводки), `sc.mjs`, `sc3–sc5.mjs`,
`s-*.png` (витрина).
