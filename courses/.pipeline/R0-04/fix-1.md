# Правки по review-1 — layout (R0-04)

| # находки | Severity | Решение | Что сделано | Где |
|---|---|---|---|---|
| 1 | major | исправлено | GAP-1 недосчитывал исключение: `grep -c 'grid-cols-\[minmax(0,1fr)\]' evidence/source/production/pages/editors/dom.html` → `0`, при этом `editors` несёт узел `class="flex flex-col gap-10 pb-10"` — побайтово идентичный узлу `authors`. Верно 3/10 (`author`, `authors`, `editors`), а не 2/10; страниц с сеткой — 7/7, а не 7/8. Число и список исключений исправлены во всех трёх местах, где они встречались | `docs/guide/layout.md` (шапка-таблица, «Когда использовать», GAP-1); `ui/layout.css` разд. 3 (комментарий); `.pipeline/R0-04/capture.md` §4.2 (заголовок и сводная фраза — примечательно, что `editors` уже стоял в самой таблице покрытия §4.2, просто не попал в сводку строкой ниже) |
| 2 | major | исправлено | Таблица «Контейнер» недосчитывала исключение: `grep -c 'class="m-auto max-w-\[1124px\] px-6"' evidence/source/production/pages/education-center/dom.html` → `0` (и `author/dom.html` → `0`). Верно 8/10, исключений два (`author`, `education-center`), а не 9/10/одно | `docs/guide/layout.md` (таблица «Контейнер»); `ui/layout.css` разд. 2 (комментарий); `.pipeline/R0-04/capture.md` §3.3 |

## Пересчёт всего документа (задача из инструкции, не отдельная находка отчёта)

По требованию — grep по всем 10 `dom.html` без выборки для каждой дроби
в `docs/guide/layout.md` и `ui/utilities.css`:

- `.leading-none`, `.container` 0/10, `app-container`/`app-content` 10/10,
  все десять чисел GAP-9 в `ui/utilities.css` — не трогал, это явно
  подтверждённый список из задания (независимо пересчитаны, совпали).
- FilterModal `6/10` (раздел «Брейкпоинты», `base-modal__wrapper`/
  `v-popper--theme-dropdown`) — перепроверил отдельно: `courses-listing`,
  `education-centers-listing`, `promocodes`, `rating`, `reviews`,
  `schools-for-children` = 6, `author`/`authors`/`editors`/
  `education-center` = 0. Число верное, не трогал.
- GAP-2 «восьмое вхождение `gap-12`» — `grep -c 'grid gap-12'
  education-centers-listing/dom.html` = 1 (второй, анонимный узел поверх
  семи страниц с сеткой секций = 8 вхождений суммарно). Арифметика не
  зависит от находок 1–2 выше, не трогал.
- Победитель `pt-10 pb-10` (5/7) и проигравшие (1/7 + 1/7) — сумма сходится
  с новым знаменателем 7 (те же 7 страниц, что несут сетку секций после
  исправления находки 1), пересчитывать нечего.

**Дополнительно найдено при пересчёте (не находка отчёта, не отдельная
роадмап-строка — правка внутри уже открытого файла, а не расширение
объёма):** комментарий `ui/layout.css` разд. 2 утверждал у первой связки
(`mx-auto max-w-[1124px] px-6 py-0 tablet:px-6`) третье, «более глубокое»
вхождение «без tablet:/py-0» на 6/10 страниц. Проверка (`grep -o
'class="[^"]*"' */dom.html | grep mx-auto | grep max-w | grep px-6`)
показала на всех 10/10 страницах ровно 3 совпадения: 2 точных узла
(обёртка контента, подвал) плюс третий, более длинный класс навигации
шапки, который эту связку не теряет, а расширяет (`… tablet:px-6 m-auto
flex h-[64px] …`). Сам этот факт уже верно записан в `.pipeline/R0-04/
capture.md` §3.3 («10/10» для третьей строки) — расхождение было только
в комментарии `ui/layout.css`, который я привёл в соответствие с уже
лежащим источником этого же шага, не вводя новых чисел. Это тот же класс
ошибки («N из 10» недосчитан/перепутан), поэтому фиксирую как правку,
а не как отдельную строку долга.

## Строка долга X-78 (не моя работа — для приёмки)

Ревьюер `review-1.md` независимо, третьим методом (реальный CSS-парсер
браузера через CSSOM `styleSheets`/`cssRules`, Playwright/msedge),
подтвердил числа `small-phone:` 460 и `phone:` 2170 — те же самые, что
были в исходном отчёте R0-04 (брейс-скан). Это уже второе независимое
подтверждение (сначала брейс-скан R0-04, теперь браузерный CSSOM
рецензента) при нуле подтверждений в пользу `470/2220` из `X-78`.
`ROADMAP.md` не трогал — не входит в границы этого шага. Рекомендация
приёмке: снять `X-78` (или переформулировать как «пересчёт приёмки
R0-03 ошибочен, верны 460/2170»).

## Note (severity: note, #3, GAP-3 в E.19)

Не находка, а замечание на будущее («метрика "попадание правила в
условие без остатка" не проверялась построчно на предмет двойного
счёта между без-префиксными компонентами»). Ревьюер сам пишет, что
расхождения не нашёл — только фиксирует метод как непрозрачный.
Решение: **принято к сведению, не правка**. GAP-3 в
`docs/guide/layout.md` уже описывает три безымянных условия
`@media` перечислением селекторов на глаз, как и отмечает ревью;
переписывать метод раздела в рамках находки severity `note` без
воспроизведённого расхождения — не по правилам («по одной находке,
закрывающей именно её», без раздувания объёма). Ушло в примечание
этого файла, не в `ROADMAP.md` (границы шага).

## Изменённые файлы

- `docs/guide/layout.md` — числа и списки исключений (находки 1, 2)
- `ui/layout.css` — комментарии разд. 2 и 3 (находки 1, 2) + приведение
  в соответствие с `capture.md` §3.3 (описано выше, не отдельная находка)
- `.pipeline/R0-04/capture.md` — §3.3, §4.2 и раздел «Найдено по ходу»
  (числа и списки исключений, находки 1, 2); файл в `.gitignore`
  (`/*/.pipeline/R*/`), в коммит не попадёт

`career/`, `ROADMAP.md`, `showcase/`, `tools/validate-classes.mjs`,
`tools/validate-components.mjs` не трогал.

## Результат валидатора

```
$ node tools/validate-counts.mjs
Checked 28 documented counts against the package. All match.
  roadmapSteps: 76
  roadmapR0: 7
  elements: 55
  pages: 10
  unreachable: 8
  cssCategories: 10
  tokens: 51
  manifest: 55
  specs: 0
  scopeProduction: 44
  scopeStorybookOnly: 5
  scopeFigmaOnly: 6
EXIT=0

$ node --check tools/*.mjs
OK tools/capture.mjs
OK tools/measure-selectors.mjs
OK tools/serve.mjs
OK tools/validate-classes.mjs
OK tools/validate-components.mjs
OK tools/validate-counts.mjs
EXIT=0
```

Информационно (не квалифицирующий гейт, `R0-06` в статусе `blocked`,
файл не менялся):

```
$ node tools/validate-components.mjs --strict
Validated 55 component records.
Manifest structure is valid.
EXIT=0
```

## Что ушло в roadmap отдельными строками

Ничего — обе находки этого отчёта закрыты правкой текста, третий
попутный факт («6/10 глубже» → «10/10», см. выше) приведён в
соответствие с уже лежащим источником `capture.md` того же шага, а не
оформлен как новая роадмап-строка. `ROADMAP.md` не редактировался
(граница шага); рекомендация по `X-78` передана приёмке текстом выше,
не строкой файла.
