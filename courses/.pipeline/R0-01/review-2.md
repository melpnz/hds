# Ревью R0-01 «Каркас пакета» — итерация 2

Вердикт: **принять с замечаниями**
Дата: 8 сентября 2026.

Шаг инфраструктурный: разделы A (соответствие источнику), C (состояния),
D (responsive), F (страницы), G (правила), H (машинный слой) чеклиста
неприменимы — элемента продукта у шага нет. Это отсутствие предмета, а не зачёт.
Проверялись закрытие 19 находок `review-1.md`, откат правки о брейкпоинтах,
инварианты METHOD §6 в той мере, в какой их можно заложить в пустой каркас,
и достоверность того, что пакет говорит о себе.

Все четыре `major` первой итерации закрыты фактически — воспроизведены, а не
приняты на слово. Ни одного `blocker`. Остаток — девять `minor` и `note`,
из них пять новых.

## Что открывалось и запускалось

| Проверка | Команда / способ |
|---|---|
| Баг съёмки (находка 1) | `node tools/capture.mjs --url file:///…/hidden.html --selector .probe --out …/out --wait 200` на узле с `@media (max-width:400px){.probe{display:none}}`, шесть ширин по умолчанию |
| Тот же узел в переиспользованный `--out` | два прогона подряд в одну папку: сначала видимый узел, затем скрытый |
| Разбор аргументов (находка 13) | шесть вызовов: `--widths` / `--wait` / `--shot-timeout` без значения, `--widths 375.5`, `--widths abc`, без `--out` |
| Валидаторы | `node tools/validate-counts.mjs`, `node tools/validate-classes.mjs`, `node tools/validate-components.mjs --strict` — вывод и коды ниже |
| Диффы валидаторов против образца | `diff -u career/tools/validate-{counts,classes}.mjs courses/tools/…` |
| Обоснование отказа от `check-evidence-age.mjs` | чтение `career/tools/check-evidence-age.mjs` + `ls evidence/source/production/*.json` |
| Порт | `grep 4178\|4179` в `career/tools/serve.mjs`, `career/playwright.config.mjs`, `courses/tools/serve.mjs`, `courses/playwright.config.mjs`; `netstat -ano \| grep 4178\|4179` |
| Точка подключения в браузере | msedge через Playwright, пустая страница с одним `<link>` на `/ui/courses.css`, слушались `response` / `requestfailed` / `console` |
| Playwright | `npx playwright test` при свободном порту |
| Числа | независимый пересчёт `roadmapSteps`, `roadmapR0`, `elements`, `pages`, `unreachable`, `cssCategories`, плюс подсчёт уникальных id шагов и разбивка по волнам |
| Токены по страницам | `Object.keys(tokens.json.custom).length` по всем десяти папкам |
| Откат правки о брейкпоинтах | шесть названных файлов построчно + `grep` по всему пакету на `480`, `768 / 1024`, «единственн\* граница», «раскладку не меняет» |
| Граница 480 | `evidence/source/production/media-queries.json` — все `min/max-width` в сборке |
| `career/` и `_sources/` | `find … -newermt "2026-09-08 00:00"` |

Временные файлы проверки (`copy-check.html`, `_rev-check.mjs`) удалены; сервер,
поднятый на 4179 для браузерной проверки, погашен, порт свободен.

---

## Откат правки о брейкпоинтах — проверен отдельно

Проверено построчно во всех шести названных файлах и `grep`-ом по всему пакету.
**Утверждений «единственный брейкпоинт 768» и «480 не работает» в пакете нет** —
ни в тексте, ни в комментариях CSS, ни в комментариях кода, ни в примерах.

| Файл | Состояние |
|---|---|
| `BRIEF.md:195` (§5.6 п. 6) | «Прод: 480 / 768 / 1024 (`phone:`, `tablet:`, `tablet-only:`). Figma: 320 / 744 / 1024» — совпадает с формулировкой, приведённой в `fix-1.md` как исходная |
| `BRIEF.md:282–295` (§9) | «Прод реализован на другой схеме — **480 / 768 / 1024**», далее мобильная раскладка на 744 и следствие для evidence. Добавленного блока измеренных фактов нет: `grep` по `max-width: 767`, «замер», `R0-00` в BRIEF даёт ноль совпадений в этом разделе |
| `ROADMAP.md:264–266` (решение 2) | «Прод реализован на 480 / 768 / 1024» |
| `ROADMAP.md:286–307` (раздел R0-00) | абзаца «Замер шага, 8 сентября» нет; на его месте врезка «Вывод шага R0-00 опровергнут ревью и здесь не приводится» с механикой ошибки (1488 из 10 877 ключей без пары, 13,7 %), фактом подмены фильтра 6/10 и прямым запретом: «Шагу R0-04 (`ui/layout.css`) опираться на замер R0-00 нельзя» |
| `ui/layout.css:9–22` | «480 / 768 / 1024» + врезка «ВНИМАНИЕ для R0-04: границу 480 доказать нечем, замер R0-00 негоден в обе стороны» |
| `tools/capture.mjs:11` | «реализация, которая построена на 480 / 768 / 1024» |
| `tools/README.md:23–29` | «построенной на 480 / 768 / 1024» + «границу 480 ни одна из шести ширин не пересекает вплотную», ссылка на X-15 |

Поиск по остальным местам пакета следов не нашёл. Единственное вхождение
опровергнутого вывода — `.pipeline/capture-log.md:81–96`, и оно записано
строкой **X-16** с номерами строк и владельцем доработки.

**Оцениваю это как приемлемое.** `.pipeline/` по METHOD §2 — не документация,
а протокол; `capture-log.md` — продукт шага R0-00, который сейчас на доработке
у другого агента, и правка его отсюда была бы и расширением объёма (METHOD §9),
и гонкой за один файл. Замечание к оформлению — находка 26 ниже.

Отдельно проверил утверждение, которое пришло на замену опровергнутому и теперь
стоит в четырёх файлах: «между 375 и 744 480 — единственная CSS-граница».
`evidence/source/production/media-queries.json` даёт полный набор границ сборки:
`max:360 · max:479 · min:480 · max:767 · min:768 · max:1023 · min:1024`.
В интервале (375, 744) действительно только 479/480. Утверждение **верно
и подтверждается evidence** — претензия только к тому, что источник рядом
не назван (находка 25).

---

## Закрытие находок review-1

| # | Было | Итог | Чем проверено |
|---|---|---|---|
| 1 | major · `capture.mjs` врёт о полноте съёмки | **закрыто** | воспроизведено, см. ниже |
| 2 | major · ложное обоснование отказа от валидаторов | **закрыто** | оба перенесены и запущены; обоснование по `check-evidence-age.mjs` на этот раз верное |
| 3 | major · порт 4178 совпадает с `career/` | **закрыто** | 4179 в `tools/serve.mjs:11`, `playwright.config.mjs:7,8` |
| 4 | major · конфликт с `guide-build` §2 нигде не зафиксирован | **закрыто по букве**, остаток → находка 20 | записан в трёх местах |
| 5 | minor · 75 шагов при 76, R0 6 при 7 | **закрыто** | независимый пересчёт: 76 строк, 76 уникальных id, R0 = 7 |
| 6 | minor · «Проверки — не заведены», дата сводки | **закрыто** | `ROADMAP.md:11,25` |
| 7 | minor · README описывает состояние, которого нет | **закрыто частично** → находка 21 | карта переписана, две строки остались |
| 8 | minor · ложная причина решения 1 (`cssRoots`) | **закрыто** | `capture.md:62–71` — переписано на «правило для человека», механизм назван верно |
| 9 | minor · волны в шапках `data-display` / `forms` | **закрыто** | «R2, `InfoTable` — R5-12», «R3, `SearchForm` — R4-06» в обоих файлах и в таблице `capture.md` |
| 10 | minor · X-01 описывает не то падение | **закрыто** | строка переписана, обе зависимости названы; падение проверено прогоном |
| 11 | minor · скрипт `test` — ложное обещание | **закрыто** | снят из `package.json` |
| 12 | minor · §6.2 пересказан как «только `--doc-*`» | **закрыто** | `ui/courses.css:17–23`, `showcase/.gitkeep`, exit criteria R0 — везде про классы |
| 13 | minor · `--widths` без значения роняет стек-трейсом | **закрыто** | шесть вызовов, все дают код 2 |
| 14 | note · ловушка относительного `@import` | **закрыто** | предупреждение в шапках всех трёх файлов |
| 15 | note · `RULES.md` против `composition.md` | **перенесено** — X-07, обоснование принимаю: решать здесь значило бы решать за R7 |
| 16 | note · отступления от METHOD §2 не отмечены | **закрыто** | `capture.md` раздел «Отступления от METHOD §2» |
| 17 | note · `computedAtWidth` только для будущих съёмок | **закрыто** | `capture.md:131–134` |
| 18 | note · шаг оставил работающий сервер | **закрыто** | `netstat` по 4178 и 4179 чист |
| 19 | note · именование образца копировать нельзя | **закрыто** | exit criteria R0, `ui/courses.css`, `showcase/.gitkeep` |

### Находка 1 воспроизведена

Тем же способом, что и в первый раз: узел, скрытый на мобильных, шесть ширин
по умолчанию.

```
  320px … не снято
    320px не снято: locator.screenshot: Timeout 9996.666ms exceeded.
  375px … не снято
    375px не снято: locator.screenshot: Timeout 9996.841ms exceeded.
  744px … 506 Б
  768px … 506 Б
  1024px … 506 Б
  1440px … 506 Б
Снято: …/out (узлов 1, токенов 0, ширин 4 из 6: 744 · 768 · 1024 · 1440)

Не снято ширин: 2 из 6 — 320, 375.
meta.json перечисляет только снятые; evidence неполон, и это не пропускается молча.
КОД ВОЗВРАТА: 1
```

`ls out` — четыре PNG, `320.png` и `375.png` отсутствуют.
`meta.json`: `"widths": [744,768,1024,1440]` (ровно то, что на диске),
`"requestedWidths"` — шесть, `"failedWidths"` — две записи с причинами,
`computedAtWidth: 1440`. Ложных записей нет, код ненулевой, ошибка в `stderr`.
Ожидание с `--shot-timeout` по умолчанию — 10 с на ширину вместо 30.

### Находка 2: обоснование отказа проверено, на этот раз оно верное

`career/tools/check-evidence-age.mjs:29` —
`fs.readdirSync(dir).filter(f => f.endsWith('.json'))`, без рекурсии.
В `courses/evidence/source/production/` плоско лежит **один** `*.json`
(`media-queries.json`), а все снимки — в `pages/<id>/meta*.json`. Скопированный
скрипт отчитался бы «Снимков продакшена: 1» и не увидел ни одной страницы.
Обоснование X-05 — факт, а не отговорка.

Прогоны обоих перенесённых валидаторов, дословно:

```
$ node tools/validate-counts.mjs
Пропущено (1) — появится по мере сборки:
  components/INDEX.md — файла ещё нет

Checked 13 documented counts against the package. All match.
  roadmapSteps: 76
  roadmapR0: 7
  elements: 55
  pages: 10
  unreachable: 8
  cssCategories: 10
  tokens: 0
  manifest: —
  specs: 0
код 0

$ node tools/validate-classes.mjs
Витрин ещё нет. Проверять пока нечего.
  showcase/components.html — заводит шаг R0-06
  showcase/pages.html — заводит шаг R6
код 2

$ node tools/validate-components.mjs --strict
Нет файла components/manifest.json — его заводит шаг R0-05. Проверять пока нечего.
код 2
```

### Числовой дрейф пересчитан руками

Валидатор мог быть настроен под текущее состояние. Пересчитал независимо:

| Счётчик | Валидатор | Ручной пересчёт | Как считал |
|---|---:|---:|---|
| `roadmapSteps` | 76 | **76** | `grep -oE "^\| (R[0-9]-[0-9]{2}) \|"` → 76 строк, `sort -u` → 76 уникальных id, дублей нет |
| `roadmapR0` | 7 | **7** | те же id, `cut -d- -f1 \| uniq -c` |
| `elements` | 55 | **55** | `inventory.elements.length`; разбивка по волнам R2 12 · R3 15 · R4 14 · R5 14 = 55 сходится с таблицей волн |
| `pages` | 10 | **10** | `inventory.pages.length` = 10 папок в `evidence/source/production/pages/` |
| `unreachable` | 8 | **8** | `inventory.unreachable.length` |
| `cssCategories` | 10 | **10** | `ls ui/components/*.css` |

Разбивка по волнам из независимого подсчёта — R0 7 · R1 2 · R2 12 · R3 15 ·
R4 14 · R5 14 · R6 5 · R7 3 · R8 4 — совпадает с таблицей «Волны» в
`ROADMAP.md:36–46` по каждой строке. Эти девять чисел валидатор не проверяет
(находка 24), но сегодня они верны.

### Точка подключения открыта в браузере

msedge, `node tools/serve.mjs` на 4179, пустая страница с одним `<link>`:

```
CSS ответов: 14  (точка входа + 13 @import)
  200 /ui/courses.css   200 /ui/tokens.css        200 /ui/foundations.css
  200 /ui/layout.css    200 /ui/components/*.css  ×10
404: 0        requestfailed: 0
console: единственная запись — 404 на /favicon.ico, который просит сам браузер
```

`curl /ui/courses.css` → 200, `curl /ui/career.css` → 404, в логе сервера корень
`D:\work\guides\courses`. Подмены пакетов на 4179 нет.

`npx playwright test` при свободном порту: `Timed out waiting 60000ms from
config.webServer`, код 1 — падение ровно то, что описывает X-01 в части
зависимости от R0-06 (`webServer.url` смотрит на `showcase/components.html`,
которого ещё нет). Первое звено больше не мешает: `npm test` снят.

---

## Новые находки

### 20 · minor · METHOD §9

X-04 записан в трёх местах, но ни одно из них не лежит на пути, которым
`guide-build` ведёт верстальщика R2-01. Скилл посылает его в
`.pipeline/inventory.json` (§1) и в строку шага роадмапа, а команду даёт
дословно в §2 — `--widths 375,768,1024,1440`. Оговорки нет ни в преамбуле волны
R2 (`ROADMAP.md:88–104`), ни в exit criteria R2, ни в одной строке шагов R2–R5.
Три записи, которые сделаны, — строка X-04 в самом низу «Найдено по ходу»,
`tools/README.md` и шапка `tools/capture.mjs` — читаются, только если верстальщик
сам туда пойдёт. Механической защиты нет: `capture.mjs` не предупреждает,
когда переданный `--widths` не содержит канонических 320 и 744.

Находка первой итерации закрыта по букве — конфликт зафиксирован. Но опасность,
ради которой она заводилась, не снята.

Где: `ROADMAP.md:321` (X-04), `ROADMAP.md:88–104` (волна R2, оговорки нет),
`tools/capture.mjs:70–79` (разбор `--widths` без проверки состава).

Как проверить: `grep -n "widths" .claude/skills/guide-build/SKILL.md` → строка 25;
`grep -n "X-04\|без \`--widths\`" ROADMAP.md` → одна строка, 321.

### 21 · minor · E.19 (остаток находки 7)

Карта пакета в `README.md` переписана и в целом честна: у каждой заготовки стоит
«пусто» и назван наполняющий шаг. Две строки правило нарушают и описывают
содержимое, которого нет:

- `README.md:96` — `docs/development/  заметки по работе с пакетом`. В папке
  один `.gitkeep`; заметок нет, шаг не назван.
- `README.md:99` — `evidence/  снимки продакшена, Figma, Storybook, покрытие`.
  Из четырёх названного есть только `source/production/`.
  `source/figma/`, `source/storybook/`, `curated/`, `verification/` — пустые
  `.gitkeep`, а `evidence/coverage.md` не существует вовсе и заводится на R7-03
  (`ROADMAP.md:222`), о чём карта не говорит.

Как проверить: `find docs evidence -type f -not -name "*.png" -not -path "*/pages/*"`.

### 22 · minor · METHOD §8, E.19

Утверждение о собственном покрытии неверно, и на него сошлётся R0-02.
`BRIEF.md:182`: «52 переменные `:root` (10/10)». Фактически 52 переменные на
**семи** страницах из десяти; на `author`, `authors`, `editors` их 51 — там нет
`--swiper-theme-color`. Правильная запись — `52 (7/10)` с названной причиной
расхождения. Ровно это расхождение первая итерация ревью уже подтвердила
по evidence, но в BRIEF оно не доехало.

Число 52 живёт ещё в двух местах и ни одним правилом не проверяется:
`ui/courses.css:10` («переменные `:root`, снятые с продакшена Курсов (52 шт.)» —
настоящим временем о пустом файле; смягчено строкой 25, но не снято) и
`ROADMAP.md:56` (шаг R0-02). У `validate-counts.mjs` правила для счётчика
`tokens` нет вовсе — то есть валидатор, заведённый ради этого класса дрейфа,
именно это число не сторожит.

Где: `BRIEF.md:182`, `ui/courses.css:10`, `ROADMAP.md:56`;
`tools/validate-counts.mjs` массив `claims` — ключа `tokens` в нём нет.

Как проверить:
`for d in evidence/source/production/pages/*/; do node -e "console.log(Object.keys(require('./$d/tokens.json').custom).length)"; done`
→ `51 51 52 51 52 52 52 52 52 52`.

BRIEF не продукт этого шага — находка на строку роадмапа, не на переделку здесь.

### 23 · minor · E.19

Строка X-02 называет адресатом `README.md`: «`README.md`, `BRIEF.md` §2 и
`.pipeline/capture-log.md` говорят „10 страниц × 4 ширины“». В README этой
формулировки больше нет — её сняли на этой же доработке по находке 7. Осталось
два адресата, а не три. Строка, которая посылает следующий шаг править уже
исправленное, — та же болезнь, от которой заведён `validate-counts`.

Где: `ROADMAP.md:319`.

Как проверить: `grep -n "4 ширины\|четыр" README.md` → пусто;
`grep -n "4 ширины\|четырёх ширин" BRIEF.md` → строки 51, 137, 218.

### 24 · minor · METHOD §9, E.19

`CHANGELOG.md:21–24` получил запись о досъёмке R0-00 — правка сверх находок,
сделанная этим шагом. Три возражения. Первое: сам файл в шапке говорит «Записи
делает приёмка шага (`guide-accept`)», а R0-01 — не приёмка. Второе: R0-00 не
принят, он на доработке с двумя `blocker`, и запись в «Добавлено» подаёт его
результат как состоявшийся. Третье: в `capture.md` этой правки нет ни в одном
разделе — «Что проверено», «Найдено по ходу» и «Чего шаг не делает» о ней молчат,
она упомянута только в `fix-1.md` под заголовком «Прочее сверх находок».

Фактическая часть записи верна: `375-reload.png`, `768-reload.png`,
`dom-320/375/744/768`, `computed-*`, `tokens-*`, `meta-*` и `media-queries.json`
на диске есть — проверено по всем десяти папкам.

Где: `CHANGELOG.md:21–24`; правило — `CHANGELOG.md:3–4`.

### 25 · note · METHOD §4

Утверждение «480 — единственная CSS-граница в интервале 375–744» стоит
в четырёх местах (`ROADMAP.md:301`, `ui/layout.css:20–21`, `tools/README.md:27–28`,
`.pipeline/R0-01/capture.md:115–117`) и ни в одном не назван источник.
Я его проверил — оно верно, источник `evidence/source/production/media-queries.json`
(все границы сборки: 360, 479/480, 767/768, 1023/1024). По METHOD §4 у факта
должен быть источник рядом с фактом, иначе следующий шаг перепроверяет заново
или принимает на веру.

### 26 · note

`.pipeline/R0-01/capture.md:131–133` посылает читателя за `computedAtWidth`
десяти старых папок в `.pipeline/capture-log.md` — тот самый файл, строки 75–96
которого несут опровергнутый вывод (X-16). Указатель верный по существу, но
без предупреждения: читатель приходит в файл за одним и попутно читает другое.
Достаточно ссылки на X-16 рядом с указателем.

### 27 · note · METHOD §4

`capture.mjs` перестал врать о полноте съёмки, но переиспользование `--out`
оставляет незамеченный мусор. Прогон 1 (узел виден) кладёт шесть PNG; прогон 2
в ту же папку (узел скрыт на мобильных) снимает четыре, `meta.widths` честно
показывает `[744,768,1024,1440]` — а `320.png` и `375.png` от **прошлого прогона
другого URL** остаются на диске и ничем не помечены. Потребитель, который берёт
`ls *.png`, а не `meta.widths`, получит шесть файлов, два из них чужие.

Заодно неточна формулировка, повторённая в `tools/README.md:60–62` и
`tools/capture.mjs:26–28`: «`widths` перечисляет только те ширины, для которых
PNG действительно оказался на диске». В этом сценарии `320.png` на диске есть,
а в `widths` его нет. Точная формулировка — «снятые в этом прогоне».

Как воспроизвести: два прогона подряд в одну папку `--out`, сначала на видимом
узле, затем на скрытом; сравнить `ls` и `meta.widths`.

### 28 · note

`validate-counts.mjs` сторожит шесть счётчиков из тех, что пакет о себе
заявляет. Вне проверки остались: разбивка волн в таблице `ROADMAP.md:36–46`
(девять чисел; сегодня все верны — пересчитал), `tokens` и `manifest` (правил
в `claims` нет), «10 страниц × 4 ширины» в BRIEF. Плюс мелочь в отчёте: сообщение
«Checked 13 documented counts» считает счётчиком и правило-якорь
`['ui/courses.css', /по файлу на категорию METHOD §5/, null]`, у которого числа
нет вовсе — фактически сверено 12 чисел и одна формулировка.

---

## Итого

blocker 0 · major 0 · minor 5 · note 4.

Все находки первой итерации закрыты фактически: 18 исправлено, 1 перенесена
строкой с обоснованием, которое я принимаю. Четыре `major` воспроизведены
и не подтвердились заново. Откат правки о брейкпоинтах выполнен полностью;
единственный оставшийся след лежит в чужом файле и записан строкой с владельцем.

Новые находки — только `minor` и `note`, ни одна не мешает пользоваться каркасом
и продолжать волну. Вердикт — **принять с замечаниями**; находки 20–24 уходят
строками в `ROADMAP.md`.

---

## Что проверено и претензий нет

- **`capture.mjs`.** Ошибка съёмки не глушится, уходит в `stderr`, ширина
  попадает в `failedWidths`, код 1; `meta.json` перечисляет только реально
  снятое, запрошенное лежит в `requestedWidths`; `--shot-timeout` 10 000 мс
  по умолчанию работает; шесть вариантов ключа без значения и нечисловой ширины
  дают код 2 с внятным сообщением; обычная съёмка `--widths 1440,320` — код 0,
  `widths` = `requestedWidths`.
- **Порт.** 4179 в `tools/serve.mjs:11`, `playwright.config.mjs` в обоих местах
  (`baseURL` и `webServer.url`). `career/` остаётся на 4178. Сервер печатает
  корень при старте; `curl` подтверждает, что отдаются файлы Курсов, а не
  соседа. Оба порта после моих прогонов свободны.
- **Валидаторы.** `validate-classes.mjs` и `validate-counts.mjs` перенесены,
  запускаются, дают заявленные коды (2 и 0). Диффы против образца показывают
  ровно описанные адаптации: рекурсивный `walk` с проверкой существования,
  свои таблицы, мягкий выход по отсутствующим файлам с названным шагом.
  Отказ от `check-evidence-age.mjs` обоснован верно — проверено чтением кода
  образца и раскладкой evidence Курсов.
- **Числа.** Шесть счётчиков пересчитаны независимо и сошлись; 76 id шагов
  уникальны, дублей нет; разбивка по волнам совпадает с таблицей волн построчно;
  сумма элементов по волнам 12+15+14+14 = 55.
- **`ui/courses.css` в браузере.** 14 ответов `.css`, все 200, ни одного 404
  на файлах пакета, ни одного `requestfailed`, консоль чистая кроме favicon.
  13 `@import` — ровно столько, сколько заявляет README; `@import` на
  несуществующий `state-contract.css` отсутствует, место под него обозначено
  комментарием.
- **Инвариант §6.2 в формулировках.** Переписан на классы во всех трёх местах:
  `ui/courses.css:17–23`, `showcase/.gitkeep`, exit criteria R0 в `ROADMAP.md`.
  Запрет копировать именование образца записан там же.
- **Волны в шапках `ui/components/*.css`.** `data-display` — «R2, `InfoTable` —
  R5-12», `forms` — «R3, `SearchForm` — R4-06»; таблица в `capture.md`
  исправлена так же. Остальные восемь файлов не тронуты и остаются верными.
- **`package.json`.** Скрипт `test` снят; пять оставшихся указывают на
  существующие файлы. `npx playwright test` падает именно на `webServer.url`,
  как описывает X-01.
- **Структура.** 13 `.gitkeep` — совпадает с заявленным; `test-results/`
  и `node_modules/` покрыты корневым `.gitignore`.
- **`career/` и `_sources/` не изменялись.** `find -newermt "2026-09-08 00:00"`
  по обоим деревьям — пусто.
- **Ловушка относительного `@import`** описана в шапках `ui/courses.css`,
  `ui/layout.css` и `ui/components/layout.css`.
- **Отступления от METHOD §2** (`docs/development/`, `evidence/source/storybook/`)
  названы в `capture.md` с обоснованием.

## Проверить не смог

- **Побайтность отката `BRIEF.md` §5.6 и §9.** Пакет вне git
  (`git ls-files courses/` пусто, `git status` показывает `?? courses/`),
  истории для сравнения нет, `BRIEF.md` менялся 8 сентября в 01:53. Проверил
  косвенно: текущий текст совпадает с формулировками, которые `fix-1.md`
  приводит как исходные, внутренне согласован с решением 2 в `ROADMAP.md`
  и не содержит ни опровергнутого вывода, ни добавленного блока измеренных
  фактов. Утверждение «слово в слово» подтвердить нечем — только «по смыслу
  и по цитатам».
- **`evidence/source/production/` и `.pipeline/R0-00/` как результат** — они
  в работе у соседнего агента. Читал оттуда только `media-queries.json`,
  `tokens.json` и списки файлов, ничего не менял; выводы о них помечены как
  снимок состояния на 8 сентября 02:15.
- **Figma и продакшен как источники** — у шага нет элемента продукта, сверять
  тройкой нечего. Раздел A чеклиста неприменим, а не пройден.
- **Инварианты §6.1 (копируемость разметки) и §6.4 (токены вместо литералов)** —
  в пакете ноль правил CSS и ноль спецификаций. Проверена только точка
  подключения, а не разметка: предмета ещё нет.
- **Витрина и её отделённость (§6.2 механически)** — `showcase/` пуст,
  `validate-classes.mjs` выходит с кодом 2. Первый настоящий прогон возможен
  на R0-06.
