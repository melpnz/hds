# Ревью R6 — первый круг, разбор находок

11 сентября 2026. Независимый ревьюер проверил шесть статей `pages/*.md`,
тексты секций `showcase/pages.html`, прозу `AuthorsBlock` и вариант
`LinkGrid`. Вердикт — вернуть на доработку: страницы воспроизводят продукт
верно, но одно утверждение о поведении ложно, а сверка `diff-page.mjs`
имела дыру. Находок: blocker 1 (процесс), major 2, minor 11, note 5.

| # | Находка | Что сделано |
|---|---|---|
| 1 | **blocker (процесс).** R6 собрана до закрытия R3–R5, решения владельца об этом в ROADMAP нет | Решение записано в ROADMAP, «Решения пользователя, 2026-09-11». Приёмка волны — вопрос владельцу |
| 2 | **major.** `rating.md`: «подсветка строки на телефоне снята `phone:hover:bg-none`» — ложно: класс снимает `background-image`, подсветка — `background-color` | Исправлено: подсветка остаётся |
| 3 | **major.** `diff-page.mjs` молча пропускал узел, видимый с одной стороны и скрытый с другой, и узлы без пары; ревьюер снял `phone:hidden` у иллюстрации — «0 расхождений» | Сверка собирает все узлы; разная видимость, другие классы и узел витрины без пары — расхождения. Контроль тем же внесением ошибки: 2 расхождения. Все шесть страниц — 0 на пяти ширинах |
| 4–6 | Неверные числа: секция «Популярные курсы» 606 (580), панель экспертов на 375 1029 (1049), таблица рейтинга на 768 1426 (1442) | Исправлены в статьях и `pages.html` |
| 7 | Высоты страниц мерились без шрифта Inter; объяснение «разница копится в переносах» ложно | `layout.mjs` перенесён в пакет (`.pipeline/pages/layout.mjs`) и рисует со шрифтом и снятым скоупом; высота совпадает со скриншотами до пикселя в 23 случаях из 30, остальные — в пределах 1,1% (1024 без своего `dom-1024.html` и 768). Все таблицы и абзацы пересчитаны |
| 8 | `education-center.md`: на 320 полоса растёт из-за описания | Исправлено: переносится строка оценки; описание в четыре строки уже на 375 |
| 9 | `courses-listing.md`: «ничего не переставляет и не прячет, кроме шапки» | Исправлено: названы картинки `AdCard` и списки подвала |
| 10 | Форма «встаёт в столбец» — механизм не назван | Исправлено: ряд полей скрыт, показан дубль в одну колонку |
| 11 | «Классов, которых нет ни в одной записи» — устарело после R5-18 | «на момент сборки не было» |
| 12 | Кандидат «48 — 6/10, на трёх 40» не сходится: 6 + 3 = 9; `gap-12` у 7 страниц | В статьях — пересчёт R6: 7/10 с `gap-12`, 3 с `gap-10`; число инвентаризации названо как её |
| 13 | Сетка курсов страницы школы и пункт опыта профиля — модули без записи и без строки в ROADMAP; решение «модули страниц — записями» не записано | Решение записано; строки X-103 и X-104 |
| 14 | `link-grid.md`: «окно 94px» — у промокодов 182 | «окно свёртки (94 или 182)» |

Note-замечания: продовые URL в разметке `authors-block.md` — конвенция
пакета (как у `PersonCard`); шаблонные огрехи генератора спецификаций («В
«Анатомии» ниже», «3 ссылок») — в 14 записях, не специфичны для R6;
`cascade-check.mjs` берёт позицию класса по первому появлению во всём
корпусе — метод хрупкий, результат сейчас 0; обратное направление
`hydration-diff` ревьюер прогнал сам — 0; в отчёте `/courses` неверно
«сетки в `computed*.json` нет» — исправлено.
