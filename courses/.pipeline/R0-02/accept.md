# Приёмка R0-02 — `ui/tokens.css`, слой переменных

Вердикт: **ПРИНЯТО**
Дата: 8 сентября 2026.

Итераций ревью — три. Последнее (`review-3.md`) — «принять с замечаниями»:
0 blocker, 0 major, 2 minor, 5 note. Оба minor и все пять note переведены
в строки долга `ROADMAP.md`; ни один из них не про слой значений.

---

## Гейты

Каждый прогнан приёмкой заново, своим запуском. Выдача скопирована из терминала.

### 1 · Валидатор реестра — ЗЕЛЁНЫЙ

```
$ node tools/validate-components.mjs --strict
Validated 55 component records.
Status counts: {"complete":0,"partial":0,"planned":55,"legacy-only":0,"figma-only":0}
Source scope counts: {"production":44,"storybook-only":5,"figma-only":6}

Отложено (3) — предмета ещё нет:
- showcase/components.html — витрины ещё нет (R0-06); якорей не проверено: 0 объявлено из 55
- requiredStates — матрицы обязательных состояний ещё нет (R1-01); у 55 записей список пуст
- спецификации — 55 записей в статусе planned; сходимость реестра (METHOD §6.5) проверяется на них не полностью

Объявлено (3) — известно, названо в манифесте, не закрыто:
- componentKey 9668fb89eb4a58bc781b568ec83d9ce0e492e70e у записей filter-chip, tab — инвентаризация записала один ключ и FilterChip, и Tab: либо это один component set, либо ошибка переноса. Разводится на R3-04 и R3-13
- зависимость верстается позже: site-header (R4-01) → header-dropdown (R4-02)
- зависимость верстается позже: page-hero (R4-05) → search-form (R4-06)

Manifest structure is valid.
                                                                     КОД 0
```

Разбивка `production/storybook-only/figma-only` — 44/5/6, тогда как
`review-3.md` записал 45/5/5. Это **не дефект R0-02**: `components/manifest.json`
прямо сейчас правит параллельный шаг R0-05, и цифры разошлись между прогоном
ревью и прогоном приёмки. Код возврата 0 в обоих случаях, структура манифеста
валидна, R0-02 в `components/` не писал ни разу.

### Счётчики документации — ЗЕЛЁНЫЙ

```
$ node tools/validate-counts.mjs
Checked 21 documented counts against the package. All match.
  roadmapSteps: 76
  roadmapR0: 7
  elements: 55
  pages: 10
  unreachable: 8
  cssCategories: 10
  tokens: 51
  manifest: 55
  specs: 0
                                                                     КОД 0
```

Прогнан дважды: до правок `ROADMAP.md` / `CHANGELOG.md` этой приёмкой и после
них — код 0 оба раза.

Правило на число токенов (X-19, работа этого шага) проверено **на красном**
самой приёмкой: в `ui/tokens.css` временно поставлено «Всего переменных: 52».

```
$ node tools/validate-counts.mjs
Счётчики разошлись (1):

  ui/tokens.css: заявлено 52, на самом деле 51 (tokens)

Правьте документацию — или правило в tools/validate-counts.mjs,
если изменилась формулировка.
                                                                     КОД 1
```

Файл восстановлен из копии и сверен `diff` — байт в байт исходный.

### Классы разметки — код 2, предмета нет

```
$ node tools/validate-classes.mjs
Витрин ещё нет. Проверять пока нечего.
  showcase/components.html — заводит шаг R0-06
  showcase/pages.html — заводит шаг R6
                                                                     КОД 2
```

Код 2 у этого инструмента означает «проверять нечего», а не «проверка упала»:
витрина заводится шагом R0-06. Гейтом R0-02 он не является и зелёным на этом
шаге быть не может. Ветка `tools/validate-classes.mjs:42–46` не менялась
ни этим шагом, ни R0-05 — сверено с `git show HEAD`.

**Важно для протокола:** `fix-2.md` цитирует этот же прогон как «КОД 0»
с одной строкой. Такой выдачи не было — см. находку 2 ревью-3 и строку X-41.

### Синтаксис инструментов — ЗЕЛЁНЫЙ

```
$ node --check tools/*.mjs
OK tools/capture.mjs
OK tools/measure-selectors.mjs
OK tools/serve.mjs
OK tools/validate-classes.mjs
OK tools/validate-components.mjs
OK tools/validate-counts.mjs
                                                                     КОД 0
```

Шесть файлов, а не пять: `tools/measure-selectors.mjs` завёл параллельный
шаг R0-05. В коммит R0-02 он не берётся.

### 2 · Браузерные проверки — предмета нет

`npm test` в пакете не заведён (`package.json` не имеет скрипта `test`,
`tests/` пуста). Это не пропуск шага: скрипт снят на доработке R0-01 как
ложное обещание и держится строкой X-01, исполнимой не раньше R0-06.

### 3 · Ревью закрыто — ДА

`review-3.md`: blocker 0, major 0. Major итерации 2 («подставленная граница
744 вместо 767/768») закрыт и перепроверен ревьюером `grep`-ом по всему
пакету. Сверено приёмкой: каждая из семи находок `review-2.md` имеет
собственный раздел в `fix-2.md` (`## 1 · major` … `## 7 · note`), плюс два
раздела на отдельные задачи приёмки — X-37 и X-38.

### 4 · Реестр сходится — неприменимо

R0-02 — слой значений, записи в `manifest.json` у него нет и быть не может.
Сходимость реестра как таковая проверена гейтом 1 (код 0).

### 5 · Evidence на месте — ДА, и сверено со слоем

`evidence/source/production/pages/<id>/tokens.json` есть на всех десяти
страницах. Приёмка сверила слой с evidence прямым счётом:

```
author                     51  swiper:нет
authors                    51  swiper:нет
courses-listing            52  swiper:да
editors                    51  swiper:нет
education-center           52  swiper:да
education-centers-listing  52  swiper:да
promocodes                 52  swiper:да
rating                     52  swiper:да
reviews                    52  swiper:да
schools-for-children       52  swiper:да
страниц со swiper: 7/10

в слое: 51 · в evidence courses-listing: 52
в слое, но не в проде: (нет)
в проде, но не в слое: --swiper-theme-color
```

То есть в слое ровно то, что объявляет продукт, минус одна переменная чанка
Swiper, и она действительно на семи страницах из десяти. Главное утверждение
шага — «51, а не 52» — подтверждено независимо от отчётов.

Отдельно пересчитаны объявления в `ui/tokens.css`: 55 строк вида `--имя:`,
из них 4 — внутри комментариев (протокол снятого: мёртвое
`--header-height: 112px` и три строки расхождения шкалы). Реальных
объявлений 51, уникальных имён 51.

### 6 · Копируемость (METHOD §6.1) — ЗЕЛЁНЫЙ

Пустая страница, единственная ссылка на `ui/courses.css`, msedge,
`getComputedStyle` по всем 51 имени слоя:

```
имён в слое: 51 · разрешилось: 51 · пустых: 0
  --color-text-main              → #2c2e34
  --color-links-main             → #346ef4
  --header-height                → 64px
  --color-main-gradient-first    → linear-gradient(180deg,#346ef4 0%,#346ef4 100%)
  --font-size-h3                 → 21px
  --line-height-h3               → 27px
запросы: {"200":14}    404: нет
```

Четырнадцать запросов — сам `ui/courses.css` и тринадцать его `@import`.
Ни одного 404. Числа сошлись с `review-3.md` в точности.

### 7 · Exit criteria волны — не проверялись

По решению вызывающего: волна R0 закрыта на 3 шага из 7. Проверка exit
criteria — на границе волны.

### 8 · Честность статуса — ДА

Слой не выдаёт мёртвое за живое. У каждой из 51 переменной проставлена
пометка живости — 26 без пометки, 15 `[NO MARKUP]`, 9 `[UNREFERENCED]`,
1 `[RUNTIME]`; пометки взаимоисключающие, порядок применения назван,
единственное пересечение (`--header-height`) разобрано отдельно. Ненаблюдённое
названо ненаблюдённым: значения Figma `Header/H3` не сняты и в выводы не
входят; полоса 745…767 объявлена неснятой; токены на 1024 отсутствуют (X-09).
Три «конфликта источников» переформулированы по измеренному в расхождения
внутри прода — это и есть главный смысловой результат шага.

Одна оговорка — не про слой, а про отчёт: цитата несуществующей выдачи
в `fix-2.md`. Заведена строкой X-41.

### 9 · Две проекции — неприменимо

`machine/` пуста, `validate-machine.mjs` в пакете нет (заводит волна R8).
Разойтись пока нечему.

### 10, 11 — неприменимы

Правила (R7) и страницы (R6) этим шагом не затрагивались.

---

## Что сделано приёмкой

**`ROADMAP.md`**

- R0-02: `review` → **`done`**.
- «Где пакет сейчас»: принято шагов 2 → **3**; заведена строка «Слой токенов»
  с составом и разметкой живости; строка «Проверки» уточнена — `validate-counts`
  сторожит 21 число, `validate-classes` до витрины отдаёт код 2.
- Закрыты: **X-02** (остаток — обе строки `README.md` про набор ширин),
  **X-19** (правило на число токенов, семь мест, плюс срезание комментариев
  в счётчике), **X-24** и **X-35** (имена префиксов в `BRIEF.md` §5 п. 6
  и `ui/layout.css`), **X-37** (дефолт `capture.mjs` — восемь ширин, защиты
  не сломаны).
- Сужены: **X-10** — снята оговорка «вывод, а не извлечение» с `phone:`,
  `tablet-only:` и сверх строки `tablet:`; остаток — `desktop:` (в корпусе
  `/courses` ноль селекторов) и несохранённый артефакт извлечения.
  **X-38** — переформулирована в «прошлые снимки остаются с CRLF»: механизм
  починен, семь файлов `courses-listing` с `CR=11` остались.
- Дополнены: **X-09** (кроме `computed` не снят и `tokens` на 1024),
  **X-27** (у строки появился адрес — `README.md:103` называет заменённый
  `media-queries.json`).
- Заведены новые: **X-39** (извлечение верно, но не воспроизводимо из пакета:
  `rulesInsideQueries` держит 4 запроса из 9, корпус не закреплён — 124 267
  против 124 136 символов у двух замеров), **X-40** (метод описан слабее
  измеренного; 11 против 12 внешних `.css` — два корпуса, нигде не разведённые),
  **X-41** (процессная находка: цитата выдачи прогона, которого не было),
  **X-42** (`actual.tokens` считает объявления во всём файле, а не имена
  внутри `:root`), **X-43** (место статьи: `docs/guide/` против `evidence/`),
  **X-44** (`capture.mjs` не отличает `:root` продукта от `:root` библиотеки),
  **X-45** (`--color-ui-black-transparent-300` без носителя),
  **X-46** (пометки живости стареют вместе со сборкой),
  **X-47** (что именно и на основании чего снимать в `.pipeline/capture-log.md`,
  `components/manifest.json` → `breakpoints` и `components/SPEC-TEMPLATE.md` —
  два последних заняты параллельным R0-05).

Сверено с 38 существовавшими строками: задвоений нет. Кандидаты `capture.md`
§10, совпавшие с уже заведёнными, влиты в них (X-09, X-27), а не продублированы.

**`CHANGELOG.md`** — запись в `## Unreleased`: слой из 51 переменной и почему
не 52; разметка живости как главное содержание слоя; статья `docs/guide/tokens.md`;
правило на число токенов. В «Исправлено» — три переформулированных «конфликта
источников», снятое утверждение про Figma `Header/H3`, граница 767/768 вместо
подставленной 744, извлечённые границы пяти префиксов, восемь ширин по умолчанию
и LF в артефактах.

---

## Проверка на «цитаты выдачи» в остальных отчётах шага

По находке 2 ревью-3 приёмка прогнала все отчёты `.pipeline/R0-02/` на предмет
приведённых выдач команд:

| Файл | Что цитирует | Сходится с фактом |
|---|---|---|
| `capture.md` §7 | `validate-counts` (код 0), `node --check` (код 0); отдельно — `validate-components --strict` код 0 и `validate-classes` **код 2, «витрин ещё нет»** | да |
| `capture.md` §6 | красный прогон `validate-counts` с подменой числа на 52 → код 1 | да, воспроизведено приёмкой |
| `fix-1.md` | `validate-counts` код 0, `validate-components` код 0, `validate-classes` **«КОД: 2 (ожидаемо: showcase заводит R0-06)»**; восемь мутаций счётчика | да |
| `fix-2.md` | `validate-counts` код 0, `node --check` код 0 | да |
| `fix-2.md` | `validate-classes` — **«КОД 0», одна строка** | **нет** — фактически код 2 и три строки (X-41) |
| `fix-2.md` | копируемость «17 × 200» | расхождение способа замера, не пакета: при отдаче страницы по HTTP добавляются три запроса оболочки; число имён и значения совпали |

Подставленная выдача — одна, в одном файле. Ни `fix-1.md`, ни `capture.md`
той же ошибки не делают: оба пишут для этого инструмента честный код 2.

---

## Состав коммита

Взято:

```
courses/BRIEF.md
courses/CHANGELOG.md
courses/README.md
courses/ROADMAP.md
courses/docs/guide/tokens.md
courses/tools/README.md
courses/tools/capture.mjs
courses/tools/validate-counts.mjs
courses/ui/courses.css
courses/ui/layout.css
courses/ui/tokens.css
courses/.pipeline/R0-02/
```

Не взято — работа параллельного шага R0-05, не завершённого и не принятого:

```
courses/components/INDEX.md
courses/components/SPEC-TEMPLATE.md
courses/components/manifest.json
courses/components/selector-census.json
courses/tools/validate-components.mjs
courses/tools/validate-classes.mjs
courses/tools/measure-selectors.mjs
courses/.pipeline/R0-05/
```

`career/` и `_sources/` не трогались.
