# ButtonGroup

> Исторический `SegmentedControl` объединён с этим компонентом как
> `variant="hero"`. Светлый вариант соответствует `variant="light"`.

| | |
|---|---|
| **Категория** | Навигация (`navigation`) |
| **Корневой класс** | `crs-button-group` — введён пакетом |
| **CSS** | `ui/components/navigation.css` |
| **Живая реализация** | [`viewer/index.html#button-group`](../../../viewer/index.html#button-group) |
| **Источник** | Figma `02_Education-NEW`, узел `14644:221762` «Tab Button Group — инстанс button group / onpage» |
| **Статус** | `figma-only` — терминальный |

## Когда использовать

**В продукте этого компонента нет.** Он существует только в макете, и это
не пробел съёмки, а установленный факт — разбор в `notes` записи реестра:

> Вариант `light` — button group / onpage: серая подложка и белый выбранный сегмент с тенью. Вариант `hero` — button group / onheader на цветном фоне. Компонент используется в CatalogMenu, FeedbackForm и SiteHeader.

Правило применения не выводится: выводить его не из чего, пока компонент
не появился в продукте. Шаг R3-16 решает судьбу записи.

## Разметка

Написана по узлу макета, а не снята. Вставляется на пустую страницу с одним
`ui/courses.css` и выглядит так же, как на витрине (инвариант METHOD §6.1).

```html
<div style="width:270px;max-width:100%">
  <div class="crs-button-group">
    <button type="button" class="crs-button-group__item crs-button-group__item--selected" aria-pressed="true">Для взрослых</button>
    <button type="button" class="crs-button-group__item" aria-pressed="false">Для детей</button>
  </div>
</div>
```

## Внешний вид

| что | значение |
|---|---|
| группа | не ниже 48, фон `#f1f1f1`, падинг 4, радиус 16 |
| сегмент | падинг 8 / 16, радиус 12, текст 14 / 20 `#2c2e34`, по центру; сегменты делят ширину поровну |
| выбранный | белый, тень `Dropdown`: 0 4 6 −1 и 0 2 4 −2, обе `#0000000d` |

Все значения — переменные слоя `ui/tokens-figma.css`, снятого с того же файла
макета. Это единственное место пакета, где вёрстка ссылается на слой макета:
у записи нет продуктового источника, и ссылаться больше не на что. Прод-записи
ссылаются на `--color-ui-*` из `ui/tokens.css`.

## Ограничения

- **Корень введён пакетом.** `crs-button-group` — не класс продукта. METHOD §6.5
  допускает корень вида `crs-<id>` ровно для этого случая: снятой разметки,
  из которой можно взять настоящее имя, не существует. Префикс `crs-` нужен,
  чтобы имя пакета нельзя было принять за класс продукта.
- **Тексты.** Подписи — из инстанса в каталоге: «Для взрослых» (выбран) и «Для детей». Тот же компонент стоит в форме обратной связи с подписями «Телеграм» и «Телефон». Обёртка шириной 270 — ширина левой колонки каталога, оформление витрины.
- **Ассеты.** Ассетов нет. Тень собрана из четырёх переменных `effect/drop shadow 1|2/*` слоя — композитный эффект `Dropdown` одной переменной не разворачивается.
- **Состояния.** Требуемые состояния записи (`default`, `hover`, `focus-visible`, `selected`) в макете отдельными вариантами не нарисованы, а измерить их негде. Разбор — [`components/STATE-CAPTURE.md`](../STATE-CAPTURE.md).
- **Статус терминальный.** Правило манифеста: `sourceScope: figma-only` не доходит до `complete`/`partial`. Пока компонент не найден в продукте, запись остаётся `figma-only`, сколько бы вёрстки к ней ни прибавилось.

## Источники

**Figma.** `02_Education-NEW` (`oNyNRRob2y0ZSgPHOdH65X`), узел `14644:221762`
«Tab Button Group — инстанс button group / onpage», прочитан `get_design_context` 11 сентября 2026.

**Production.** Разметки нет — это и есть содержание записи.

---

_Собрано bulk-проходом `.pipeline/R2-bulk/gen-figma-only.mjs` по узлу макета._


## Уточнение v0.2 · PageTabs

Пользовательское имя компонента — `PageTabs`; исторический id `button-group` сохранён. Это табы для светлого фона страницы, карточки или формы. В viewer показаны `default`, `hover`, `focus-visible`, `selected`, `disabled` и `loading` по Courses Figma `education-lib`, node `4813:238`. Группа сегментов лежит на единой подложке `#F1F1F1` с радиусом 16 px и внутренним отступом 4 px по node `4813:288`; выбранный белый сегмент и его тень накладываются поверх неё.
