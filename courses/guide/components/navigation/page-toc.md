# PageToc

| | |
|---|---|
| **Категория** | Навигация (`navigation`) |
| **Корневой класс** | `crs-page-toc` — введён пакетом |
| **CSS** | `ui/components/navigation.css` |
| **Живая реализация** | [`viewer/index.html#page-toc`](../../../viewer/index.html#page-toc) |
| **Источник** | Figma `02_Education-NEW`, узел `12958:143535` «меню-оглавление» |
| **Статус** | `complete` · нормативный Figma-контракт v0.2 |

## Когда использовать

Используйте как оглавление длинной страницы. Компонент занимает доступную ширину контейнера и наследует общие hover/focus-visible состояния ссылок Courses.

## Разметка

Написана по узлу макета, а не снята. Вставляется на пустую страницу с одним
`ui/courses.css` и выглядит так же, как на витрине (инвариант METHOD §6.1).

```html
<nav class="crs-page-toc" aria-label="На странице">
  <p class="crs-page-toc__title">На странице</p>
  <div class="crs-page-toc__list">
    <a href="#c-page-toc" class="crs-page-toc__link">Как выбрать и купить обучение?</a>
    <a href="#c-page-toc" class="crs-page-toc__link">Когда лучше покупать онлайн-курс?</a>
    <a href="#c-page-toc" class="crs-page-toc__link">Часто задаваемые вопросы</a>
    <a href="#c-page-toc" class="crs-page-toc__link">Как мы отбираем лучшие курсы<br>и онлайн-школы</a>
    <a href="#c-page-toc" class="crs-page-toc__link">Ответственная информация</a>
  </div>
</nav>
```

## Внешний вид

| что | значение |
|---|---|
| заголовок | «На странице» стилем `Header/H4` — 18 / 22, 600, −0.5 |
| ссылки | 14 / 20, `#346ef4` — `color style/font/link`, через 12 |
| до списка | 24; корень и список растягиваются на доступную ширину контейнера, ручной перенос из исходного кадра на desktop не сохраняется |

Все значения — переменные слоя `ui/tokens-figma.css`, снятого с того же файла
макета. Это единственное место пакета, где вёрстка ссылается на слой макета:
у записи нет продуктового источника, и ссылаться больше не на что. Прод-записи
ссылаются на `--color-ui-*` из `ui/tokens.css`.

## Решение v0.2

Используйте как оглавление длинной страницы. Компонент занимает доступную ширину контейнера и наследует общие hover/focus-visible состояния ссылок Courses.

Корень с префиксом `crs-` остаётся именем пакета, а отсутствие production-разметки явно сохраняется в provenance. Это не мешает использовать принятый Figma-контракт как нормативный.

## Источники

**Figma.** `02_Education-NEW` (`oNyNRRob2y0ZSgPHOdH65X`), узел `12958:143535`
«меню-оглавление», прочитан `get_design_context` 11 сентября 2026.

**Production.** Разметки нет — это и есть содержание записи.

---

_Собрано bulk-проходом `.pipeline/R2-bulk/gen-figma-only.mjs` по узлу макета._
