# SpecializationTag

| | |
|---|---|
| **Категория** | Отображение данных (`data-display`) |
| **Корневой класс** | `crs-specialization-tag` — введён пакетом |
| **CSS** | `ui/components/data-display.css` |
| **Живая реализация** | [`viewer/index.html#specialization-tag`](../../../viewer/index.html#specialization-tag) |
| **Источник** | Figma `02_Education-NEW`, узел `14089:190182` «tag-specialization» |
| **Статус** | `figma-only` — терминальный |

## Когда использовать

**В продукте этого компонента нет.** Он существует только в макете, и это
не пробел съёмки, а установленный факт — разбор в `notes` записи реестра:

> figma-only: цветной тег специализации со страницы профессии и из вакансии — «Конкуренция» (красный), «Умеренный вход» (жёлтый), «Высокий спрос» (синий). В продукте раздела профессий нет. Нейтральный тег макета — это продуктовый Chip. Заведена 11 сентября 2026 решением владельца: компоненты макета, которых нет в продукте, входят в пакет как figma-only.

Правило применения не выводится: выводить его не из чего, пока компонент
не появился в продукте. Шаг R2-13 решает судьбу записи.

## Разметка

Написана по узлу макета, а не снята. Вставляется на пустую страницу с одним
`ui/courses.css` и выглядит так же, как на витрине (инвариант METHOD §6.1).

```html
<div style="display:flex;gap:4px;flex-wrap:wrap">
  <span class="crs-specialization-tag crs-specialization-tag--hard">Конкуренция</span>
  <span class="crs-specialization-tag crs-specialization-tag--medium">Умеренный вход</span>
  <span class="crs-specialization-tag crs-specialization-tag--easy">Высокий спрос</span>
</div>
```

## Внешний вид

| что | значение |
|---|---|
| высота | 24, падинг 4 / 8 (4 тега + 4 текстового блока), пилюля |
| текст | 12 / 16, 400 |
| --hard | фон `#fce9e9`, текст `#e84444` — «Конкуренция» |
| --medium | фон `#fff2e2`, текст `#ff7e47` — «Умеренный вход» |
| --easy | фон `#eff5ff`, текст `#346ef4` — «Высокий спрос», в вакансии — грейд «Middle» |

Все значения — переменные слоя `ui/tokens-figma.css`, снятого с того же файла
макета. Это единственное место пакета, где вёрстка ссылается на слой макета:
у записи нет продуктового источника, и ссылаться больше не на что. Прод-записи
ссылаются на `--color-ui-*` из `ui/tokens.css`.

## Ограничения

- **Корень введён пакетом.** `crs-specialization-tag` — не класс продукта. METHOD §6.5
  допускает корень вида `crs-<id>` ровно для этого случая: снятой разметки,
  из которой можно взять настоящее имя, не существует. Префикс `crs-` нужен,
  чтобы имя пакета нельзя было принять за класс продукта.
- **Тексты.** Три подписи — три варианта компонента макета по свойствам «спрос / вход» × «сложно / средне / чил». Модификатор назван по второму свойству. Нейтрального варианта у записи нет: серый тег макета (`tag/text`) — продуктовый `Chip`. Обёртка ряда — оформление витрины.
- **Ассеты.** Ассетов нет. Цвета — из слоя макета; у красного и жёлтого текста есть продуктовые двойники (`--color-ui-red-500`, `--color-ui-orange-500`), у подложек `red/50` и `yellow/50` — нет.
- **Состояния.** Требуемые состояния записи (`default`) в макете отдельными вариантами не нарисованы, а измерить их негде. Разбор — [`components/STATE-CAPTURE.md`](../STATE-CAPTURE.md).
- **Статус терминальный.** Правило манифеста: `sourceScope: figma-only` не доходит до `complete`/`partial`. Пока компонент не найден в продукте, запись остаётся `figma-only`, сколько бы вёрстки к ней ни прибавилось.

## Источники

**Figma.** `02_Education-NEW` (`oNyNRRob2y0ZSgPHOdH65X`), узел `14089:190182`
«tag-specialization», прочитан `get_design_context` 11 сентября 2026.

**Production.** Разметки нет — это и есть содержание записи.

---

_Собрано bulk-проходом `.pipeline/R2-bulk/gen-figma-only.mjs` по узлу макета._
