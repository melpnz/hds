# Опись доказательной базы · Курсы

Что снято, когда и сколько этого на диске. Файл собирается
`.pipeline/principles/gen-coverage.mjs` из самой папки `evidence/` и
`components/manifest.json` — числа не пишутся руками. Что пакет из этого
выводит и где знание кончается — [`docs/guide/coverage.md`](../docs/guide/coverage.md).

Всего файлов доказательной базы: **432**, 191.5 МБ, из них 101 скриншот.

## Продакшен — снято гостем

Каждая страница снята в нескольких окнах: `dom*.html` — разметка,
`computed*.json` — вычисленные стили, `tokens*.json` — переменные `:root`,
`meta*.json` — условия съёмки, `*.png` — скриншот.

| страница | ширины `dom` | файлов | объём |
|---|---|---|---|
| `author` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 6587 КБ |
| `authors` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 16.0 МБ |
| `courses-listing` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 65.4 МБ |
| `editors` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 5587 КБ |
| `education-center` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 14.4 МБ |
| `education-centers-listing` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 26.0 МБ |
| `promocodes` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 15.4 МБ |
| `rating` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 9289 КБ |
| `reviews` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 11.8 МБ |
| `schools-for-children` | 320, 375, 479, 480, 744, 768, 1440 | 38 | 20.4 МБ |

Корпус CSS — 10 инлайновых файлов (по одному на страницу, 993 КБ) и 12 внешних (21 КБ). Правила в них написаны с атрибутом области видимости Vue (`[data-v-…]`); все инструменты пакета снимают его перед разбором.

Отдельно лежит `evidence/curated/` — материал, собранный пакетом, а не снятый с продукта: сейчас это контрольная раскладка спрайта значков (`sprite-icon/copy-check-400x200.png`).

Замеры границ: `media-queries.json` и `media-queries-cold.json` — холодная проверка ширин, на которых меняется раскладка.

## Storybook — локальный снимок

13 файлов CSS сборки (103 КБ) и 7 снятых story (6 КБ), опись — `evidence/source/storybook/inventory.md`. Снимок read-only: пакет из него берёт только то, чего нет в продакшене, и помечает `storybook-only`.

## Figma — файлов нет

Папка `evidence/source/figma` пуста: макет читается через MCP, а не выгружается. Адрес каждого узла лежит в записи реестра, поле `figmaEvidence`: файл, имя слоя, `nodeId` или `componentKey`. Записей с адресом в Figma — 47 из 72. Страницы компонентов библиотеки `education-lib` через MCP не открываются (`docs/guide/tokens.md`, GAP-6), поэтому часть узлов адресуется только ключом компонента.

## Записи реестра

| разрез | значения |
|---|---|
| всего записей | 72 |
| источник (`sourceScope`) | production — 47; figma-only — 21; storybook-only — 4 |
| статус | complete — 39; figma-only — 21; partial — 12 |
| с адресом в продакшене | 47 |
| с адресом в Storybook | 13 |
| с адресом в Figma | 47 |
| с записанным конфликтом источников | 3 |

Состояния: требуется 187, снято 148, не снято 37. Какие именно не сняты — в поле `uncapturedStates` каждой записи.

Записи с конфликтом источников:

| запись | ссылка | о чём |
|---|---|---|
| `button` | BRIEF §5, конфликт 4 | padding размеров L и XL: прод 16/8 и 24/12; Figma elements/button/ 20/12 и 24/16 |
| `filter-chip` | BRIEF §5, конфликт 1 | выбранное состояние: прод bg #2c2e34 + белый текст; Figma и токены --color-chip-inactive #ebf3ff / --color-chip-press #b8d5ff описывают синее. Решать по METHOD §3, расхождение — в «Ограничения» |
| `site-header` | BRIEF §5, конфликт 2 | --header-height: прод 64, сборка Storybook 112 (мобильный 144) — расхождение сборок |

## Замеры принципов (R7)

Правила уровня страницы стоят на `.pipeline/principles/axes.json`: 10 страниц × 4 ширины (375, 768, 1024, 1440), по 17 осям. Файл пересобирается одним запуском `measure-axes.mjs`, таблицы — `gen-evidence.mjs` → `.pipeline/principles-evidence.md`. Страницы рисуются из того же `dom*.html` и корпуса CSS, что описаны выше, со шрифтом Inter из `ui/assets/fonts` и без скрипта.

## Что снято не было

- Прод ради принципов и ради страниц повторно не открывается: шаги R6 и R7
  работают на снимках инвентаризации (METHOD, skills `guide-pages` и
  `guide-principles`).
- Ассеты раздела экспертов (спрайт `experts.svg`, иллюстрация и заглушка
  логотипа) в `ui/assets/` не лежат — строка X-102 роадмапа; на витрине
  страницы они стоят заглушкой.
- Ширина 1024 у токенов и у замеров шкалы отсутствует (`tokens.md` GAP-8,
  `typography.md` GAP-4); у принципов 1024 снята.
